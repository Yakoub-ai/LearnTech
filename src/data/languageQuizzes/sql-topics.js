export const topicQuizzes = {
  beginner: [
    {
      topicId: 'select-fundamentals',
      topicTitle: 'SELECT Fundamentals',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is the recommended practice when selecting columns in production SQL queries?',
          options: [
            'Always use SELECT * for simplicity',
            'Select only the specific columns you need',
            'Select all columns but filter with WHERE',
            'Use SELECT TOP to limit columns',
          ],
          correctIndex: 1,
          explanation: 'Selecting only needed columns reduces network transfer, memory usage, and makes queries self-documenting. SELECT * in production is an anti-pattern because schema changes can break your application.',
        },
        {
          question: 'What does the ORDER BY clause do when no direction is specified?',
          options: [
            'Sorts in descending order',
            'Returns rows in insertion order',
            'Sorts in ascending order (ASC is the default)',
            'Throws a syntax error',
          ],
          correctIndex: 2,
          explanation: 'ASC is the default sort direction in ORDER BY. Without specifying ASC or DESC, results are sorted in ascending order.',
        },
        {
          question: 'What is the purpose of OFFSET in a query like "LIMIT 20 OFFSET 20"?',
          options: [
            'It limits the result to 20 rows',
            'It skips the first 20 rows before returning results',
            'It returns rows 1 through 20',
            'It sets a timeout of 20 milliseconds',
          ],
          correctIndex: 1,
          explanation: 'OFFSET skips a number of rows before starting to return results. LIMIT 20 OFFSET 20 returns rows 21-40, which is page 2 of a 20-items-per-page result set.',
        },
        {
          question: 'What does SELECT DISTINCT do?',
          options: [
            'Selects only NULL values',
            'Selects the first occurrence of each row',
            'Eliminates duplicate rows from results',
            'Selects rows with distinct primary keys only',
          ],
          correctIndex: 2,
          explanation: 'DISTINCT eliminates duplicate rows from the result set. When applied to multiple columns, it returns unique combinations of those columns.',
        },
        {
          question: 'In the logical processing order of SQL, which clause is processed first?',
          options: ['SELECT', 'WHERE', 'FROM', 'ORDER BY'],
          correctIndex: 2,
          explanation: 'The database processes FROM first to identify the data source, then WHERE to filter rows, then GROUP BY/HAVING, then SELECT, and finally ORDER BY and LIMIT.',
        },
        {
          question: 'What is a column alias in SQL?',
          options: [
            'An alternative table name used in JOIN conditions',
            'A temporary name assigned to a column in the result set using AS',
            'A shortcut for frequently used column names',
            'A reference to a column in another table',
          ],
          correctIndex: 1,
          explanation: 'A column alias, defined with the AS keyword, assigns a temporary name to a column in the result set. For example, "salary * 12 AS annual_salary" names the computed column annual_salary.',
        },
        {
          question: 'Why is OFFSET-based pagination considered slow on large datasets?',
          options: [
            'OFFSET is not supported in most databases',
            'The database must scan and discard all preceding rows before returning results',
            'It cannot be combined with ORDER BY',
            'It requires a full table scan regardless of indexes',
          ],
          correctIndex: 1,
          explanation: 'OFFSET requires the database to scan through and discard all rows before the offset position. At large offsets (e.g., OFFSET 1000000), this becomes very slow. Cursor-based pagination is preferred at scale.',
        },
      ],
    },
    {
      topicId: 'filtering-and-sorting',
      topicTitle: 'Filtering & Sorting',
      objectiveIndex: 1,
      questions: [
        {
          question: 'What is the correct way to check for NULL values in a WHERE clause?',
          options: [
            'WHERE phone = NULL',
            'WHERE phone == NULL',
            'WHERE phone IS NULL',
            'WHERE ISNULL(phone)',
          ],
          correctIndex: 2,
          explanation: 'NULL represents the absence of a value and cannot be compared with = or ==. You must use IS NULL or IS NOT NULL. The expression "phone = NULL" never returns true because NULL is not equal to anything, including itself.',
        },
        {
          question: 'AND and OR are used together in a WHERE clause. Which operator has higher precedence?',
          options: ['OR has higher precedence', 'AND has higher precedence', 'They have equal precedence', 'Precedence depends on the database'],
          correctIndex: 1,
          explanation: 'AND has higher precedence than OR, meaning it binds more tightly. This can cause bugs: "WHERE a = 1 OR b = 2 AND c = 3" is evaluated as "a = 1 OR (b = 2 AND c = 3)". Always use parentheses when mixing AND and OR.',
        },
        {
          question: 'What does BETWEEN 10 AND 50 mean in a WHERE clause?',
          options: [
            'Values greater than 10 and less than 50 (exclusive)',
            'Values greater than or equal to 10 and less than or equal to 50 (inclusive)',
            'Values exactly equal to 10 or 50',
            'Values between 10 and 50 exclusive of 10 only',
          ],
          correctIndex: 1,
          explanation: 'BETWEEN is inclusive on both ends. "price BETWEEN 10 AND 50" is equivalent to "price >= 10 AND price <= 50".',
        },
        {
          question: 'In LIKE pattern matching, what does the % wildcard represent?',
          options: [
            'Exactly one character',
            'Any single digit',
            'Any sequence of zero or more characters',
            'A literal percent sign',
          ],
          correctIndex: 2,
          explanation: '% matches any sequence of zero or more characters. For example, "LIKE \'%tech%\'" matches any string containing "tech" anywhere. The _ wildcard matches exactly one character.',
        },
        {
          question: 'What is the advantage of IN over multiple OR conditions?',
          options: [
            'IN is faster than OR in all cases',
            'IN supports NULL comparisons while OR does not',
            'IN is cleaner and more readable for matching a value against a list',
            'IN can use indexes while OR cannot',
          ],
          correctIndex: 2,
          explanation: 'IN is cleaner and more readable than chaining multiple OR conditions. "WHERE department IN (\'Engineering\', \'Marketing\', \'Sales\')" is equivalent to three OR conditions but easier to read and maintain.',
        },
        {
          question: 'What does NULL + 5 evaluate to in SQL?',
          options: ['5', '0', 'NULL', 'An error'],
          correctIndex: 2,
          explanation: 'NULL propagates through expressions. Any arithmetic or comparison involving NULL returns NULL because NULL represents an unknown value. This "three-valued logic" (true/false/unknown) is a key SQL concept.',
        },
        {
          question: 'What does the COALESCE function do?',
          options: [
            'Converts NULL to an empty string',
            'Returns the first non-NULL value from its arguments',
            'Checks if all values are equal',
            'Merges two columns into one',
          ],
          correctIndex: 1,
          explanation: 'COALESCE returns the first non-NULL value from its list of arguments. It is commonly used to provide default values when a column may be NULL, e.g., COALESCE(phone, \'No phone on file\').',
        },
      ],
    },
    {
      topicId: 'insert-update-delete',
      topicTitle: 'INSERT, UPDATE, DELETE',
      objectiveIndex: 2,
      questions: [
        {
          question: 'What is the best practice when writing an INSERT statement?',
          options: [
            'Omit column names to keep the query short',
            'Always specify column names explicitly',
            'Use SELECT * in the VALUES clause',
            'Insert all columns in alphabetical order',
          ],
          correctIndex: 1,
          explanation: 'Always specifying column names makes INSERT statements resilient to schema changes (new columns, reordered columns) and makes the query self-documenting.',
        },
        {
          question: 'What does the RETURNING clause do in a PostgreSQL INSERT?',
          options: [
            'It returns the number of rows inserted',
            'It returns the inserted row data, including generated values like auto-increment IDs',
            'It returns the table schema after insertion',
            'It reverts the insert if a conflict occurs',
          ],
          correctIndex: 1,
          explanation: 'RETURNING retrieves data from the rows that were inserted (or updated/deleted). It eliminates the need for a follow-up SELECT to get the generated ID or other computed values.',
        },
        {
          question: 'What is the danger of running UPDATE without a WHERE clause?',
          options: [
            'It will throw an error in most databases',
            'It will only update the first row',
            'It will update every row in the table',
            'It will create a backup before updating',
          ],
          correctIndex: 2,
          explanation: 'An UPDATE without a WHERE clause modifies every row in the table. This is one of the most common destructive SQL mistakes. Always test your WHERE clause with a SELECT first in production.',
        },
        {
          question: 'What does ON CONFLICT (sku) DO UPDATE accomplish in PostgreSQL?',
          options: [
            'It deletes the conflicting row and inserts a new one',
            'It skips the insert if the SKU already exists',
            'It performs an upsert — inserting if the SKU is new, updating if it exists',
            'It raises an error when a duplicate SKU is found',
          ],
          correctIndex: 2,
          explanation: 'ON CONFLICT DO UPDATE is PostgreSQL\'s upsert syntax. If the INSERT violates the specified constraint (e.g., duplicate SKU), it executes the UPDATE instead of failing.',
        },
        {
          question: 'What is the difference between DELETE and TRUNCATE?',
          options: [
            'DELETE removes all rows; TRUNCATE removes specific rows',
            'DELETE logs each row deletion and can have a WHERE clause; TRUNCATE removes all rows much faster and typically resets auto-increment',
            'TRUNCATE supports WHERE clauses; DELETE does not',
            'They are identical in behavior',
          ],
          correctIndex: 1,
          explanation: 'DELETE can target specific rows with WHERE and fires triggers, but is slower for bulk removal. TRUNCATE removes all rows very quickly, resets auto-increment sequences, and cannot target specific rows.',
        },
        {
          question: 'What is a "soft delete" pattern?',
          options: [
            'Deleting rows with a LIMIT clause to avoid accidents',
            'Using a transaction so the DELETE can be rolled back',
            'Setting a deleted_at timestamp column instead of physically removing the row',
            'Moving rows to an archive table before deletion',
          ],
          correctIndex: 2,
          explanation: 'Soft deletes mark rows as deleted by setting a deleted_at timestamp rather than physically removing them. This preserves data for auditing, enables recovery, and is required by many compliance regulations.',
        },
        {
          question: 'What is the advantage of inserting multiple rows in a single INSERT statement vs. individual INSERT statements?',
          options: [
            'Multiple-row inserts bypass constraint checking',
            'Multiple-row inserts are dramatically faster because they reduce round trips and transaction overhead',
            'Multiple-row inserts skip index updates',
            'Individual inserts are always faster for fewer than 10 rows',
          ],
          correctIndex: 1,
          explanation: 'Bulk inserts using a single INSERT with multiple VALUES rows are much faster than individual INSERTs because they reduce network round trips, transaction commits, and index maintenance overhead.',
        },
      ],
    },
    {
      topicId: 'joins',
      topicTitle: 'JOINs',
      objectiveIndex: 3,
      questions: [
        {
          question: 'What does an INNER JOIN return?',
          options: [
            'All rows from the left table, with NULLs for non-matching right rows',
            'All rows from both tables regardless of matching',
            'Only rows that have matching values in both tables',
            'All rows from the right table, with NULLs for non-matching left rows',
          ],
          correctIndex: 2,
          explanation: 'INNER JOIN returns only rows where there is a match in both tables based on the join condition. Rows with no match in either table are excluded from the result.',
        },
        {
          question: 'How do you find customers who have never placed an order using a LEFT JOIN?',
          options: [
            'LEFT JOIN orders ON customer_id, then WHERE order_id = 0',
            'LEFT JOIN orders ON customer_id, then WHERE o.order_id IS NULL',
            'INNER JOIN orders ON customer_id, then WHERE order_count = 0',
            'RIGHT JOIN orders, then check for NULL customer_id',
          ],
          correctIndex: 1,
          explanation: 'A LEFT JOIN returns all customers with NULL in the orders columns when no match exists. Adding WHERE o.order_id IS NULL filters to only those customers with no orders — this is the "anti-join" pattern.',
        },
        {
          question: 'What does a CROSS JOIN produce?',
          options: [
            'Rows that exist in one table but not the other',
            'The Cartesian product — every combination of rows from both tables',
            'Rows where the join condition is not met',
            'A summary of rows from both tables',
          ],
          correctIndex: 1,
          explanation: 'A CROSS JOIN produces the Cartesian product — if table A has 5 rows and table B has 4 rows, the result has 20 rows (5 × 4). It is useful for generating combinations like all size-color pairs.',
        },
        {
          question: 'What is a self-join used for?',
          options: [
            'Joining a table to a backup copy of itself',
            'Joining a table to itself, typically to work with hierarchical relationships like org charts',
            'Creating a recursive query that loops through all rows',
            'Duplicating rows within a single table',
          ],
          correctIndex: 1,
          explanation: 'A self-join joins a table to itself using aliases. It is the standard technique for hierarchical data like finding employees and their managers from the same employees table.',
        },
        {
          question: 'What does a FULL OUTER JOIN return?',
          options: [
            'Only rows with matches in both tables',
            'All rows from the left table only',
            'All rows from both tables, with NULLs where there is no match on either side',
            'The union of both tables without duplicates',
          ],
          correctIndex: 2,
          explanation: 'FULL OUTER JOIN returns all rows from both tables. Where there is no match, the columns from the non-matching side are filled with NULL. It combines the effects of both LEFT and RIGHT JOINs.',
        },
        {
          question: 'When joining multiple tables (orders, customers, order_items, products), what is the correct approach?',
          options: [
            'You can only join two tables at a time and must use subqueries for more',
            'Chain multiple JOIN clauses, each specifying the matching columns with ON',
            'Use a UNION to combine the tables',
            'Use CROSS JOIN between all tables and filter with WHERE',
          ],
          correctIndex: 1,
          explanation: 'SQL supports chaining multiple JOIN clauses in a single query. Each JOIN specifies its own ON condition matching the relevant foreign key columns.',
        },
      ],
    },
    {
      topicId: 'aggregate-functions',
      topicTitle: 'Aggregate Functions',
      objectiveIndex: 4,
      questions: [
        {
          question: 'What is the difference between COUNT(*) and COUNT(phone)?',
          options: [
            'They are identical in all cases',
            'COUNT(*) counts all rows; COUNT(phone) counts only rows where phone is not NULL',
            'COUNT(*) is slower than COUNT(column)',
            'COUNT(phone) counts only distinct phone numbers',
          ],
          correctIndex: 1,
          explanation: 'COUNT(*) counts all rows including those with NULLs. COUNT(column_name) only counts rows where that column is not NULL. This distinction is critical when a column has missing values.',
        },
        {
          question: 'What does GROUP BY do in a SQL query?',
          options: [
            'Sorts the result set by the specified columns',
            'Filters rows before aggregation',
            'Divides rows into groups and applies aggregate functions to each group',
            'Joins tables on the specified columns',
          ],
          correctIndex: 2,
          explanation: 'GROUP BY divides the rows into groups based on the specified columns. Aggregate functions like COUNT, SUM, and AVG then compute a single result for each group.',
        },
        {
          question: 'What is the difference between WHERE and HAVING?',
          options: [
            'WHERE filters groups; HAVING filters individual rows',
            'WHERE filters rows before aggregation; HAVING filters groups after aggregation',
            'They are interchangeable in all cases',
            'WHERE works only with numeric columns; HAVING works with all types',
          ],
          correctIndex: 1,
          explanation: 'WHERE filters individual rows before GROUP BY runs. HAVING filters groups after aggregation. You cannot use aggregate functions in a WHERE clause — that is what HAVING is for.',
        },
        {
          question: 'Which query finds departments with more than 10 employees?',
          options: [
            'SELECT department FROM employees WHERE COUNT(*) > 10',
            'SELECT department, COUNT(*) FROM employees GROUP BY department WHERE COUNT(*) > 10',
            'SELECT department, COUNT(*) FROM employees GROUP BY department HAVING COUNT(*) > 10',
            'SELECT department FROM employees HAVING COUNT(*) > 10',
          ],
          correctIndex: 2,
          explanation: 'Filtering on an aggregate result requires HAVING, not WHERE. The GROUP BY groups rows by department, then HAVING filters to only groups with more than 10 rows.',
        },
        {
          question: 'What does AVG() do with NULL values in a column?',
          options: [
            'It treats NULL as 0 when calculating the average',
            'It returns NULL if any value is NULL',
            'It ignores NULL values and averages only the non-NULL rows',
            'It raises an error when NULLs are present',
          ],
          correctIndex: 2,
          explanation: 'Aggregate functions like AVG, SUM, MIN, and MAX ignore NULL values. AVG computes the mean only over non-NULL rows. This is important to understand to avoid unexpected results.',
        },
        {
          question: 'What does "ORDER BY total_payroll DESC" do in a GROUP BY query?',
          options: [
            'Sorts the groups before aggregation occurs',
            'Sorts the final aggregated result set by the total_payroll value, highest first',
            'Filters groups where total_payroll is not NULL',
            'Groups by total_payroll instead of the GROUP BY column',
          ],
          correctIndex: 1,
          explanation: 'ORDER BY in a GROUP BY query sorts the final aggregated result set. It runs after the grouping and aggregation, so you can sort by aggregate values like total_payroll.',
        },
      ],
    },
    {
      topicId: 'subqueries',
      topicTitle: 'Subqueries',
      objectiveIndex: 5,
      questions: [
        {
          question: 'What is a scalar subquery?',
          options: [
            'A subquery that returns multiple rows and columns',
            'A subquery used only in the FROM clause',
            'A subquery that returns exactly one value and can be used wherever a single value is expected',
            'A subquery that scales to large datasets',
          ],
          correctIndex: 2,
          explanation: 'A scalar subquery returns exactly one row and one column — a single value. It can be used in a SELECT list, WHERE clause, or anywhere a single value is expected, such as comparing a salary to the average.',
        },
        {
          question: 'What is a correlated subquery?',
          options: [
            'A subquery that references columns from the outer query and is re-evaluated for each outer row',
            'A subquery that references another subquery',
            'A subquery used with the IN operator',
            'A subquery that returns correlated (related) tables',
          ],
          correctIndex: 0,
          explanation: 'A correlated subquery references a column from the outer query, so it must be re-executed for each row in the outer query. This can be slow but enables powerful per-row comparisons like "salary above department average."',
        },
        {
          question: 'What is the advantage of EXISTS over IN for large subquery result sets?',
          options: [
            'EXISTS always returns faster results regardless of data size',
            'EXISTS can short-circuit — it stops as soon as it finds one matching row — and handles NULLs correctly',
            'EXISTS supports more data types than IN',
            'EXISTS uses indexes while IN does not',
          ],
          correctIndex: 1,
          explanation: 'EXISTS short-circuits evaluation: it returns true as soon as the first matching row is found. NOT EXISTS also handles NULLs correctly, unlike NOT IN which can return unexpected results when the subquery contains NULLs.',
        },
        {
          question: 'What is a derived table (table subquery)?',
          options: [
            'A table created from a schema template',
            'A subquery in the FROM clause that acts as a temporary named result set',
            'A view that derives its data from another view',
            'A table created by copying from another table',
          ],
          correctIndex: 1,
          explanation: 'A derived table is a subquery in the FROM clause given an alias, acting as a temporary table for the outer query. For example, "(SELECT dept, AVG(salary) FROM employees GROUP BY dept) AS dept_stats" creates a derived table.',
        },
        {
          question: 'What does the following query do? SELECT * FROM orders WHERE total = (SELECT MAX(total) FROM orders)',
          options: [
            'Returns all orders with a total above the average',
            'Returns all orders sorted by total descending',
            'Returns the order(s) with the highest total',
            'Returns the single maximum total value as a scalar',
          ],
          correctIndex: 2,
          explanation: 'The scalar subquery (SELECT MAX(total) FROM orders) returns one value — the highest order total. The outer query then returns all rows where total equals that maximum value (could be multiple rows if there are ties).',
        },
      ],
    },
    {
      topicId: 'basic-indexing',
      topicTitle: 'Basic Indexing',
      objectiveIndex: 6,
      questions: [
        {
          question: 'What problem does an index solve?',
          options: [
            'It prevents duplicate rows from being inserted',
            'It enforces foreign key relationships',
            'It speeds up data retrieval by avoiding a full table scan',
            'It compresses table data to reduce storage',
          ],
          correctIndex: 2,
          explanation: 'Without an index, the database must scan every row (sequential scan) to find matches. An index provides a fast lookup structure so the database can find relevant rows without reading the entire table.',
        },
        {
          question: 'Why does wrapping an indexed column in a function prevent index use?',
          options: [
            'Functions are not allowed in WHERE clauses',
            'The function output has no connection to the index sorted values, so the index cannot be used',
            'Functions always force a full table scan as a safety measure',
            'Only scalar functions break index usage',
          ],
          correctIndex: 1,
          explanation: 'When you apply a function to a column (e.g., YEAR(order_date)), the database cannot map the function output back to the B-tree index on the original column. Rewrite as an explicit range instead: WHERE order_date >= \'2025-01-01\' AND order_date < \'2026-01-01\'.',
        },
        {
          question: 'For a composite index on (status, order_date), which query CANNOT use the index?',
          options: [
            'WHERE status = \'pending\'',
            'WHERE status = \'pending\' AND order_date > \'2025-01-01\'',
            'WHERE order_date > \'2025-01-01\'',
            'WHERE status IN (\'pending\', \'processing\')',
          ],
          correctIndex: 2,
          explanation: 'A composite index can only be used from the left. If you skip the leading column (status), the index cannot be used. A query filtering only on order_date needs a separate single-column index.',
        },
        {
          question: 'What does EXPLAIN ANALYZE tell you that plain EXPLAIN does not?',
          options: [
            'The query syntax errors',
            'The actual execution times and row counts from running the query, not just estimates',
            'The index definitions used',
            'The query cost in dollars',
          ],
          correctIndex: 1,
          explanation: 'EXPLAIN shows the planned execution strategy with estimated costs. EXPLAIN ANALYZE actually runs the query and shows both planned estimates and real execution times and row counts, letting you compare estimates to actuals.',
        },
        {
          question: 'Why is an index on a boolean column (like is_active) generally less useful?',
          options: [
            'Boolean columns cannot be indexed',
            'Boolean values are too small to benefit from indexing',
            'Low selectivity — with only two values, the index would match a large fraction of rows, making a table scan equally efficient',
            'Boolean indexes are not supported in PostgreSQL',
          ],
          correctIndex: 2,
          explanation: 'Index effectiveness depends on selectivity. A boolean column has only two values, so each value matches roughly 50% of rows. For large match sets, a sequential scan can be more efficient than following index pointers to many scattered rows.',
        },
        {
          question: 'What overhead does adding an index impose?',
          options: [
            'None — indexes only affect read performance',
            'Additional time and storage for INSERT, UPDATE, and DELETE operations since the index must be maintained',
            'Slower SELECT queries on the indexed column',
            'Indexes require exclusive locks during all reads',
          ],
          correctIndex: 1,
          explanation: 'Every index must be updated whenever the underlying data changes. INSERT, UPDATE, and DELETE operations become slightly slower as each index on the affected columns must be maintained. This is why you should only add indexes that are actually needed.',
        },
      ],
    },
    {
      topicId: 'data-types-and-constraints',
      topicTitle: 'Data Types & Constraints',
      objectiveIndex: 7,
      questions: [
        {
          question: 'What is the difference between VARCHAR(255) and TEXT in PostgreSQL?',
          options: [
            'VARCHAR is faster; TEXT is slower',
            'VARCHAR has a maximum length limit; TEXT has no maximum length',
            'TEXT stores Unicode; VARCHAR stores ASCII only',
            'They are identical in storage and performance in PostgreSQL',
          ],
          correctIndex: 1,
          explanation: 'VARCHAR(n) enforces a maximum length of n characters. TEXT has no length limit. In PostgreSQL, both are stored the same way internally — the main difference is the length constraint.',
        },
        {
          question: 'What does a FOREIGN KEY constraint enforce?',
          options: [
            'That the column value is unique across the table',
            'That the column cannot be NULL',
            'Referential integrity — the value must exist in the referenced table',
            'That the column is indexed for fast lookups',
          ],
          correctIndex: 2,
          explanation: 'A FOREIGN KEY constraint enforces referential integrity: every value in the foreign key column must match an existing value in the referenced table\'s primary key. This prevents orphaned records.',
        },
        {
          question: 'What does ON DELETE CASCADE mean on a FOREIGN KEY?',
          options: [
            'Prevent deletion of the parent row if child rows exist',
            'Set child rows to NULL when the parent is deleted',
            'Automatically delete child rows when the parent row is deleted',
            'Log the deletion to an audit table',
          ],
          correctIndex: 2,
          explanation: 'ON DELETE CASCADE automatically deletes all child rows when the parent row is deleted. ON DELETE RESTRICT (the other common option) prevents the parent from being deleted if child rows exist.',
        },
        {
          question: 'What does a CHECK constraint do?',
          options: [
            'Checks if the table exists before inserting',
            'Validates that column values satisfy a custom condition at insert/update time',
            'Checks for duplicate values across tables',
            'Verifies that indexes are up to date',
          ],
          correctIndex: 1,
          explanation: 'A CHECK constraint enforces a custom validation rule on a column. For example, CHECK (price >= 0) prevents negative prices from being stored. The constraint is evaluated on every INSERT and UPDATE.',
        },
        {
          question: 'What is the difference between DECIMAL(10,2) and FLOAT for storing prices?',
          options: [
            'FLOAT is more precise for financial values',
            'DECIMAL stores exact values; FLOAT uses binary floating point which can have rounding errors',
            'They are identical for values under 1 million',
            'DECIMAL is faster for arithmetic operations',
          ],
          correctIndex: 1,
          explanation: 'DECIMAL (and NUMERIC) stores exact decimal values, which is essential for financial data. FLOAT uses binary floating point representation, which cannot represent many decimal fractions exactly, leading to rounding errors in calculations.',
        },
        {
          question: 'What does TIMESTAMP WITH TIME ZONE store compared to plain TIMESTAMP?',
          options: [
            'It stores the timezone abbreviation alongside the timestamp',
            'It converts the input to UTC for storage and displays it in the session timezone on retrieval',
            'It stores the timestamp in the server\'s local timezone',
            'They store identical data — WITH TIME ZONE is just a label',
          ],
          correctIndex: 1,
          explanation: 'TIMESTAMP WITH TIME ZONE (timestamptz) converts the input to UTC for storage and displays it converted to the session timezone. Plain TIMESTAMP stores whatever value you give it without timezone conversion.',
        },
      ],
    },
  ],

  mid: [
    {
      topicId: 'window-functions',
      topicTitle: 'Window Functions',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is the key difference between window functions and GROUP BY aggregations?',
          options: [
            'Window functions are faster than GROUP BY',
            'Window functions calculate across related rows without collapsing them into a single output row',
            'GROUP BY supports more aggregate functions than window functions',
            'Window functions can only be used with ORDER BY',
          ],
          correctIndex: 1,
          explanation: 'GROUP BY collapses rows into a single result per group. Window functions perform calculations across a set of related rows while still returning each individual row — you see both the detail and the aggregate.',
        },
        {
          question: 'What is the difference between RANK() and DENSE_RANK()?',
          options: [
            'RANK uses 1-based numbering; DENSE_RANK uses 0-based',
            'RANK skips numbers after ties (1,1,3); DENSE_RANK does not skip (1,1,2)',
            'DENSE_RANK skips numbers after ties; RANK does not',
            'They are identical except DENSE_RANK works across partitions',
          ],
          correctIndex: 1,
          explanation: 'When two rows tie, both RANK and DENSE_RANK assign them the same number. But RANK then skips the next number(s) (1,1,3,4), while DENSE_RANK continues sequentially (1,1,2,3).',
        },
        {
          question: 'What does PARTITION BY do in a window function?',
          options: [
            'It splits the table into physical partitions for performance',
            'It divides the rows into groups within which the window function calculation is performed independently',
            'It filters rows before the window calculation',
            'It sorts the rows for the window function',
          ],
          correctIndex: 1,
          explanation: 'PARTITION BY divides rows into groups (like GROUP BY but without collapsing). The window function restarts its calculation for each partition. For example, ROW_NUMBER() OVER (PARTITION BY department) numbers employees within each department.',
        },
        {
          question: 'What do LAG() and LEAD() functions do?',
          options: [
            'LAG calculates a running total; LEAD calculates a running average',
            'LAG looks at the previous row; LEAD looks at the next row within the partition',
            'LAG and LEAD both delay query execution for performance',
            'They are used to compute statistical lag in time series',
          ],
          correctIndex: 1,
          explanation: 'LAG accesses data from a previous row in the result set without a self-join. LEAD accesses data from a following row. They are ideal for comparing a value to its previous/next period, like month-over-month revenue changes.',
        },
        {
          question: 'What does ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW define?',
          options: [
            'A window frame from the beginning of the partition to the current row, used for running totals',
            'A window that includes only the previous and current row',
            'A window that spans the entire result set',
            'A window of a fixed number of preceding rows',
          ],
          correctIndex: 0,
          explanation: 'This window frame specification includes all rows from the start of the partition to the current row. When used with SUM(), it produces a running total. ROWS BETWEEN 6 PRECEDING AND CURRENT ROW would create a 7-day moving average.',
        },
        {
          question: 'What does NTILE(4) accomplish?',
          options: [
            'It returns the 4th row in each partition',
            'It divides rows into 4 roughly equal groups and assigns a group number (1-4) to each row',
            'It calculates the value at the 4th percentile',
            'It rounds values to 4 decimal places',
          ],
          correctIndex: 1,
          explanation: 'NTILE(n) distributes rows into n roughly equal buckets and assigns each row a bucket number from 1 to n. NTILE(4) creates quartiles; NTILE(100) creates percentiles.',
        },
        {
          question: 'To get the top 3 highest-paid employees per department, what is the correct window function approach?',
          options: [
            'Use MAX(salary) with GROUP BY department and LIMIT 3',
            'Use ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) and filter WHERE rn <= 3',
            'Use RANK() and filter WHERE salary IN (SELECT TOP 3)',
            'Use DENSE_RANK() and filter WHERE dense_rank <= 3 — both RANK and DENSE_RANK work identically here',
          ],
          correctIndex: 1,
          explanation: 'ROW_NUMBER() with PARTITION BY department assigns sequential numbers within each department. Wrapping this in a CTE and filtering WHERE rn <= 3 returns the top 3 per department. RANK() would also work but may return more than 3 if there are salary ties.',
        },
      ],
    },
    {
      topicId: 'common-table-expressions-ctes',
      topicTitle: 'Common Table Expressions (CTEs)',
      objectiveIndex: 1,
      questions: [
        {
          question: 'What is a CTE (Common Table Expression)?',
          options: [
            'A permanent table created from a query result',
            'A named temporary result set defined with the WITH clause that exists for the duration of a single query',
            'A stored procedure that returns tabular data',
            'A compressed table that common data is extracted into',
          ],
          correctIndex: 1,
          explanation: 'A CTE is defined with the WITH keyword and gives a name to a subquery result. It exists only for the duration of the query, improving readability and enabling multi-step logic without deeply nested subqueries.',
        },
        {
          question: 'Can a CTE reference another CTE defined earlier in the same WITH block?',
          options: [
            'No — each CTE is independent',
            'Yes — later CTEs in the same WITH block can reference earlier ones',
            'Only if the earlier CTE has been materialized',
            'Only in recursive CTEs',
          ],
          correctIndex: 1,
          explanation: 'CTEs are evaluated in order within a WITH block, and later CTEs can reference earlier ones. This enables step-by-step data transformations, where each step builds on the previous, like a pipeline.',
        },
        {
          question: 'What is the main advantage of using CTEs over deeply nested subqueries?',
          options: [
            'CTEs are always faster than subqueries',
            'CTEs can be indexed unlike subqueries',
            'CTEs name each logical step, making complex queries readable and maintainable',
            'CTEs avoid all N+1 query problems',
          ],
          correctIndex: 2,
          explanation: 'CTEs improve readability by naming each logical step of a complex query. Instead of parsing nested parentheses, you read named blocks that build on each other sequentially — similar to well-factored functions in code.',
        },
        {
          question: 'In which SQL tool/ecosystem are CTEs particularly central to data transformation pipelines?',
          options: ['PgAdmin', 'dbt (data build tool)', 'EXPLAIN ANALYZE', 'pg_stat_statements'],
          correctIndex: 1,
          explanation: 'dbt (data build tool) uses CTEs as its primary modeling pattern. Data engineers use CTEs to build multi-step SQL transformation pipelines that are readable, testable, and maintainable.',
        },
        {
          question: 'What does the following CTE accomplish? WITH active_customers AS (SELECT ... WHERE status = \'active\') SELECT ... FROM active_customers',
          options: [
            'Creates a permanent view named active_customers',
            'Defines a temporary result set of active customers used in the main SELECT',
            'Caches the active customers list for the session',
            'Creates an index on the status column',
          ],
          correctIndex: 1,
          explanation: 'This CTE defines active_customers as the result of filtering customers by active status. The main SELECT then queries that named result set. The CTE is not stored — it is re-evaluated each time the query runs.',
        },
      ],
    },
    {
      topicId: 'recursive-queries',
      topicTitle: 'Recursive Queries',
      objectiveIndex: 2,
      questions: [
        {
          question: 'What are the two required parts of a recursive CTE?',
          options: [
            'An anchor query and a termination query',
            'A base case (anchor) and a recursive case that references the CTE itself, combined with UNION ALL',
            'A starting query and a loop body',
            'A seed query and a recursive query separated by INTERSECT',
          ],
          correctIndex: 1,
          explanation: 'A recursive CTE requires: (1) an anchor/base case that defines the starting rows, and (2) a recursive case that references the CTE itself to find the next iteration\'s rows. They are combined with UNION ALL.',
        },
        {
          question: 'What keyword must be added to the WITH clause for recursive CTEs in PostgreSQL?',
          options: ['WITH LOOP', 'WITH RECURSIVE', 'WITH ITERATE', 'WITH SELF'],
          correctIndex: 1,
          explanation: 'PostgreSQL requires the RECURSIVE keyword in the WITH clause to enable recursive CTEs: WITH RECURSIVE cte_name AS (...). Without it, the CTE cannot reference itself.',
        },
        {
          question: 'What type of data structure is a recursive CTE ideal for traversing?',
          options: [
            'Flat tables with no relationships',
            'Hierarchical or tree-structured data like org charts, category trees, and bill of materials',
            'Large flat files imported from CSV',
            'Indexed tables with range queries',
          ],
          correctIndex: 1,
          explanation: 'Recursive CTEs are designed for hierarchical data where records reference other records in the same table (parent-child relationships). Classic examples include org charts (employee-manager), category hierarchies, and bill of materials.',
        },
        {
          question: 'Why is cycle detection important in recursive CTEs on graph data?',
          options: [
            'To prevent the query from returning duplicate rows',
            'To avoid infinite recursion when a node references an ancestor, creating a circular path',
            'To improve performance by detecting repeated calculations',
            'To enforce referential integrity in the recursive results',
          ],
          correctIndex: 1,
          explanation: 'In graph data, circular references can cause infinite recursion. The standard technique is to track a path array and stop recursing when the next node is already in the path (NOT c.component_id = ANY(bom.path)).',
        },
        {
          question: 'A recursive CTE org chart query starts with WHERE manager_id IS NULL. What does this represent?',
          options: [
            'Employees who have no direct reports',
            'The base case — the root of the hierarchy (typically the CEO who has no manager)',
            'Employees who have not been assigned a manager yet',
            'A filter to remove NULL values from the result',
          ],
          correctIndex: 1,
          explanation: 'The anchor query of an org chart recursive CTE starts at the root of the hierarchy — the row with no parent. For an org chart, this is typically the CEO whose manager_id IS NULL. The recursive case then finds everyone who reports to them.',
        },
      ],
    },
    {
      topicId: 'transactions-and-acid',
      topicTitle: 'Transactions & ACID',
      objectiveIndex: 3,
      questions: [
        {
          question: 'What does ACID stand for in database transactions?',
          options: [
            'Atomicity, Compression, Isolation, Durability',
            'Atomicity, Consistency, Isolation, Durability',
            'Access, Concurrency, Integrity, Distribution',
            'Atomicity, Consistency, Index, Distribution',
          ],
          correctIndex: 1,
          explanation: 'ACID stands for Atomicity (all or nothing), Consistency (valid state to valid state), Isolation (concurrent transactions don\'t interfere), and Durability (committed data survives crashes).',
        },
        {
          question: 'What does atomicity mean in a database transaction?',
          options: [
            'Each statement is processed at the atomic (CPU) level for maximum speed',
            'All operations in the transaction either all succeed (COMMIT) or all fail and are undone (ROLLBACK)',
            'The transaction runs in isolation from all other transactions',
            'Data is written atomically to disk without buffering',
          ],
          correctIndex: 1,
          explanation: 'Atomicity means a transaction is "all or nothing." If any operation in the transaction fails, all previous operations in that transaction are rolled back, leaving the database as if nothing happened.',
        },
        {
          question: 'What is the purpose of a SAVEPOINT within a transaction?',
          options: [
            'To permanently save data midway through a transaction',
            'To mark a point within a transaction that you can ROLLBACK TO without aborting the entire transaction',
            'To commit partial results before the full transaction completes',
            'To improve performance by checkpointing long transactions',
          ],
          correctIndex: 1,
          explanation: 'A SAVEPOINT marks a position within a transaction to which you can partially roll back. ROLLBACK TO SAVEPOINT undoes only the work done after the savepoint, preserving earlier operations in the transaction.',
        },
        {
          question: 'Which isolation level is the PostgreSQL default?',
          options: ['Read Uncommitted', 'Read Committed', 'Repeatable Read', 'Serializable'],
          correctIndex: 1,
          explanation: 'Read Committed is PostgreSQL\'s default isolation level. Each statement sees only data committed before that statement began. This prevents dirty reads but allows non-repeatable reads within the same transaction.',
        },
        {
          question: 'What is a deadlock in database transactions?',
          options: [
            'A transaction that takes too long to commit',
            'When a transaction acquires too many locks and runs out of memory',
            'When two transactions each hold a lock the other needs, causing both to wait forever',
            'A lock that cannot be released because the connection was dropped',
          ],
          correctIndex: 2,
          explanation: 'A deadlock occurs when transaction A holds a lock needed by transaction B, while transaction B holds a lock needed by transaction A. Neither can proceed. The database detects this and aborts one transaction. Prevention: always acquire locks in a consistent order.',
        },
        {
          question: 'What isolation level prevents phantom reads?',
          options: [
            'Read Uncommitted',
            'Read Committed',
            'Repeatable Read',
            'Serializable',
          ],
          correctIndex: 3,
          explanation: 'Serializable is the strongest isolation level and prevents phantom reads (new rows appearing in a range query when re-run). Note: PostgreSQL\'s Repeatable Read also prevents phantom reads in practice, but the SQL standard only guarantees this at Serializable.',
        },
      ],
    },
    {
      topicId: 'views-and-materialized-views',
      topicTitle: 'Views & Materialized Views',
      objectiveIndex: 4,
      questions: [
        {
          question: 'What is a regular (non-materialized) view?',
          options: [
            'A named table that stores query results periodically',
            'A named query that is re-executed every time it is accessed — it stores no data itself',
            'A read-only copy of a table',
            'A compressed version of a table for reporting',
          ],
          correctIndex: 1,
          explanation: 'A regular view is a stored query definition, not stored data. Every time you query a view, the underlying query is re-executed. It always returns current data but has no performance advantage over running the query directly.',
        },
        {
          question: 'What is the key difference between a regular view and a materialized view?',
          options: [
            'Materialized views can be updated; regular views cannot',
            'Materialized views store the query result physically on disk; regular views do not',
            'Regular views support indexing; materialized views do not',
            'Materialized views auto-refresh; regular views require manual refresh',
          ],
          correctIndex: 1,
          explanation: 'A materialized view stores the actual query result physically, enabling fast reads without re-executing the query. The trade-off is that the data can become stale and must be refreshed with REFRESH MATERIALIZED VIEW.',
        },
        {
          question: 'When would you choose a materialized view over a regular view?',
          options: [
            'When you need real-time data accuracy',
            'When the underlying query is slow and the data does not need to be perfectly current',
            'When you need to update the view data from the application',
            'When the source tables change frequently',
          ],
          correctIndex: 1,
          explanation: 'Materialized views are best for expensive queries (e.g., large aggregations) where slightly stale data is acceptable. They trade freshness for read speed — ideal for dashboards, reports, and summary tables that are refreshed on a schedule.',
        },
        {
          question: 'What does REFRESH MATERIALIZED VIEW CONCURRENTLY do differently from a regular refresh?',
          options: [
            'It refreshes faster by using parallel workers',
            'It refreshes without acquiring a lock that blocks reads, requiring a unique index',
            'It refreshes in the background without blocking any queries',
            'It refreshes incrementally instead of replacing all data',
          ],
          correctIndex: 1,
          explanation: 'CONCURRENTLY performs the refresh without locking the view for reads, so queries can continue to read the old data while the refresh runs. It requires a unique index on the materialized view and takes longer than a regular refresh.',
        },
        {
          question: 'What does WITH CHECK OPTION do on an updatable view?',
          options: [
            'It prevents any updates through the view',
            'It verifies that updates don\'t violate the view\'s WHERE condition — rows that would disappear from the view are rejected',
            'It checks for constraint violations before applying updates',
            'It creates a backup before allowing updates',
          ],
          correctIndex: 1,
          explanation: 'WITH CHECK OPTION ensures that any INSERT or UPDATE through the view must result in a row still visible through the view\'s WHERE filter. For example, you cannot set status = \'inactive\' through a view that only shows active rows.',
        },
      ],
    },
    {
      topicId: 'query-optimization',
      topicTitle: 'Query Optimization',
      objectiveIndex: 5,
      questions: [
        {
          question: 'What does EXPLAIN ANALYZE show that plain EXPLAIN does not?',
          options: [
            'The SQL query after the planner rewrites it',
            'Actual execution times and row counts from running the query, in addition to the planned estimates',
            'The indexes available on each table',
            'Memory usage during the query',
          ],
          correctIndex: 1,
          explanation: 'EXPLAIN shows the planner\'s strategy and cost estimates without running the query. EXPLAIN ANALYZE actually executes the query and reports both estimated and actual row counts and execution times, enabling comparison of planner estimates to reality.',
        },
        {
          question: 'When you see "Seq Scan on orders" for a large table in EXPLAIN output, what should you investigate?',
          options: [
            'The table has too many rows and should be archived',
            'There may be a missing index, a function wrapping the indexed column, or wrong column order in a composite index',
            'The database is running low on memory',
            'The query should be rewritten using a CTE',
          ],
          correctIndex: 1,
          explanation: 'A sequential scan on a large table where you expected an index scan usually means: a missing index, a function applied to the indexed column preventing its use, or the leading column of a composite index being skipped.',
        },
        {
          question: 'Why does "WHERE customer_id = 123 OR order_date = \'2025-06-01\'" prevent optimal index use?',
          options: [
            'OR conditions are not supported in indexed queries',
            'OR on different columns requires evaluating two separate indexes which can\'t be combined efficiently',
            'The database cannot optimize OR conditions at all',
            'The date literal format prevents index use',
          ],
          correctIndex: 1,
          explanation: 'OR on different columns may force a full table scan. A better approach is to use UNION: one branch filters on customer_id using its index, the other on order_date using its index — combining results efficiently.',
        },
        {
          question: 'What is a covering index?',
          options: [
            'An index that spans all columns in the table',
            'An index that includes all columns needed by a query so no table heap access is required',
            'An index that covers the full text of a document column',
            'An index with a higher fill factor to cover more insertions',
          ],
          correctIndex: 1,
          explanation: 'A covering index includes all columns needed by a query in the index itself (using INCLUDE for non-key columns). This enables an Index Only Scan — no trips back to the table heap — which is the fastest scan type.',
        },
        {
          question: 'What does running ANALYZE on a table do?',
          options: [
            'It rebuilds all indexes on the table',
            'It updates the query planner\'s statistics about the table\'s data distribution',
            'It checks the table for corruption',
            'It compresses the table to reduce storage',
          ],
          correctIndex: 1,
          explanation: 'ANALYZE samples the table\'s data and updates the pg_statistic system catalog with information about column values (distribution, most common values, etc.). The query planner uses these statistics to choose efficient execution plans.',
        },
        {
          question: 'What is implicit type conversion and why can it hurt performance?',
          options: [
            'PostgreSQL automatically converts data types, which is always beneficial',
            'When you compare a column to a value of a different type, the database may not be able to use an index on that column',
            'Type conversion forces the query to run in serial mode',
            'It only affects text columns, not numeric ones',
          ],
          correctIndex: 1,
          explanation: 'If a column is VARCHAR but you compare it to an integer (WHERE phone = 5551234), the database may apply a type cast to every row, preventing index use. Always match the data type of your filter values to the column type.',
        },
      ],
    },
    {
      topicId: 'normalization',
      topicTitle: 'Normalization',
      objectiveIndex: 6,
      questions: [
        {
          question: 'What does First Normal Form (1NF) require?',
          options: [
            'Every column must depend on the entire primary key',
            'No transitive dependencies between non-key columns',
            'Every column must contain only atomic (indivisible) values with no repeating groups',
            'Every table must have a single-column primary key',
          ],
          correctIndex: 2,
          explanation: '1NF requires atomic values — each cell holds a single value, not a list or array. Storing "Widget, Gadget, Bolt" in one column violates 1NF. The fix is separate rows with one value each.',
        },
        {
          question: 'A table has a composite primary key (order_id, product_id). The product_name column depends only on product_id. Which normal form is violated?',
          options: ['First Normal Form (1NF)', 'Second Normal Form (2NF)', 'Third Normal Form (3NF)', 'Boyce-Codd Normal Form (BCNF)'],
          correctIndex: 1,
          explanation: '2NF requires that every non-key column depends on the entire primary key, not just part of it. product_name depends only on product_id (partial dependency), violating 2NF. The fix is to move product_name to a separate products table.',
        },
        {
          question: 'An employees table stores employee_id, department_id, and department_location. The department_location depends on department_id, not on employee_id. Which normal form is violated?',
          options: ['1NF', '2NF', '3NF', 'BCNF'],
          correctIndex: 2,
          explanation: '3NF requires no transitive dependencies: non-key columns must depend only on the primary key, not on other non-key columns. department_location depends on department_id (a non-key), not on employee_id. Fix: move department info to a separate departments table.',
        },
        {
          question: 'When is strategic denormalization appropriate?',
          options: [
            'Never — denormalization always causes data integrity problems',
            'Only in small tables with fewer than 1000 rows',
            'In read-heavy workloads where JOIN performance is a proven bottleneck and data changes infrequently',
            'Only when there are no foreign key constraints',
          ],
          correctIndex: 2,
          explanation: 'Denormalization trades data integrity risk for read performance. It makes sense for read-heavy workloads (like analytics), reporting tables, or as caching layers via materialized views — when the JOIN overhead is a proven bottleneck.',
        },
        {
          question: 'What problem does 4NF address that 3NF does not?',
          options: [
            'Partial dependencies in composite key tables',
            'Transitive dependencies between non-key columns',
            'Multi-valued dependencies — when one entity can independently have multiple values of two different attributes',
            'Circular references between tables',
          ],
          correctIndex: 2,
          explanation: '4NF addresses multi-valued dependencies. If a student can have multiple hobbies AND multiple phones, storing them together creates spurious combinations. 4NF requires separating them into student_hobbies and student_phones tables.',
        },
      ],
    },
    {
      topicId: 'stored-procedures-and-functions',
      topicTitle: 'Stored Procedures & Functions',
      objectiveIndex: 7,
      questions: [
        {
          question: 'What is the main difference between a stored procedure and a function in PostgreSQL?',
          options: [
            'Functions can return values; procedures cannot',
            'Procedures can manage transactions (COMMIT/ROLLBACK inside); functions cannot',
            'Functions support parameters; procedures do not',
            'Procedures are faster because they bypass the query planner',
          ],
          correctIndex: 1,
          explanation: 'In PostgreSQL 11+, procedures (created with CREATE PROCEDURE and called with CALL) can commit and rollback transactions within their body. Functions run inside the calling transaction and cannot contain COMMIT or ROLLBACK.',
        },
        {
          question: 'What language is used to write procedural functions in the PostgreSQL examples in the content?',
          options: ['SQL', 'PL/pgSQL', 'JavaScript', 'Python'],
          correctIndex: 1,
          explanation: 'PL/pgSQL (Procedural Language/PostgreSQL SQL) is PostgreSQL\'s built-in procedural language. It adds control flow (IF/ELSIF, LOOP, FOR), variable declarations, and exception handling to SQL.',
        },
        {
          question: 'What does RAISE EXCEPTION do in a PL/pgSQL function?',
          options: [
            'It logs a warning to the PostgreSQL log file',
            'It increases the priority of the current transaction',
            'It throws an error that aborts the function and rolls back the transaction',
            'It sends a notification to connected clients',
          ],
          correctIndex: 2,
          explanation: 'RAISE EXCEPTION throws an error from a PL/pgSQL function, aborting execution and rolling back the current transaction (unless caught by an EXCEPTION block). It is used for custom validation and error reporting.',
        },
        {
          question: 'What is the advantage of using a function with RETURNS TABLE?',
          options: [
            'It creates a permanent table from the function\'s results',
            'It allows the function to return multiple rows and columns, queryable like a table',
            'It is faster than returning individual scalars',
            'It automatically creates indexes on the returned data',
          ],
          correctIndex: 1,
          explanation: 'A function with RETURNS TABLE can return multiple rows and columns. It can be called with SELECT * FROM function_name(params) just like querying a table, making it useful for parameterized report queries.',
        },
        {
          question: 'What is one trade-off of putting business logic in stored procedures vs. application code?',
          options: [
            'Stored procedures are always slower than application code',
            'Stored procedures cannot access the database',
            'Stored procedures can be harder to test, version-control, and may lock logic into a specific database',
            'Stored procedures cannot handle errors',
          ],
          correctIndex: 2,
          explanation: 'While stored procedures can improve performance by reducing network round trips, they can make testing harder, complicate version control (SQL code vs. application code), and create vendor lock-in. Many teams prefer application-level logic with explicit transactions.',
        },
      ],
    },
  ],

  senior: [
    {
      topicId: 'execution-plans-deep-dive',
      topicTitle: 'Execution Plans Deep Dive',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is an Index Only Scan and when does it occur?',
          options: [
            'A scan that only checks the index for existence without reading data',
            'The fastest scan type, where all needed columns are in the index and no table heap access is required',
            'A scan that uses the index to find rows but skips the WHERE filter',
            'A scan type used only for unique indexes',
          ],
          correctIndex: 1,
          explanation: 'An Index Only Scan occurs when all columns needed by the query are present in the index (via INCLUDE columns or as key columns). The database never touches the table heap, shown as "Heap Fetches: 0" in EXPLAIN ANALYZE output.',
        },
        {
          question: 'When does the planner choose a Bitmap Heap Scan over a direct Index Scan?',
          options: [
            'When the table has no primary key',
            'For medium-selectivity queries — it builds a bitmap of matching pages and reads them in physical order to reduce random I/O',
            'When the index is on a boolean column',
            'When the query has no ORDER BY clause',
          ],
          correctIndex: 1,
          explanation: 'A Bitmap Index Scan builds an in-memory bitmap of matching heap pages, then the Bitmap Heap Scan reads those pages in physical order. This is more efficient than random heap fetches for medium-selectivity queries (roughly 5-20% of rows).',
        },
        {
          question: 'What does a significant difference between estimated rows and actual rows in EXPLAIN ANALYZE indicate?',
          options: [
            'The query has a bug in its WHERE clause',
            'Stale planner statistics — running ANALYZE on the table should help',
            'The index is corrupted and needs rebuilding',
            'The query is running out of memory',
          ],
          correctIndex: 1,
          explanation: 'Large discrepancies between estimated and actual rows mean the planner\'s statistics are outdated. Running ANALYZE refreshes the statistics, giving the planner better information to choose efficient plans.',
        },
        {
          question: 'What is a Nested Loop Join and when is it the best choice?',
          options: [
            'A join that iterates through both tables simultaneously using sorted order',
            'For each row in the outer table, scan the inner table — best when the outer set is small and the inner table has an index on the join column',
            'A join that builds a hash table from both inputs for O(1) lookup',
            'A join used only for self-joins',
          ],
          correctIndex: 1,
          explanation: 'A Nested Loop Join iterates the outer table and probes the inner table for each row. It is most efficient when the outer result set is small and the inner table has an index — lookups are O(log N) per row.',
        },
        {
          question: 'What does random_page_cost control in PostgreSQL, and why should it be lowered for SSDs?',
          options: [
            'It limits the number of random reads per second',
            'It tells the planner the relative cost of random page reads vs. sequential reads — SSDs have much lower random I/O cost so lowering it makes the planner prefer index scans',
            'It controls how many pages fit in the buffer cache',
            'It sets the page size for the storage engine',
          ],
          correctIndex: 1,
          explanation: 'random_page_cost (default 4.0) represents the cost of a random disk read relative to a sequential read (seq_page_cost = 1.0). SSDs have much lower random I/O latency, so setting random_page_cost to 1.1-1.5 makes the planner more willing to use indexes.',
        },
        {
          question: 'What does "startup cost" mean in the EXPLAIN cost notation "cost=startup..total"?',
          options: [
            'The time spent before the database process starts',
            'The estimated cost before the first output row can be returned — relevant for nested loops where you need to build a hash table first',
            'The cost of parsing and planning the query',
            'The index traversal cost to find the first matching row',
          ],
          correctIndex: 1,
          explanation: 'Startup cost is the estimated cost before the first row can be returned. A Hash Join has a high startup cost (building the hash table) but low per-row cost. A Nested Loop has near-zero startup cost. This distinction matters for queries that need the first row quickly.',
        },
      ],
    },
    {
      topicId: 'index-internals',
      topicTitle: 'Index Internals',
      objectiveIndex: 1,
      questions: [
        {
          question: 'Why do B-tree leaf nodes form a doubly-linked list?',
          options: [
            'To allow the index to be traversed in reverse order',
            'To enable efficient range scans — after finding the starting value via tree traversal, the scan walks through linked leaf nodes',
            'To speed up INSERT operations by linking new entries',
            'To support concurrent reads without locking',
          ],
          correctIndex: 1,
          explanation: 'The doubly-linked list of leaf nodes makes range scans efficient. The tree traversal finds the start of the range in O(log N), then the scan walks through adjacent leaf nodes to collect the remaining matching rows without revisiting internal nodes.',
        },
        {
          question: 'What type of queries can a Hash index support that B-tree indexes cannot handle more efficiently?',
          options: [
            'Range queries and ORDER BY',
            'Only equality (=) lookups, but potentially with smaller index size for large values',
            'LIKE prefix matching',
            'Full-text search queries',
          ],
          correctIndex: 1,
          explanation: 'Hash indexes support only equality comparisons and cannot handle ranges, ORDER BY, or LIKE prefix matching. They can be smaller and offer O(1) lookup for equality-only use cases, unlike B-tree\'s O(log N).',
        },
        {
          question: 'What type of data is a GIN index best suited for?',
          options: [
            'Single-value columns with high cardinality like email',
            'Multi-valued data types like JSONB, arrays, and full-text search vectors',
            'Numeric ranges and geometric data',
            'Columns with low cardinality like status flags',
          ],
          correctIndex: 1,
          explanation: 'GIN (Generalized Inverted Index) indexes map individual values to the set of rows containing them — ideal for multi-valued types. A single JSONB document or array element can appear in many GIN index entries.',
        },
        {
          question: 'What is a partial index and what is its key benefit?',
          options: [
            'An index that covers only some columns of a table',
            'An index that covers only rows matching a WHERE condition — smaller size and less write overhead',
            'An index built incrementally over time',
            'An index that is temporarily disabled during bulk loads',
          ],
          correctIndex: 1,
          explanation: 'A partial index only indexes rows that match a WHERE condition. For example, an index on pending orders is much smaller than an index on all orders. Smaller index = faster reads, less memory usage, and lower write overhead.',
        },
        {
          question: 'What does an expression index enable that a regular column index cannot?',
          options: [
            'Indexing foreign key columns automatically',
            'Indexing the result of a function or expression, enabling index use for queries with functions in WHERE clauses',
            'Indexing multiple tables with one index definition',
            'Automatically updating when the underlying data changes',
          ],
          correctIndex: 1,
          explanation: 'An expression index (e.g., CREATE INDEX ON users (LOWER(email))) indexes a computed value. Queries using that exact expression in WHERE (WHERE LOWER(email) = \'...\') can then use the index instead of doing a full table scan.',
        },
        {
          question: 'What does the INCLUDE clause in CREATE INDEX accomplish?',
          options: [
            'It includes the index in query plans automatically',
            'It adds non-key columns to the index leaf nodes, enabling Index Only Scans without those columns being search keys',
            'It includes the index in a backup operation',
            'It copies data from included columns to a faster storage tier',
          ],
          correctIndex: 1,
          explanation: 'INCLUDE adds columns to the index that are not search keys — they are stored in leaf nodes only. Queries that need those columns can be served entirely from the index (Index Only Scan) without fetching from the table heap.',
        },
        {
          question: 'What is a GiST index best suited for?',
          options: [
            'Equality lookups on text columns',
            'Full-text search only',
            'Complex data types like geometric shapes, ranges, and nearest-neighbor searches',
            'Compressed storage of large text documents',
          ],
          correctIndex: 2,
          explanation: 'GiST (Generalized Search Tree) supports complex data types and operations like geometric containment, range overlap, and KNN (nearest-neighbor) search. It is used for PostGIS geography types, range types, and proximity queries.',
        },
      ],
    },
    {
      topicId: 'partitioning',
      topicTitle: 'Partitioning',
      objectiveIndex: 2,
      questions: [
        {
          question: 'What problem does table partitioning primarily solve?',
          options: [
            'Enforcing data integrity across related tables',
            'Managing very large tables by splitting them into smaller physical pieces while maintaining a single logical interface',
            'Speeding up individual row lookups on small tables',
            'Distributing data across multiple database servers',
          ],
          correctIndex: 1,
          explanation: 'Partitioning splits large tables (hundreds of millions to billions of rows) into smaller physical partitions. Benefits include faster queries via partition pruning, faster maintenance (DROP TABLE on old partition), and smaller per-partition indexes.',
        },
        {
          question: 'What is partition pruning?',
          options: [
            'Removing old partitions that exceed their retention period',
            'The planner automatically skipping irrelevant partitions when the query filter matches the partition key',
            'Compressing partitions that have not been accessed recently',
            'Merging small partitions to reduce overhead',
          ],
          correctIndex: 1,
          explanation: 'Partition pruning is when the query planner recognizes that a query\'s WHERE filter excludes certain partitions entirely and scans only the relevant ones. For a date-range query on a monthly-partitioned table, only the relevant month\'s partition is scanned.',
        },
        {
          question: 'When is hash partitioning appropriate vs. range partitioning?',
          options: [
            'Hash partitioning is used for date-based data; range partitioning for arbitrary IDs',
            'Hash partitioning is used for even distribution when there is no natural range key; range partitioning is used for ordered data like dates',
            'They are interchangeable — choose based on index availability',
            'Hash partitioning is only used for user session data',
          ],
          correctIndex: 1,
          explanation: 'Range partitioning suits ordered data where you query by range (dates, IDs). Hash partitioning distributes rows evenly across partitions using a hash function — ideal when you want balanced load with no natural ordering, like UUIDs or session IDs.',
        },
        {
          question: 'Why is dropping an old partition faster than deleting old rows?',
          options: [
            'DROP TABLE bypasses MVCC and is faster for any table',
            'DELETE requires checking foreign keys; DROP TABLE does not',
            'DROP TABLE is a single metadata operation; DELETE must scan, mark, and vacuum millions of individual rows',
            'Partition tables use compressed storage that makes DROP faster',
          ],
          correctIndex: 2,
          explanation: 'DROP TABLE (or DETACH PARTITION + DROP) is a fast metadata operation that removes the entire physical file. DELETE on millions of rows requires scanning rows, marking them as dead, and waiting for VACUUM to reclaim space — much slower.',
        },
        {
          question: 'What must be true about the partition key column when defining a PRIMARY KEY on a partitioned table?',
          options: [
            'The partition key must be the primary key',
            'The partition key must be included in the primary key',
            'The partition key must be a foreign key to another table',
            'The partition key must be a NOT NULL column',
          ],
          correctIndex: 1,
          explanation: 'PostgreSQL requires the partition key column(s) to be part of the primary key on a partitioned table. This is because uniqueness must be enforceable within each partition independently.',
        },
      ],
    },
    {
      topicId: 'locking-and-concurrency',
      topicTitle: 'Locking & Concurrency',
      objectiveIndex: 3,
      questions: [
        {
          question: 'What does SELECT ... FOR UPDATE do?',
          options: [
            'It returns rows and marks them for a pending UPDATE statement',
            'It acquires exclusive row-level locks on the selected rows, preventing other transactions from modifying them until COMMIT',
            'It makes the SELECT result updateable through the result set',
            'It creates a snapshot of the selected rows',
          ],
          correctIndex: 1,
          explanation: 'SELECT FOR UPDATE acquires exclusive locks on the selected rows. Other transactions attempting to UPDATE, DELETE, or FOR UPDATE the same rows will wait until the lock is released at COMMIT or ROLLBACK.',
        },
        {
          question: 'What does SKIP LOCKED do in a SELECT ... FOR UPDATE query?',
          options: [
            'It skips rows that have been updated recently',
            'It skips rows that are currently locked by another transaction instead of waiting',
            'It skips the locking step for performance',
            'It returns locked rows with NULL values for locked columns',
          ],
          correctIndex: 1,
          explanation: 'SKIP LOCKED skips rows that are already locked by another transaction rather than waiting. This is the standard pattern for job queues — multiple workers can each claim different jobs without waiting for each other.',
        },
        {
          question: 'How does MVCC (Multi-Version Concurrency Control) benefit read operations?',
          options: [
            'It caches read results for faster repeated queries',
            'Readers never block writers and writers never block readers — each transaction sees a consistent snapshot',
            'It compresses data to make reads faster',
            'It routes reads to a read replica automatically',
          ],
          correctIndex: 1,
          explanation: 'MVCC maintains multiple versions of rows. Readers see a consistent snapshot of the database as of their transaction start, without blocking writers. Writers create new row versions rather than overwriting, so readers and writers don\'t interfere.',
        },
        {
          question: 'What is the optimistic locking pattern?',
          options: [
            'Trusting that no conflicts will occur and skipping all locking',
            'Reading data without a lock, then updating with a version check — if the version changed since the read, the conflict is detected at write time and the operation retries',
            'Using the lowest isolation level for best performance',
            'Acquiring all required locks at transaction start',
          ],
          correctIndex: 1,
          explanation: 'Optimistic locking reads data without acquiring a lock, then performs the UPDATE with a WHERE version = <read_version> condition. If 0 rows are affected, someone else modified the row — retry is needed. It scales better than pessimistic locking in low-conflict scenarios.',
        },
        {
          question: 'What are advisory locks used for?',
          options: [
            'They are automatic locks created by PostgreSQL for schema changes',
            'Application-level locks managed by PostgreSQL but not tied to any table or row — used to coordinate application logic like preventing duplicate cron jobs',
            'Locks that advisory the DBA before taking effect',
            'Locks on index structures during rebuild operations',
          ],
          correctIndex: 1,
          explanation: 'Advisory locks are application-defined locks using an integer key. They are managed by PostgreSQL (ensuring mutual exclusion) but have no automatic connection to tables or rows. Common uses: preventing duplicate cron job execution, coordinating distributed processing.',
        },
        {
          question: 'What happens to dead tuples in PostgreSQL and why does this matter?',
          options: [
            'Dead tuples are automatically removed after each transaction',
            'Dead tuples accumulate from MVCC row versioning and must be cleaned by VACUUM — long-running transactions prevent cleanup, causing table bloat',
            'Dead tuples are compressed but remain accessible for recovery',
            'Dead tuples only occur from failed transactions and are immediately cleaned up',
          ],
          correctIndex: 1,
          explanation: 'PostgreSQL\'s MVCC creates a new row version on UPDATE rather than overwriting — the old version becomes a "dead tuple." VACUUM reclaims this space. Long-running transactions prevent VACUUM from cleaning up old versions, causing table and index bloat.',
        },
      ],
    },
    {
      topicId: 'performance-tuning',
      topicTitle: 'Performance Tuning',
      objectiveIndex: 4,
      questions: [
        {
          question: 'Why is connection pooling like PgBouncer important for PostgreSQL?',
          options: [
            'It provides automatic query caching across connections',
            'PostgreSQL creates a new OS process per connection — without pooling, hundreds of connections means hundreds of processes consuming significant memory',
            'It automatically distributes queries across multiple servers',
            'It compresses query results to reduce bandwidth',
          ],
          correctIndex: 1,
          explanation: 'PostgreSQL uses a process-per-connection model. Each connection spawns an OS process using ~5-10MB of RAM. Without pooling, 500 connections = 500 processes. PgBouncer multiplexes many client connections onto a small number of actual database connections.',
        },
        {
          question: 'What is the fastest way to load a large amount of data into PostgreSQL?',
          options: [
            'Individual INSERT statements in a loop',
            'Multi-row INSERT with 100 rows per statement',
            'COPY command — which uses the binary COPY protocol and is 10-100x faster than INSERT',
            'INSERT with RETURNING to batch the operations',
          ],
          correctIndex: 2,
          explanation: 'COPY uses PostgreSQL\'s efficient binary copy protocol to load data from a file or stream. It is 10-100x faster than individual INSERTs for bulk operations because it bypasses much of the per-row overhead.',
        },
        {
          question: 'What does VACUUM FULL do differently from regular VACUUM, and what is the risk?',
          options: [
            'VACUUM FULL is faster because it skips index cleanup',
            'VACUUM FULL rewrites the entire table to reclaim physical disk space but acquires ACCESS EXCLUSIVE lock, blocking all operations on the table',
            'VACUUM FULL runs in parallel for faster cleanup',
            'VACUUM FULL only cleans the most bloated pages',
          ],
          correctIndex: 1,
          explanation: 'Regular VACUUM marks dead tuple space as reusable but does not shrink the file. VACUUM FULL rewrites the table compactly, reclaiming disk space, but acquires ACCESS EXCLUSIVE lock — blocking all reads and writes on the table while it runs.',
        },
        {
          question: 'What does pg_stat_statements track?',
          options: [
            'Locks held by active transactions',
            'Execution statistics for all SQL queries — call count, total time, average time, and I/O — enabling identification of the slowest queries',
            'The schema history of all database changes',
            'Connection pool usage statistics',
          ],
          correctIndex: 1,
          explanation: 'pg_stat_statements is a PostgreSQL extension that aggregates execution statistics per normalized query. It is the primary tool for identifying slow queries, high-frequency queries, and I/O-heavy queries in production.',
        },
        {
          question: 'What is transaction mode pooling in PgBouncer?',
          options: [
            'Connections are dedicated to clients for their entire session',
            'Connections are returned to the pool after each transaction completes — most efficient mode',
            'Connections are returned to the pool after each statement',
            'All transactions share a single connection',
          ],
          correctIndex: 1,
          explanation: 'In transaction mode, PgBouncer returns a server connection to the pool after each transaction. This is the most common and efficient mode — a client holding an idle connection between transactions frees the server connection for other clients.',
        },
      ],
    },
    {
      topicId: 'migration-strategies',
      topicTitle: 'Migration Strategies',
      objectiveIndex: 5,
      questions: [
        {
          question: 'What is the "expand and contract" pattern for zero-downtime migrations?',
          options: [
            'Running the migration on a small test database first, then expanding to production',
            'Adding the new schema element while keeping the old one, transitioning application code to use both, then removing the old element after all code is updated',
            'Expanding table storage before adding new columns',
            'Contracting write operations to a single server during migration',
          ],
          correctIndex: 1,
          explanation: 'Expand and contract: (1) Add the new column/table, (2) backfill data, (3) deploy code that writes to both old and new, (4) deploy code that reads from the new only, (5) drop the old column. Each step is backward-compatible with the running application.',
        },
        {
          question: 'Why should you use CREATE INDEX CONCURRENTLY instead of regular CREATE INDEX in production?',
          options: [
            'CONCURRENTLY creates the index faster using parallel workers',
            'Regular CREATE INDEX acquires a lock that blocks all writes on the table while building — CONCURRENTLY does not block writes',
            'CONCURRENTLY creates the index without taking any disk space temporarily',
            'Regular CREATE INDEX does not work on tables with existing data',
          ],
          correctIndex: 1,
          explanation: 'Regular CREATE INDEX acquires a SHARE lock that blocks all INSERT, UPDATE, and DELETE operations while the index is built. CREATE INDEX CONCURRENTLY uses a weaker lock mode that allows writes to continue, at the cost of taking longer to complete.',
        },
        {
          question: 'Why is ALTER TABLE users ADD COLUMN full_name VARCHAR(255) NOT NULL dangerous to run directly on a large table?',
          options: [
            'PostgreSQL does not support adding NOT NULL columns',
            'Adding a NOT NULL column without a DEFAULT requires a full table rewrite to validate every existing row, causing prolonged downtime',
            'The operation will fail if any existing rows have a NULL username',
            'VARCHAR columns cannot be added with NOT NULL constraints after table creation',
          ],
          correctIndex: 1,
          explanation: 'Adding a NOT NULL column to a table with millions of rows without a DEFAULT triggers a full table rewrite to set and validate the value for every row. The safe pattern is: add as nullable, backfill in batches, then add the NOT NULL constraint.',
        },
        {
          question: 'What does ALTER TABLE ... ADD CONSTRAINT ... NOT VALID do?',
          options: [
            'It adds an invalid constraint that must be fixed before the table can be used',
            'It adds a constraint that will be enforced only for new rows — existing rows are not checked immediately',
            'It adds the constraint definition without enforcing it for any rows',
            'It schedules constraint validation for the next maintenance window',
          ],
          correctIndex: 1,
          explanation: 'NOT VALID adds a constraint (e.g., FOREIGN KEY) that is enforced for new inserts and updates but does not immediately validate existing rows. This is instant. A separate VALIDATE CONSTRAINT command then validates existing rows with a weaker lock.',
        },
        {
          question: 'What is a key best practice for migration files in tools like Flyway or Alembic?',
          options: [
            'Always combine schema changes and data migrations in the same file',
            'Never edit a migration file that has been applied to production — create a new one instead',
            'Keep all migrations in a single file for easy review',
            'Run all migrations in a single transaction for atomicity',
          ],
          correctIndex: 1,
          explanation: 'Migration tools track which migrations have been applied by their version/checksum. Editing an already-applied migration creates inconsistency between environments. Always create new migration files for new changes.',
        },
      ],
    },
    {
      topicId: 'advanced-patterns',
      topicTitle: 'Advanced Patterns',
      objectiveIndex: 6,
      questions: [
        {
          question: 'What is a temporal table pattern and what does it enable?',
          options: [
            'A table partitioned by time for performance',
            'A table that tracks the full history of every row change using valid_from/valid_to columns, enabling time-travel queries',
            'A table that automatically expires old rows based on a TTL',
            'A table used for temporary query results during complex operations',
          ],
          correctIndex: 1,
          explanation: 'Temporal tables use valid_from and valid_to timestamp columns to track all versions of a row. You can query what a row\'s value was at any point in time. Current records have valid_to = \'infinity\'.',
        },
        {
          question: 'What is the primary advantage of soft deletes over hard deletes?',
          options: [
            'Soft deletes are faster because they avoid index updates',
            'Soft deletes preserve data for auditing, compliance, and recovery by marking rows as deleted rather than removing them',
            'Soft deletes automatically archive data to cold storage',
            'Soft deletes prevent other users from seeing the deleted record immediately',
          ],
          correctIndex: 1,
          explanation: 'Soft deletes (setting deleted_at timestamp) preserve all data, enabling compliance audit trails, undo functionality, and data recovery. The trade-off is that all queries must filter WHERE deleted_at IS NULL, often enforced via Row-Level Security or views.',
        },
        {
          question: 'What PostgreSQL operator checks if a JSONB column contains a specific key-value pair?',
          options: ['->>', '@>', '?', '#>>'],
          correctIndex: 1,
          explanation: 'The @> "contains" operator checks if the left JSONB value contains the right JSONB fragment. For example, "data @> \'{\\"status\\": \\"failed\\"}\'" finds all rows where the JSONB data has status equal to "failed". GIN indexes on JSONB support this operator.',
        },
        {
          question: 'What does a trigger-based audit log table track?',
          options: [
            'Only SELECT queries run against the table',
            'The full old and new row data for every INSERT, UPDATE, and DELETE, including who made the change and when',
            'Schema changes made by database administrators',
            'Connection attempts and query execution times',
          ],
          correctIndex: 1,
          explanation: 'An audit log trigger captures INSERT, UPDATE, and DELETE operations, recording the old (pre-change) and new (post-change) row data as JSONB, along with the current user and timestamp. This enables full change history queries.',
        },
        {
          question: 'What PostgreSQL data type and operator is used for full-text search?',
          options: [
            'TEXT with LIKE \'%keyword%\'',
            'tsvector column with @@ operator and tsquery',
            'VARCHAR with ILIKE for case-insensitive matching',
            'JSONB with the ? operator',
          ],
          correctIndex: 1,
          explanation: 'Full-text search in PostgreSQL uses tsvector (indexed lexeme vectors) and tsquery (search expressions). The @@ operator checks if a tsvector matches a tsquery. GIN indexes on tsvector columns make full-text search fast.',
        },
        {
          question: 'How does Row-Level Security (RLS) relate to soft deletes?',
          options: [
            'RLS prevents soft-deleted rows from being restored',
            'RLS can automatically filter soft-deleted rows from all queries for a role, without requiring WHERE deleted_at IS NULL in every query',
            'RLS is required for soft deletes to work correctly',
            'RLS and soft deletes conflict and cannot be used together',
          ],
          correctIndex: 1,
          explanation: 'Row-Level Security policies can automatically filter out soft-deleted rows for specific roles. A policy USING (deleted_at IS NULL) applied to all SELECT operations means application queries never see deleted rows without needing an explicit WHERE clause.',
        },
      ],
    },
  ],

  exams: {
    beginner: [
      {
        question: 'What is wrong with "SELECT * FROM users WHERE phone = NULL"?',
        options: [
          'NULL should be in quotes: phone = \'NULL\'',
          'You cannot use = with NULL; use IS NULL instead',
          'WHERE cannot filter on nullable columns',
          'SELECT * should be SELECT phone',
        ],
        correctIndex: 1,
        explanation: 'NULL cannot be compared with = because NULL represents an unknown value and any comparison with NULL returns UNKNOWN. The correct syntax is WHERE phone IS NULL.',
      },
      {
        question: 'Which clause is processed before GROUP BY in SQL\'s logical processing order?',
        options: ['HAVING', 'SELECT', 'ORDER BY', 'WHERE'],
        correctIndex: 3,
        explanation: 'SQL processes FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT. WHERE filters individual rows before they are grouped, which is why you cannot use aggregate functions in a WHERE clause.',
      },
      {
        question: 'A query returns customers who have never placed an order. Which JOIN type and filter achieves this?',
        options: [
          'INNER JOIN orders, WHERE order_count = 0',
          'LEFT JOIN orders ON customer_id, WHERE o.order_id IS NULL',
          'RIGHT JOIN orders ON customer_id, WHERE c.customer_id IS NULL',
          'FULL OUTER JOIN orders, WHERE total = 0',
        ],
        correctIndex: 1,
        explanation: 'LEFT JOIN returns all customers including those with no matching orders. When no order exists, order columns are NULL. Filtering WHERE o.order_id IS NULL isolates the customers with no orders.',
      },
      {
        question: 'What does COUNT(phone) return when 3 out of 10 employees have a NULL phone?',
        options: ['10', '3', '7', 'NULL'],
        correctIndex: 2,
        explanation: 'COUNT(column_name) counts only non-NULL values. With 3 NULLs out of 10, COUNT(phone) returns 7. COUNT(*) would return 10.',
      },
      {
        question: 'Which SQL correctly finds the top 5 products by price?',
        options: [
          'SELECT product_name FROM products TOP 5 ORDER BY price DESC',
          'SELECT product_name FROM products ORDER BY price DESC LIMIT 5',
          'SELECT TOP(5) product_name FROM products ORDER BY price DESC',
          'SELECT product_name FROM products HAVING price > 0 LIMIT 5',
        ],
        correctIndex: 1,
        explanation: 'The standard PostgreSQL syntax uses ORDER BY with LIMIT. SELECT TOP is SQL Server syntax. HAVING without GROUP BY is not correct here.',
      },
      {
        question: 'Why does "WHERE (category = \'Electronics\' OR category = \'Books\') AND price > 20" need parentheses?',
        options: [
          'Parentheses are always required around OR conditions',
          'Without parentheses, AND binds tighter than OR, giving "Electronics OR (Books AND price > 20)" — a different meaning',
          'Parentheses are needed to help the planner use indexes',
          'OR conditions without parentheses cause a syntax error',
        ],
        correctIndex: 1,
        explanation: 'AND has higher precedence than OR. Without parentheses, the condition is read as "category = \'Electronics\' OR (category = \'Books\' AND price > 20)", which would include all Electronics regardless of price.',
      },
      {
        question: 'What constraint prevents negative prices from being stored in a products table?',
        options: ['NOT NULL', 'UNIQUE', 'CHECK (price >= 0)', 'DEFAULT 0'],
        correctIndex: 2,
        explanation: 'A CHECK constraint enforces a custom validation condition. CHECK (price >= 0) rejects any INSERT or UPDATE that would store a negative price.',
      },
      {
        question: 'What does the B-tree index default store at its leaf nodes?',
        options: [
          'A full copy of the indexed row',
          'The indexed column values plus row IDs pointing back to the table',
          'A hash of the indexed values',
          'The column values only, with no row references',
        ],
        correctIndex: 1,
        explanation: 'B-tree leaf nodes store the indexed column values plus heap tuple IDs (ctid) that point to the actual rows in the table. The index is not a copy of the table — extra columns must be fetched from the heap separately.',
      },
      {
        question: 'What is the safe pattern for running a DELETE in production?',
        options: [
          'Always wrap in a stored procedure',
          'First run a SELECT with the same WHERE clause to verify the affected rows, then run the DELETE',
          'Always use TRUNCATE instead of DELETE for safety',
          'Run DELETE with LIMIT 1 first to test',
        ],
        correctIndex: 1,
        explanation: 'The safe DELETE pattern is to first run a SELECT with the identical WHERE clause to see exactly which rows will be affected. Only after verifying the results look correct should you run the DELETE.',
      },
      {
        question: 'Which statement about BETWEEN is true?',
        options: [
          'BETWEEN is exclusive on both ends',
          'BETWEEN is inclusive on both ends',
          'BETWEEN is inclusive on the lower bound only',
          'BETWEEN is exclusive on the lower bound only',
        ],
        correctIndex: 1,
        explanation: 'BETWEEN is inclusive on both bounds. "price BETWEEN 10 AND 50" is equivalent to "price >= 10 AND price <= 50", including both 10 and 50.',
      },
      {
        question: 'What does DISTINCT do when applied to multiple columns?',
        options: [
          'Returns rows where all specified columns are distinct',
          'Returns unique combinations of the specified columns',
          'Returns only the first unique value per column',
          'Applies DISTINCT to only the first column listed',
        ],
        correctIndex: 1,
        explanation: 'DISTINCT on multiple columns returns unique combinations of those columns. Two rows are considered duplicates only if every specified column has the same value.',
      },
      {
        question: 'Which INSERT pattern efficiently captures the newly assigned auto-increment ID?',
        options: [
          'INSERT INTO ... VALUES ...; SELECT MAX(id) FROM table;',
          'INSERT INTO ... VALUES ... RETURNING id;',
          'SELECT nextval(\'sequence\'); INSERT INTO ... VALUES ...;',
          'INSERT INTO ... VALUES ...; SELECT LAST_INSERT_ID();',
        ],
        correctIndex: 1,
        explanation: 'RETURNING returns data from the inserted row, including generated values like SERIAL primary keys. This is more reliable and efficient than a separate MAX() query, which could return a different row in a concurrent environment.',
      },
      {
        question: 'What aggregate function would you use to find the earliest hire date in a department?',
        options: ['FIRST(hire_date)', 'MIN(hire_date)', 'EARLIEST(hire_date)', 'LIMIT 1 ORDER BY hire_date'],
        correctIndex: 1,
        explanation: 'MIN() returns the minimum value in a group, which for dates is the earliest date. There is no FIRST() or EARLIEST() aggregate in standard SQL.',
      },
      {
        question: 'A FOREIGN KEY with ON DELETE RESTRICT will:',
        options: [
          'Delete all child rows when the parent is deleted',
          'Set child rows to NULL when the parent is deleted',
          'Prevent the parent from being deleted if child rows exist',
          'Do nothing when the parent is deleted',
        ],
        correctIndex: 2,
        explanation: 'ON DELETE RESTRICT prevents deletion of the parent row if any child rows reference it. This is the strictest referential integrity option — you must delete the children first.',
      },
      {
        question: 'What does EXISTS return when its subquery finds at least one row?',
        options: ['The count of matching rows', 'The first matching row', 'TRUE', 'NULL'],
        correctIndex: 2,
        explanation: 'EXISTS returns TRUE if the subquery returns any rows, FALSE otherwise. It short-circuits as soon as the first matching row is found, making it efficient for checking existence.',
      },
      {
        question: 'What is an index\'s biggest drawback?',
        options: [
          'Indexes slow down SELECT queries',
          'Indexes consume extra disk space and add overhead to INSERT, UPDATE, and DELETE operations',
          'Indexes prevent the use of WHERE clauses',
          'Indexes are lost when the database restarts',
        ],
        correctIndex: 1,
        explanation: 'Every index must be maintained on write operations. Each INSERT, UPDATE, or DELETE must update all relevant indexes on the affected table. This overhead is usually worth it for read-heavy tables but can hurt write performance.',
      },
      {
        question: 'What does a correlated subquery do differently from a regular subquery?',
        options: [
          'It runs once and caches its result for the entire outer query',
          'It references a column from the outer query and is re-evaluated for every outer row',
          'It always runs faster than non-correlated subqueries',
          'It can return multiple columns unlike a regular subquery',
        ],
        correctIndex: 1,
        explanation: 'A correlated subquery references columns from its outer query, making it dependent on each outer row. It must be re-executed for every row in the outer query, which can be slow but enables per-row computations like comparing against a group average.',
      },
    ],

    mid: [
      {
        question: 'What keyword is required in the WITH clause for recursive CTEs in PostgreSQL?',
        options: ['WITH LOOP', 'WITH RECURSIVE', 'WITH SELF', 'WITH ITERATE'],
        correctIndex: 1,
        explanation: 'PostgreSQL requires WITH RECURSIVE to enable a CTE to reference itself. Without RECURSIVE, a self-referencing CTE will produce an error.',
      },
      {
        question: 'What is the difference between ROW_NUMBER() and RANK() when two rows have equal values?',
        options: [
          'They always produce identical results',
          'ROW_NUMBER assigns unique sequential numbers; RANK assigns the same number to ties and skips the next rank',
          'RANK assigns unique numbers; ROW_NUMBER assigns the same number to ties',
          'ROW_NUMBER skips numbers after ties; RANK does not',
        ],
        correctIndex: 1,
        explanation: 'ROW_NUMBER always assigns a unique number — ties get different numbers based on arbitrary ordering. RANK assigns the same number to tied rows but skips the next rank (1,1,3). DENSE_RANK assigns the same number to ties without skipping (1,1,2).',
      },
      {
        question: 'What isolation level prevents a transaction from seeing changes committed by other transactions after it starts?',
        options: ['Read Uncommitted', 'Read Committed', 'Repeatable Read', 'Both Repeatable Read and Serializable'],
        correctIndex: 3,
        explanation: 'Both Repeatable Read and Serializable provide snapshot isolation — the transaction sees a consistent view of data from when it started, unaffected by concurrent commits. Read Committed allows seeing newly committed data within the same transaction.',
      },
      {
        question: 'A materialized view on a dashboard query is 1 day old. What is the correct command to update it without blocking reads?',
        options: [
          'UPDATE MATERIALIZED VIEW monthly_sales_report',
          'REFRESH MATERIALIZED VIEW CONCURRENTLY monthly_sales_report',
          'REBUILD MATERIALIZED VIEW monthly_sales_report',
          'REFRESH MATERIALIZED VIEW monthly_sales_report',
        ],
        correctIndex: 1,
        explanation: 'REFRESH MATERIALIZED VIEW CONCURRENTLY refreshes the data without acquiring an exclusive lock that blocks reads. Regular REFRESH blocks all reads. CONCURRENTLY requires a unique index on the materialized view.',
      },
      {
        question: 'A CTE called "customer_spending" calculates total spending per customer. A second CTE "customer_tiers" classifies them. Can "customer_tiers" reference "customer_spending"?',
        options: [
          'No — CTEs cannot reference other CTEs',
          'Yes — later CTEs in the same WITH block can reference earlier ones',
          'Only if RECURSIVE is specified',
          'Only if they are in separate WITH blocks',
        ],
        correctIndex: 1,
        explanation: 'CTEs in the same WITH block are evaluated in order. Later CTEs can reference earlier ones, enabling step-by-step pipeline transformations. This is one of the most powerful features of chained CTEs.',
      },
      {
        question: 'What does PARTITION BY do differently from GROUP BY in window functions?',
        options: [
          'They are interchangeable in window functions',
          'PARTITION BY divides rows into groups for window calculation without collapsing rows; GROUP BY collapses rows into one per group',
          'GROUP BY can only be used with aggregate functions; PARTITION BY with any function',
          'PARTITION BY is for physical table partitions; GROUP BY is for query grouping',
        ],
        correctIndex: 1,
        explanation: 'PARTITION BY in a window function creates groups for the window calculation but preserves all individual rows in the output. GROUP BY collapses all rows in a group into a single output row.',
      },
      {
        question: 'What normal form is violated when the same data appears in multiple rows (e.g., department_name stored in every employee row)?',
        options: ['1NF', '2NF', '3NF — transitive dependency', 'BCNF'],
        correctIndex: 2,
        explanation: 'When department_name depends on department_id (which depends on employee_id), this is a transitive dependency violating 3NF. The fix is to create a departments table and store department_name there, not in the employees table.',
      },
      {
        question: 'What does a Seq Scan on a large table in EXPLAIN output suggest?',
        options: [
          'The query is already optimal',
          'A missing index, a function masking the indexed column, or wrong column order in a composite index',
          'The table is too small to benefit from an index',
          'The query needs to be rewritten as a stored procedure',
        ],
        correctIndex: 1,
        explanation: 'Sequential scan on a large table often indicates a performance problem. Common causes: no index on the filtered column, a function wrapped around the column (YEAR(order_date)), or a composite index where the leading column is not in the query.',
      },
      {
        question: 'What does a stored procedure in PostgreSQL 11+ support that a function does not?',
        options: [
          'Returning multiple result sets',
          'Managing transactions — COMMIT and ROLLBACK within the procedure body',
          'Accepting parameters by reference',
          'Accessing system catalogs',
        ],
        correctIndex: 1,
        explanation: 'PostgreSQL 11+ stored procedures (CREATE PROCEDURE, called with CALL) can contain COMMIT and ROLLBACK statements. Functions run inside the calling transaction and cannot commit or roll back independently.',
      },
      {
        question: 'What does LAG(salary) OVER (PARTITION BY employee_id ORDER BY effective_date) compute?',
        options: [
          'The average salary for the employee',
          'The salary from the previous row for the same employee, ordered by effective date',
          'The salary from the most recent hire in the department',
          'The total salary for the partition',
        ],
        correctIndex: 1,
        explanation: 'LAG looks at the previous row within the partition. Here it returns the salary from the previous effective_date for the same employee_id, enabling calculation of salary changes over time.',
      },
      {
        question: 'What is the purpose of SAVEPOINT in a transaction?',
        options: [
          'To permanently commit partial work during a long transaction',
          'To mark a point within a transaction to which you can partially roll back without aborting the entire transaction',
          'To improve performance by checkpointing intermediate results',
          'To lock rows at a specific point in time',
        ],
        correctIndex: 1,
        explanation: 'SAVEPOINT creates a named restore point within a transaction. ROLLBACK TO SAVEPOINT undoes work done after the savepoint while preserving earlier work. The full transaction can still be committed or rolled back afterwards.',
      },
      {
        question: 'When should you denormalize a database schema?',
        options: [
          'Always, for better read performance',
          'Never — normalization always provides better integrity and performance',
          'In read-heavy workloads where JOIN performance is a proven bottleneck and data changes infrequently',
          'Only when the table has more than 10 columns',
        ],
        correctIndex: 2,
        explanation: 'Denormalization trades data integrity risk for read performance. It is justified for analytics tables, reporting, and caching layers (materialized views) when JOINs are proven to be a bottleneck and the denormalized data changes infrequently.',
      },
      {
        question: 'What does NTILE(10) OVER (ORDER BY total_spent DESC) compute?',
        options: [
          'The top 10 customers by spending',
          'Deciles — assigning each customer a number from 1-10 based on their spending rank',
          'The 10th percentile spending value',
          'A running total grouped into 10 buckets',
        ],
        correctIndex: 1,
        explanation: 'NTILE(10) divides rows into 10 roughly equal groups (deciles) and assigns each row a number from 1 to 10. NTILE(1) would put everyone in one bucket; NTILE(100) creates percentiles.',
      },
      {
        question: 'In a view with WITH CHECK OPTION, what happens if you try to UPDATE a row so it no longer satisfies the view\'s WHERE condition?',
        options: [
          'The update succeeds and the row disappears from the view',
          'The update is rejected with an error',
          'The view is automatically refreshed to show the updated row',
          'The row is moved to a default partition',
        ],
        correctIndex: 1,
        explanation: 'WITH CHECK OPTION prevents UPDATE or INSERT operations through a view that would result in rows no longer visible through the view. The operation is rejected with an error to prevent rows from silently "disappearing" from the view.',
      },
      {
        question: 'What is the query rewrite for "WHERE YEAR(order_date) = 2025" to allow index use?',
        options: [
          'WHERE order_date LIKE \'2025%\'',
          'WHERE order_date >= \'2025-01-01\' AND order_date < \'2026-01-01\'',
          'WHERE EXTRACT(YEAR FROM order_date) = 2025',
          'WHERE order_date BETWEEN \'2025\' AND \'2026\'',
        ],
        correctIndex: 1,
        explanation: 'Functions applied to indexed columns prevent index use. Rewriting as an explicit range (>= start AND < end) allows the B-tree index on order_date to efficiently scan only the relevant leaf nodes.',
      },
      {
        question: 'How do you prevent a deadlock between two transactions that both need to update accounts 1 and 2?',
        options: [
          'Use Serializable isolation level',
          'Always acquire locks in a consistent order — e.g., always lock the lower account_id first',
          'Use advisory locks before the transaction',
          'Increase the lock timeout',
        ],
        correctIndex: 1,
        explanation: 'Deadlocks occur when transactions acquire locks in different orders. The prevention technique is to always acquire locks in a consistent, predetermined order (e.g., by account_id ascending). This eliminates the circular wait condition.',
      },
    ],

    senior: [
      {
        question: 'The planner estimates 100 rows but EXPLAIN ANALYZE shows 50,000 actual rows. What is the most likely cause?',
        options: [
          'The query has a bug in its WHERE clause',
          'Stale planner statistics — running ANALYZE should improve the estimate',
          'The index is corrupted',
          'The table is too large for accurate estimates',
        ],
        correctIndex: 1,
        explanation: 'A large discrepancy between estimated and actual rows indicates stale statistics. The planner uses pg_statistic data updated by ANALYZE. Running ANALYZE on the table refreshes the statistics and usually fixes bad estimates.',
      },
      {
        question: 'What scan type uses the least I/O for a highly selective query on a covering index?',
        options: ['Sequential Scan', 'Index Scan', 'Bitmap Heap Scan', 'Index Only Scan'],
        correctIndex: 3,
        explanation: 'An Index Only Scan reads all needed columns directly from the index without accessing the table heap. It shows "Heap Fetches: 0" in EXPLAIN ANALYZE and is the fastest scan type for queries where the index covers all needed columns.',
      },
      {
        question: 'What is the trade-off of CREATE INDEX CONCURRENTLY vs regular CREATE INDEX?',
        options: [
          'CONCURRENTLY creates smaller indexes',
          'CONCURRENTLY does not block writes but takes longer, runs in its own transaction, and can leave an INVALID index that must be dropped and recreated if it fails',
          'Regular CREATE INDEX is always faster',
          'CONCURRENTLY cannot be used on tables with existing data',
        ],
        correctIndex: 1,
        explanation: 'CONCURRENTLY avoids the write-blocking lock but takes longer and runs outside the caller\'s transaction. If it fails, it leaves an INVALID index that must be explicitly dropped before trying again.',
      },
      {
        question: 'What makes a partial index smaller and faster than a full-table index for pending orders?',
        options: [
          'Partial indexes use compression that full indexes cannot',
          'Only rows matching the WHERE condition are indexed — for orders, pending orders are a tiny fraction of historical orders',
          'Partial indexes skip the B-tree structure for simpler lookups',
          'The WHERE condition eliminates null values from the index',
        ],
        correctIndex: 1,
        explanation: 'A partial index on (order_date) WHERE status = \'pending\' only indexes current pending orders. Historical completed/cancelled orders are excluded. This makes the index dramatically smaller, faster to read, and cheaper to maintain.',
      },
      {
        question: 'What does VACUUM FULL do to a heavily bloated table, and when should it be used?',
        options: [
          'VACUUM FULL is the default vacuum mode — use it weekly',
          'VACUUM FULL rewrites the table compactly, reclaiming physical disk space, but requires ACCESS EXCLUSIVE lock blocking all operations — use only during maintenance windows',
          'VACUUM FULL is faster than regular VACUUM and should always be preferred',
          'VACUUM FULL only removes dead tuples without rewriting the table',
        ],
        correctIndex: 1,
        explanation: 'VACUUM FULL rewrites the entire table to a new physical file, reclaiming disk space. The ACCESS EXCLUSIVE lock blocks all reads and writes during this operation. For production systems, use it only during maintenance windows when bloat is severe.',
      },
      {
        question: 'In the expand-and-contract migration pattern, why must the application write to both old and new columns during the transition phase?',
        options: [
          'To double-check data integrity during migration',
          'To ensure backward compatibility — if the deployment is rolled back, the old column still has current data',
          'Because the new column has a NOT NULL constraint',
          'To allow A/B testing of the new schema',
        ],
        correctIndex: 1,
        explanation: 'Writing to both columns during transition ensures that a rollback to the previous code version still works — the old column remains up-to-date. Once the new code is fully deployed and stable, writing to the old column can stop and it can eventually be dropped.',
      },
      {
        question: 'What is optimistic locking and when does it scale better than pessimistic locking?',
        options: [
          'Optimistic locking acquires locks earlier than needed for safety; it always scales better',
          'Optimistic locking detects conflicts at write time using a version check rather than pre-acquiring locks — scales better when conflicts are rare',
          'Optimistic locking skips all conflict detection for maximum throughput',
          'Optimistic locking uses advisory locks instead of row locks',
        ],
        correctIndex: 1,
        explanation: 'Optimistic locking reads without a lock and checks at write time if the version changed. When conflicts are rare, this allows high concurrency. Pessimistic locking pre-acquires locks, which creates contention even when no conflict would have occurred.',
      },
      {
        question: 'What does partition pruning require to work correctly?',
        options: [
          'An index on every partition',
          'The query\'s WHERE filter must reference the partition key column directly (without function wrapping)',
          'VACUUM must have been run on all partitions',
          'The table must have at least 10 partitions',
        ],
        correctIndex: 1,
        explanation: 'Partition pruning works when the planner can determine at plan time which partitions are relevant. The WHERE clause must filter on the partition key directly. Wrapping the partition key in a function (e.g., YEAR(created_at)) can prevent pruning.',
      },
      {
        question: 'What is the GIN index\'s primary advantage over B-tree for JSONB columns?',
        options: [
          'GIN indexes are smaller than B-tree indexes on JSON data',
          'GIN indexes individual keys and values within JSONB documents, enabling fast containment (@>) and key existence (?) queries',
          'GIN indexes support ORDER BY on JSONB fields',
          'GIN indexes prevent concurrent writes to JSONB columns',
        ],
        correctIndex: 1,
        explanation: 'GIN (Generalized Inverted Index) decomposes JSONB documents into individual key-value pairs and indexes each one. This enables fast lookups for containment (@>), key existence (?), and other JSONB operators that a B-tree index on the entire JSONB value cannot support.',
      },
      {
        question: 'What does pg_stat_statements primarily help you identify?',
        options: [
          'Tables that need VACUUM',
          'The slowest and most resource-intensive queries aggregated across all executions',
          'Deadlocks and lock contention',
          'Invalid indexes and corrupted data',
        ],
        correctIndex: 1,
        explanation: 'pg_stat_statements normalizes queries (replacing literals with placeholders) and aggregates execution statistics across all calls. It shows total time, average time, call count, and I/O — the primary tool for finding which queries cost the most overall.',
      },
      {
        question: 'What is the purpose of an audit log trigger that stores to_jsonb(OLD) and to_jsonb(NEW)?',
        options: [
          'To compress old row data for archival',
          'To capture the complete before and after state of every changed row, enabling forensic queries about what changed, who changed it, and when',
          'To replicate changes to another database',
          'To validate that the new data satisfies all constraints',
        ],
        correctIndex: 1,
        explanation: 'Audit triggers capturing old and new row data as JSONB create a complete change history. You can query exactly what a row looked like before any change, who made it, and when — essential for compliance (GDPR, SOX, HIPAA) and debugging.',
      },
      {
        question: 'What is the key reason to use connection pooling in transaction mode (PgBouncer) rather than session mode?',
        options: [
          'Transaction mode supports prepared statements while session mode does not',
          'Transaction mode returns server connections to the pool after each transaction, allowing far more application clients than available server connections',
          'Transaction mode provides better security than session mode',
          'Session mode does not work with PostgreSQL 14+',
        ],
        correctIndex: 1,
        explanation: 'In transaction mode, a server connection is held only for the duration of a transaction, then returned to the pool. This allows many idle application clients to share a small pool of server connections, dramatically increasing scalability.',
      },
      {
        question: 'A temporal table has valid_from and valid_to columns. How do you query the state of a product on 2025-03-15?',
        options: [
          'WHERE valid_from = \'2025-03-15\'',
          'WHERE valid_from <= \'2025-03-15\' AND valid_to > \'2025-03-15\'',
          'WHERE valid_to = \'2025-03-15\'',
          'WHERE order_date BETWEEN valid_from AND valid_to',
        ],
        correctIndex: 1,
        explanation: 'To find the row version active on a specific date, check that the date falls within the row\'s validity range: valid_from <= target_date AND valid_to > target_date. This correctly handles both historical records and the current record (valid_to = \'infinity\').',
      },
      {
        question: 'What does ALTER TABLE orders ADD CONSTRAINT fk ... NOT VALID accomplish vs. a regular ADD CONSTRAINT?',
        options: [
          'NOT VALID creates an invalid constraint that allows violations',
          'NOT VALID adds the constraint instantly without validating existing rows, then VALIDATE CONSTRAINT checks existing rows with a weaker lock — avoids blocking writes on large tables',
          'NOT VALID is the same as DEFERRABLE INITIALLY DEFERRED',
          'NOT VALID disables the constraint until explicitly enabled',
        ],
        correctIndex: 1,
        explanation: 'NOT VALID skips validation of existing rows (instant operation), enforcing the constraint only for new writes. A separate VALIDATE CONSTRAINT command then validates existing rows using a ShareUpdateExclusiveLock, which allows concurrent DML during validation.',
      },
      {
        question: 'In a Hash Join, which input is hashed and why?',
        options: [
          'The larger table is always hashed to maximize cache efficiency',
          'The smaller table is hashed into memory; the larger table is probed row by row against the hash',
          'Both tables are hashed in parallel for maximum performance',
          'The table with the index is hashed to avoid index access',
        ],
        correctIndex: 1,
        explanation: 'The planner hashes the smaller input into an in-memory hash table, then probes it with rows from the larger input. This is O(N + M) and efficient when the hash table fits in work_mem. If the hash table spills to disk, performance degrades.',
      },
      {
        question: 'What does "bloat" mean in the context of PostgreSQL tables and indexes?',
        options: [
          'Tables that have grown too large for available RAM',
          'Wasted space from dead tuples and old MVCC row versions that VACUUM has not yet reclaimed, causing files to be larger than the live data',
          'Tables with too many indexes consuming excess storage',
          'Query plans that are unnecessarily complex',
        ],
        correctIndex: 1,
        explanation: 'Bloat is the accumulation of dead tuples from UPDATE and DELETE operations under MVCC. Regular VACUUM marks dead space as reusable but does not shrink the file. Tables with high update rates can grow significantly larger than their live data size until VACUUM FULL is run.',
      },
    ],
  },
};
