export default {
  beginner: [
    { term: 'API Data Ingestion', definition: 'Pulling data from external systems by making HTTP requests against their REST or GraphQL endpoints — paginating through results and handling authentication, rate limits, and retries on transient errors.' },
    { term: 'Avro', definition: 'A compact, row-oriented binary format with an embedded schema, widely used for event payloads in Kafka and other streaming systems where producers and consumers must agree on structure.' },
    { term: 'Batch Processing', definition: 'Running pipelines against large, bounded chunks of data on a schedule — hourly or nightly — as opposed to processing events one at a time as they arrive.' },
    { term: 'CRUD Operations', definition: 'The four basic persistent-storage operations — Create, Read, Update, Delete — mapped onto SQL statements (INSERT, SELECT, UPDATE, DELETE) and most database or API interactions.' },
    { term: 'CSV', definition: 'A plain-text tabular format using comma-separated values — universally supported, human-readable, and schemaless, but slow and bloated compared to columnar formats for analytical workloads.' },
    { term: 'CTE (Common Table Expression)', definition: 'A named, temporary result set declared with a WITH clause that makes multi-step SQL queries readable by breaking logic into sequential building blocks rather than deeply nested subqueries.' },
    { term: 'Data Catalogue', definition: 'A searchable inventory of the data assets an organisation holds — tables, views, topics, reports — together with their owners, schemas, and descriptions, enabling discovery and trust.' },
    { term: 'Data Lake', definition: 'Low-cost, large-scale storage (typically object storage such as S3 or ADLS) that holds raw data in its native structured, semi-structured, or unstructured form for flexible downstream processing.' },
    { term: 'Data Modelling', definition: 'The discipline of deciding how entities, attributes, and relationships are represented in storage — from conceptual ER diagrams down to physical tables — so that data fits its intended use.' },
    { term: 'Data Pipeline', definition: 'An automated sequence of steps that moves data from source systems through transformations to a destination, typically scheduled or event-triggered and monitored for failures.' },
    { term: 'Data Warehouse', definition: 'A centralised repository optimised for analytical queries over structured historical data, typically modelled for reporting and BI — Snowflake, BigQuery, Redshift, and Synapse are common examples.' },
    { term: 'DDL (Data Definition Language)', definition: 'The subset of SQL used to define and modify schema objects — CREATE, ALTER, DROP, TRUNCATE — as opposed to reading or changing the rows inside them.' },
    { term: 'Dimension Table', definition: 'A table of descriptive attributes — customer, product, date, geography — that gives context to the numeric facts in a star schema, typically denormalised for fast joins.' },
    { term: 'DML (Data Manipulation Language)', definition: 'The subset of SQL that changes the rows inside tables — INSERT, UPDATE, DELETE, MERGE — separate from schema-level definitions and privilege management.' },
    { term: 'ELT (Extract, Load, Transform)', definition: 'A modern ingestion pattern that lands raw data in the warehouse first and transforms it there using SQL, leveraging the warehouse compute power — now the default over classic ETL.' },
    { term: 'ETL (Extract, Transform, Load)', definition: 'A data integration pattern that pulls data from sources, transforms it in a staging layer — cleaning, enriching, reshaping — and loads the result into a target warehouse or mart.' },
    { term: 'Fact Table', definition: 'The central table in a dimensional model holding numeric measurements of a business process — sales, claims, page views — alongside foreign keys that reference dimension tables.' },
    { term: 'Foreign Key', definition: 'A column or set of columns in one table that references the primary key of another, enforcing referential integrity and defining the relationships that give relational databases their name.' },
    { term: 'Idempotent Pipeline', definition: 'A pipeline whose output is the same whether it runs once or many times for the same input — essential for safe retries, backfills, and recovery from partial failures.' },
    { term: 'Incremental Loading', definition: 'Processing only the rows that are new or changed since the last run — typically driven by a watermark column or CDC feed — instead of reprocessing every record each cycle.' },
    { term: 'Join Types', definition: 'SQL clauses that combine rows from multiple tables based on a related column — INNER keeps only matches, LEFT/RIGHT keep unmatched rows from one side, FULL OUTER keeps both.' },
    { term: 'JSON (JavaScript Object Notation)', definition: 'A lightweight text format using nested key-value pairs and arrays — the default payload for REST APIs and event logs, verbose on disk but human-readable and flexible.' },
    { term: 'JSON Lines (JSONL)', definition: 'A file format where each line is a standalone JSON document, enabling streaming reads and line-by-line processing — common for logs and append-only event data.' },
    { term: 'Normalisation', definition: 'The process of organising relational tables to eliminate redundancy and enforce consistency — First, Second, and Third Normal Form each remove a specific class of duplication.' },
    { term: 'OLTP vs OLAP', definition: 'OLTP systems are tuned for many small transactional reads and writes on operational data, while OLAP systems are tuned for wide analytical scans and aggregations over historical data.' },
    { term: 'ORM (Object-Relational Mapping)', definition: 'A library — SQLAlchemy, Django ORM — that maps database tables to programming-language objects, letting engineers query and manipulate data without hand-writing SQL.' },
    { term: 'Parquet', definition: 'A columnar, compressed, binary file format with embedded schema metadata — the de facto standard for analytical data on cloud object storage because reads only fetch the needed columns.' },
    { term: 'Pipeline Orchestration', definition: 'The layer that schedules pipeline tasks, tracks their dependencies, retries failures, and exposes monitoring — Airflow, Dagster, and Prefect are the common open-source choices.' },
    { term: 'Primary Key', definition: 'A column or composite set of columns that uniquely identifies each row in a table — enforced by the database and used as the natural anchor for foreign-key references.' },
    { term: 'PySpark', definition: 'The Python API for Apache Spark — lets engineers write distributed DataFrame and SQL transformations in Python while Spark handles cluster execution and parallelism.' },
    { term: 'Relational vs NoSQL', definition: 'Relational stores such as PostgreSQL enforce schemas and use SQL, while NoSQL stores — MongoDB, DynamoDB, Cassandra — use flexible document, key-value, wide-column, or graph models for different trade-offs.' },
    { term: 'REST API', definition: 'An architectural style for web services using standard HTTP methods on resource URLs — the most common pattern Data Engineers integrate with when pulling data from SaaS or internal systems.' },
    { term: 'Schema', definition: 'The formal definition of a dataset or database — tables, columns, data types, constraints, and relationships — that determines how data is organised, validated, and queried.' },
    { term: 'Snowflake Schema', definition: 'A dimensional-modelling variant of the star schema where dimension tables are further normalised into sub-tables — storage-efficient but requires more joins at query time.' },
    { term: 'SQL (Structured Query Language)', definition: 'The standard language for querying and manipulating relational and warehouse data — every major analytical engine, from PostgreSQL to Spark SQL to BigQuery, speaks a dialect of it.' },
    { term: 'Star Schema', definition: 'A dimensional-modelling pattern with a central fact table surrounded by denormalised dimension tables — simple, fast to query, and the default shape for most analytical marts.' },
    { term: 'Stream Processing', definition: 'Continuously processing unbounded event streams as they arrive, typically with sub-second to seconds latency — the alternative to batch when freshness matters.' },
    { term: 'Surrogate Key', definition: 'A system-generated identifier — usually an integer or UUID — used as the primary key of a table instead of a business-meaningful natural key, insulating joins from source-system changes.' },
    { term: 'Transformation', definition: 'A step in a pipeline that reshapes data — cleaning nulls, casting types, joining, aggregating, deriving columns — to move raw inputs closer to the analytical shape consumers need.' },
    { term: 'Virtual Environment', definition: 'An isolated Python installation, typically created with venv or conda, that pins dependencies per project so pipeline jobs stay reproducible and free of system-wide conflicts.' },
  ],
  mid: [
    { term: 'Airflow', definition: 'The industry-standard open-source orchestrator — workflows are defined as Python DAGs of tasks with dependencies, schedules, retries, and a UI for monitoring runs and clearing failures.' },
    { term: 'Azure Data Factory (ADF)', definition: 'A managed cloud orchestration service with a visual pipeline designer, 100+ connectors, and Spark-backed data flows — Azure\'s equivalent of Airflow plus built-in data movement.' },
    { term: 'Backfill', definition: 'Running a pipeline retroactively over a historical date range — typically to populate a new model, recover from an outage, or reprocess after a bug fix — relying on idempotency for safety.' },
    { term: 'BigQuery', definition: 'Google Cloud\'s serverless, columnar analytical warehouse — charges per terabyte scanned by default, with separation of storage and compute and native support for nested and streaming data.' },
    { term: 'Bucketing', definition: 'A Hive- and Spark-style technique that hashes rows into a fixed number of files per partition so that join keys co-locate, reducing shuffle for repeated joins on the same column.' },
    { term: 'CDC (Change Data Capture)', definition: 'A pattern that captures inserts, updates, and deletes from a source database in near real time — usually by reading the transaction log — so downstream systems stay in sync without full snapshots.' },
    { term: 'Columnar Storage', definition: 'Storing data column by column rather than row by row — as in Parquet or ORC — so analytical scans compress tightly and read only the columns each query actually needs.' },
    { term: 'Consumer Group', definition: 'A set of Kafka consumers that cooperate to read a topic — each partition is assigned to exactly one consumer in the group, giving horizontal scaling with ordering preserved per partition.' },
    { term: 'DAG (Directed Acyclic Graph)', definition: 'The execution model used by Airflow and dbt — tasks or models are nodes, dependencies are edges, and the absence of cycles guarantees a valid topological execution order.' },
    { term: 'Data Freshness', definition: 'A measure of how recent the data in a downstream system is compared to its source — usually monitored via timestamps, arrival times, and explicit SLAs on ingestion pipelines.' },
    { term: 'Data Partitioning', definition: 'Dividing large datasets into smaller segments — by date, region, or tenant — so query engines can skip irrelevant data, reducing both scan cost and execution time.' },
    { term: 'Data Quality', definition: 'The degree to which data is accurate, complete, consistent, timely, and unique — enforced in modern pipelines through automated tests, expectations, and monitoring rather than after-the-fact audits.' },
    { term: 'Databricks', definition: 'A commercial lakehouse platform built around Apache Spark and Delta Lake that bundles managed clusters, notebooks, Unity Catalog, and MLflow on top of cloud object storage.' },
    { term: 'dbt (data build tool)', definition: 'A transformation framework that lets teams write modular SQL models inside the warehouse — with version control, tests, docs, and lineage — bringing software-engineering discipline to the T in ELT.' },
    { term: 'dbt Macro', definition: 'A reusable Jinja-templated SQL snippet in a dbt project that factors out repeated logic — generating select lists, audit columns, or dialect-specific syntax across many models.' },
    { term: 'dbt Seed', definition: 'A small CSV file checked into a dbt project that is loaded into the warehouse as a table — useful for static lookups such as country codes or mapping tables that change rarely.' },
    { term: 'dbt Test', definition: 'A declarative or SQL-based assertion run by dbt against a model — not_null, unique, relationships, accepted_values, or custom queries that must return zero rows to pass.' },
    { term: 'Debezium', definition: 'The leading open-source CDC platform — reads MySQL binlogs, PostgreSQL WAL, or CosmosDB change feeds and publishes change events to Kafka topics, one topic per table.' },
    { term: 'Dimensional Modelling', definition: 'A Kimball-pioneered approach to warehouse design that organises data into fact and dimension tables in a star or snowflake schema — the dominant pattern for analytical marts.' },
    { term: 'Docker', definition: 'A platform for packaging a pipeline and its dependencies into a portable container image that runs consistently across laptops, CI, and production clusters.' },
    { term: 'Grain', definition: 'The level of detail represented by a single row in a fact table — one row per order, per order line, per day per store — and the single most important decision when designing a dimensional model.' },
    { term: 'Great Expectations', definition: 'An open-source data-quality framework for declaring expectations about datasets — row counts, ranges, uniqueness, regex — and producing Data Docs that document what was checked and when.' },
    { term: 'Hive Metastore', definition: 'The original Hadoop-era catalogue service that stores table and partition metadata for SQL-on-files engines — still widely used as the backing catalogue for Spark and Presto/Trino clusters.' },
    { term: 'Incremental Model', definition: 'A dbt or Spark model configured to append or merge only new or changed rows on each run rather than rebuilding from scratch — essential when source tables outgrow full refreshes.' },
    { term: 'Kafka', definition: 'A distributed event-streaming platform that stores ordered, immutable events in partitioned topics on a durable commit log — the backbone of most real-time data architectures.' },
    { term: 'Kafka Connect', definition: 'A framework for streaming data into and out of Kafka using declarative source and sink connectors — avoiding custom producer or consumer code for common systems such as JDBC or S3.' },
    { term: 'Kinesis', definition: 'AWS\'s managed streaming service — similar to Kafka but serverless — with Kinesis Data Streams for ingestion and Kinesis Firehose for delivery to S3, Redshift, or OpenSearch.' },
    { term: 'Materialised View', definition: 'A view whose result set is physically stored and refreshed on a schedule or on write — trading storage and staleness for dramatically faster repeated analytical queries.' },
    { term: 'Medallion Architecture', definition: 'A lakehouse convention that layers data into Bronze (raw ingested), Silver (cleaned and conformed), and Gold (business-ready) zones — making lineage and quality guarantees visible in the layout.' },
    { term: 'ORC', definition: 'An optimised, columnar, self-describing file format popular in the Hadoop and Hive ecosystems — similar niche to Parquet with slightly different compression and indexing characteristics.' },
    { term: 'Redshift', definition: 'AWS\'s analytical data warehouse — originally a fork of ParAccel, now with RA3 nodes that separate storage from compute and tight integration with S3 via Spectrum.' },
    { term: 'Schema Evolution', definition: 'The practice of changing a dataset\'s schema over time — adding columns, widening types, renaming — under compatibility rules so existing producers and consumers continue to work.' },
    { term: 'Schema Registry', definition: 'A service — commonly Confluent Schema Registry — that stores Avro, Protobuf, or JSON schemas for Kafka topics and enforces compatibility checks before producers are allowed to publish.' },
    { term: 'Slowly Changing Dimension (SCD)', definition: 'A set of techniques for tracking changes in dimension attributes — Type 1 overwrites, Type 2 adds a versioned row with validity dates, Type 3 adds a previous-value column.' },
    { term: 'Snowflake', definition: 'A cloud-native warehouse with complete separation of storage, compute (virtual warehouses), and services layers — known for zero-copy cloning, time travel, and seamless semi-structured data support.' },
    { term: 'Window Function', definition: 'A SQL construct — ROW_NUMBER, RANK, LAG, SUM() OVER — that computes a value across a set of related rows without collapsing them, enabling ranking, running totals, and period comparisons.' },
  ],
  senior: [
    { term: 'Apache Flink', definition: 'A distributed stream-processing engine with true event-time semantics, rich state management, and exactly-once guarantees — preferred over Kafka Streams for complex, large-scale streaming jobs.' },
    { term: 'Apache Hudi', definition: 'An open lakehouse table format from Uber focused on efficient upserts and incremental pulls — particularly strong for CDC-driven workloads and mutable datasets on object storage.' },
    { term: 'Apache Iceberg', definition: 'An open table format with hidden partitioning, schema and partition evolution, and snapshot isolation — now natively supported by Snowflake, BigQuery, Athena, and Databricks UniForm.' },
    { term: 'Apache Pulsar', definition: 'A distributed messaging and streaming platform with a separated broker and BookKeeper storage layer, native multi-tenancy, and geo-replication — a Kafka alternative at very large scale.' },
    { term: 'Backpressure', definition: 'A flow-control mechanism where a slow consumer signals upstream producers or operators to slow down — preventing unbounded buffers and out-of-memory failures under load spikes.' },
    { term: 'Cost Optimisation', definition: 'The discipline of controlling cloud data-platform spend through partition pruning, auto-suspend, storage tiering, query caching, clustered tables, and per-query observability of scanned bytes and compute seconds.' },
    { term: 'Data Contract', definition: 'A formal, versioned agreement between a data producer and its consumers covering schema, semantics, SLAs, and quality expectations — enabling producers to evolve safely without breaking downstream systems.' },
    { term: 'Data Governance', definition: 'The policies, processes, and controls that ensure data is accurate, secure, compliant, and responsibly used — covering ownership, classification, access, retention, and lineage across the estate.' },
    { term: 'Data Lineage', definition: 'The ability to trace data from its origin through every transformation to its downstream consumers — essential for impact analysis, debugging, compliance, and deletion requests under GDPR.' },
    { term: 'Data Mesh', definition: 'A decentralised data architecture where domain teams own and publish their data as products on a self-serve platform, governed by federated computational policies — Zhamak Dehghani\'s response to central-team bottlenecks.' },
    { term: 'Delta Lake', definition: 'An open table format built on Parquet with an ACID transaction log, time travel, schema enforcement, and MERGE INTO upserts — the default lakehouse format in Databricks and Microsoft Fabric.' },
    { term: 'Exactly-Once Semantics', definition: 'A delivery guarantee that each event is reflected in downstream state exactly once despite retries and failures — achieved through idempotent producers, transactional writes, and atomic offset commits.' },
    { term: 'GDPR (General Data Protection Regulation)', definition: 'The EU regulation governing the collection, processing, storage, and deletion of personal data — directly shaping pipeline design through data minimisation, purpose limitation, retention, and the right to erasure.' },
    { term: 'Lakehouse', definition: 'An architecture that unifies the data lake and data warehouse by layering ACID transactions, schema enforcement, and governance — through Delta Lake, Iceberg, or Hudi — on top of open formats on object storage.' },
    { term: 'Late-Arriving Data', definition: 'Events that reach a stream processor after their event-time window has nominally closed — handled with watermarks, allowed-lateness configuration, and side outputs for downstream correction.' },
    { term: 'MDM (Master Data Management)', definition: 'The discipline of establishing a single authoritative record for core business entities — customers, products, suppliers — across heterogeneous systems, typically through matching, survivorship, and golden-record rules.' },
    { term: 'Metadata Management', definition: 'The capture, storage, and exposure of technical, operational, and business metadata — schemas, ownership, lineage, tags, quality metrics — through catalogues such as DataHub, Atlan, OpenMetadata, or Purview.' },
    { term: 'PII (Personally Identifiable Information)', definition: 'Any data that can identify a natural person directly or indirectly — names, emails, device IDs, IP addresses — subject to stricter access controls, masking, and retention rules under GDPR.' },
    { term: 'Pseudonymisation vs Anonymisation', definition: 'Pseudonymised data still permits re-identification with additional information and therefore remains personal data under GDPR, while true anonymisation makes re-identification practically impossible and removes GDPR scope.' },
    { term: 'Query Optimisation', definition: 'The engine-side process of turning a SQL query into an efficient physical plan — predicate pushdown, join reordering, partition pruning, cost-based statistics — and the engineer-side work of writing queries that cooperate with it.' },
    { term: 'RAG (Retrieval-Augmented Generation)', definition: 'An LLM architecture where responses are grounded in documents retrieved from a vector store — Data Engineers own the embedding pipeline, chunking strategy, index refresh, and evaluation of retrieval quality.' },
    { term: 'Stateful Streaming', definition: 'Stream processing that maintains keyed state — counters, windows, joins, sessionisation — across events, typically backed by a state store such as RocksDB and periodically checkpointed for recovery.' },
    { term: 'Streaming Join', definition: 'Joining two or more unbounded streams, or a stream against a slowly changing table, using windowed, interval, or temporal semantics — a notably harder problem than batch joins due to out-of-order arrival.' },
    { term: 'Watermark', definition: 'A timestamp heuristic in stream processing that declares "no events older than this are expected" — the mechanism that lets event-time windows close and aggregations emit results despite unbounded input.' },
    { term: 'Z-Ordering', definition: 'A multi-dimensional clustering technique in Delta Lake and Iceberg that co-locates related rows across several columns — dramatically improving data-skipping for selective queries beyond what simple partitioning allows.' },
  ],
}
