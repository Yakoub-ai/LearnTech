export default {
  beginner: [
    { term: 'Type Annotation', definition: 'Explicit syntax for declaring the type of a variable, parameter, or return value using a colon — e.g., let name: string = "Alice".' },
    { term: 'Interface', definition: 'A named contract that describes the shape of an object, specifying required and optional properties and their types. Interfaces can be extended and implemented by classes.' },
    { term: 'Type Alias', definition: 'A custom name for any type, declared with the type keyword — e.g., type ID = string | number. Unlike interfaces, type aliases can represent unions, tuples, and primitives.' },
    { term: 'Union Type (|)', definition: 'A type that allows a value to be one of several types, written with the pipe operator — e.g., string | number means the value can be either a string or a number.' },
    { term: 'Literal Type', definition: 'A type that represents an exact value rather than a broad category — e.g., type Direction = "north" | "south" restricts the value to those specific strings.' },
    { term: 'Enum', definition: 'A set of named constants. Numeric enums auto-increment from 0 by default; string enums require explicit values. Use as const objects as a lighter alternative.' },
    { term: 'Array Type', definition: 'Typed arrays are written as type[] or Array<type> — e.g., number[] ensures every element is a number. TypeScript enforces element types at compile time.' },
    { term: 'Tuple', definition: 'A fixed-length array where each position has a specific type — e.g., [string, number] requires exactly a string first and a number second.' },
    { term: 'any / unknown', definition: 'any disables type checking entirely, while unknown is a type-safe alternative that requires narrowing before use. Prefer unknown over any for safety.' },
    { term: 'void / never', definition: 'void indicates a function returns no meaningful value. never indicates a function never returns at all — e.g., it always throws or contains an infinite loop.' },
    { term: 'Type Inference', definition: 'TypeScript\'s ability to automatically determine types from context without explicit annotations — e.g., let x = 5 is inferred as number.' },
    { term: 'tsconfig.json', definition: 'The configuration file for a TypeScript project, specifying compiler options (target, module, strict), file includes/excludes, and project structure.' },
  ],
  mid: [
    { term: 'Generics', definition: 'Type parameters that let functions, classes, and interfaces work with multiple types while preserving type safety — e.g., function identity<T>(arg: T): T returns the same type it receives.' },
    { term: 'Utility Types (Partial, Required, Pick, Omit, Record)', definition: 'Built-in generic types that transform existing types. Partial makes all properties optional, Required makes them mandatory, Pick/Omit select or exclude properties, and Record creates key-value mapped types.' },
    { term: 'Intersection Type (&)', definition: 'Combines multiple types into one that has all members of each — e.g., A & B requires the value to satisfy both type A and type B simultaneously.' },
    { term: 'Type Narrowing / Type Guards', definition: 'Techniques that refine a broad type to a more specific one within a code block — using typeof, instanceof, in operator, or custom type predicate functions (param is Type).' },
    { term: 'Discriminated Unions', definition: 'A pattern where each member of a union has a common literal property (the discriminant) that TypeScript uses for exhaustive narrowing in switch/if statements.' },
    { term: 'Mapped Types', definition: 'Types that transform each property of an existing type using the [K in keyof T] syntax — e.g., { [K in keyof T]: boolean } converts all property types to boolean.' },
    { term: 'Conditional Types', definition: 'Types that select one of two outcomes based on a condition — e.g., T extends string ? "yes" : "no". The foundation for advanced type-level logic.' },
    { term: 'Module Augmentation', definition: 'A technique for extending existing module declarations — e.g., adding properties to a third-party library\'s types by re-declaring its module with additional members.' },
    { term: 'Declaration Files (.d.ts)', definition: 'Files containing only type information (no runtime code) that describe the shape of JavaScript libraries, enabling TypeScript to type-check code that uses them.' },
    { term: 'Strict Mode', definition: 'A set of tsconfig compiler flags (strictNullChecks, noImplicitAny, strictFunctionTypes, etc.) that enable the most thorough type checking. Enabled together via "strict": true.' },
    { term: 'Index Signatures', definition: 'A syntax for typing objects with dynamic keys — e.g., { [key: string]: number } allows any string key with a number value. Can be combined with known properties.' },
    { term: 'Template Literal Types', definition: 'Types that use template literal syntax to construct string types from other types — e.g., type EventName = `on${Capitalize<string>}` matches "onClick", "onHover", etc.' },
  ],
  senior: [
    { term: 'Advanced Generics (Constraints & Variance)', definition: 'Using extends to constrain generic parameters — e.g., <T extends { id: number }> — and understanding how generic type parameters behave covariantly or contravariantly depending on position.' },
    { term: 'infer Keyword', definition: 'Used inside conditional types to extract and capture a type from a pattern — e.g., T extends Promise<infer U> ? U : T unwraps the resolved type of a Promise.' },
    { term: 'Recursive Types', definition: 'Types that reference themselves in their definition, enabling representation of deeply nested structures — e.g., type JSON = string | number | boolean | null | JSON[] | { [key: string]: JSON }.' },
    { term: 'Branded / Nominal Types', definition: 'A pattern that simulates nominal typing in TypeScript\'s structural type system by adding a unique phantom property — e.g., type USD = number & { __brand: "USD" } — to prevent accidental interchangeability.' },
    { term: 'Type-Level Programming', definition: 'Writing complex logic purely in the type system using conditional types, mapped types, recursive types, and infer to compute types at compile time — effectively treating types as a functional programming language.' },
    { term: 'Declaration Merging', definition: 'TypeScript\'s ability to combine multiple declarations with the same name into a single definition — interfaces merge their members, namespaces merge with classes/functions/enums.' },
    { term: 'Module Resolution Strategies', definition: 'The algorithms TypeScript uses to locate imported modules: "node" mimics Node.js resolution, "bundler" aligns with modern bundlers, and "nodenext" supports ESM/CJS interop with package.json exports.' },
    { term: 'Performance Optimization (Project References, Incremental Builds)', definition: 'Techniques for scaling TypeScript in large codebases: project references split a monorepo into independently compilable units, and incremental builds (tsBuildInfo) skip recompiling unchanged files.' },
    { term: 'Covariance / Contravariance', definition: 'Variance describes how subtyping of generic types relates to subtyping of their type parameters. TypeScript uses in/out modifiers (since 4.7) to explicitly annotate variance on generic type parameters.' },
    { term: 'satisfies Operator', definition: 'Validates that an expression matches a type without widening it — e.g., const palette = { red: [255, 0, 0] } satisfies Record<string, number[]> preserves the literal type while checking the constraint.' },
    { term: 'const Assertions', definition: 'The as const suffix that narrows a value to its most specific literal type, making arrays readonly tuples and objects deeply readonly with literal property types.' },
    { term: 'TypeScript Compiler API', definition: 'The programmatic interface to the TypeScript compiler (ts.createProgram, AST traversal, type checker) used to build custom linters, codemods, and code generation tools.' },
  ],
}
