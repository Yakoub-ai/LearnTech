export default {
  beginner: [
    { term: 'Accessibility (a11y)', definition: 'The practice of designing and building web content so it is usable by everyone, including people relying on screen readers, keyboard navigation, or other assistive technologies.' },
    { term: 'Ajax (Asynchronous JavaScript and XML)', definition: 'A technique for sending and receiving data from a server without reloading the page — originally XMLHttpRequest, now usually the fetch API.' },
    { term: 'Alt Text', definition: 'The text alternative on an <img> element that describes the image to screen readers and appears when the image fails to load.' },
    { term: 'ARIA (Accessible Rich Internet Applications)', definition: 'A set of attributes (role, aria-label, aria-expanded) that communicate the purpose and state of custom UI to assistive technologies when semantic HTML alone is insufficient.' },
    { term: 'Arrow Function', definition: 'A concise JavaScript function syntax (x => x * 2) that does not bind its own this, arguments, or prototype — useful for callbacks and methods inside components.' },
    { term: 'Box Model', definition: 'The CSS rule that every element is a rectangular box made up of content, padding, border, and margin — with box-sizing controlling whether width includes padding and border.' },
    { term: 'Browser DevTools', definition: 'Built-in debugging tools in browsers like Chrome and Firefox for inspecting the DOM, editing CSS live, profiling performance, and debugging JavaScript.' },
    { term: 'Closure', definition: 'A JavaScript function that remembers variables from the scope in which it was created, even after that outer scope has returned — the foundation of many hook and callback patterns.' },
    { term: 'Cookie', definition: 'A small piece of data stored by the browser and sent back to the server with each request — used for sessions, preferences, and tracking.' },
    { term: 'CORS (Cross-Origin Resource Sharing)', definition: 'A browser security mechanism that controls whether a page can make requests to a different origin, enforced via response headers like Access-Control-Allow-Origin.' },
    { term: 'CSS (Cascading Style Sheets)', definition: 'The stylesheet language that controls the visual presentation of HTML — layout, colours, typography, spacing, and responsive behaviour.' },
    { term: 'CSS Selector', definition: 'A pattern that targets HTML elements for styling — by element (p), class (.card), id (#main), attribute ([type=text]), or combinators (ul > li).' },
    { term: 'Debounce', definition: 'A technique that delays a function call until a set time has passed since the last invocation — useful for search inputs and resize handlers to avoid excessive work.' },
    { term: 'DOM (Document Object Model)', definition: 'A tree-like representation of an HTML document that JavaScript can read and manipulate to update page content, structure, and styles at runtime.' },
    { term: 'ES6+ (ECMAScript 2015 and later)', definition: 'The modern JavaScript language versions that introduced let/const, classes, modules, arrow functions, destructuring, template literals, and promises.' },
    { term: 'Event Bubbling', definition: 'The default DOM event flow where an event fires on the target element and then propagates up through its ancestors, allowing parent handlers to respond.' },
    { term: 'Event Delegation', definition: 'A pattern of attaching a single listener to a parent element and using event.target to handle events from many children — efficient for long lists and dynamic content.' },
    { term: 'Event Loop', definition: 'The runtime mechanism that lets single-threaded JavaScript handle asynchronous work by pulling callbacks from task and microtask queues when the call stack is empty.' },
    { term: 'fetch', definition: 'The modern browser API for making HTTP requests, returning a promise that resolves to a Response object — the successor to XMLHttpRequest.' },
    { term: 'Flexbox', definition: 'A one-dimensional CSS layout module for arranging items in a row or column with flexible sizing, alignment, wrapping, and distribution of space.' },
    { term: 'Hoisting', definition: 'The JavaScript behaviour where variable and function declarations are conceptually moved to the top of their scope — var is hoisted and initialised as undefined, let and const are not accessible until their declaration.' },
    { term: 'HTML (HyperText Markup Language)', definition: 'The markup language for structuring web content using elements such as headings, paragraphs, lists, links, images, and forms.' },
    { term: 'JavaScript', definition: 'The programming language of the web, enabling interactive behaviour in browsers — DOM manipulation, event handling, and asynchronous data fetching.' },
    { term: 'JSON (JavaScript Object Notation)', definition: 'A lightweight, text-based data format using key-value pairs and arrays — the standard payload format for REST APIs and fetch responses.' },
    { term: 'Keyboard Navigation', definition: 'The ability to operate a UI without a pointing device, using Tab, Shift+Tab, Enter, Space, and arrow keys — a baseline requirement for accessibility.' },
    { term: 'let and const', definition: 'Block-scoped JavaScript variable declarations introduced in ES6 — const cannot be reassigned, let can. Both replace var in modern code.' },
    { term: 'Local Storage', definition: 'A browser key-value store (up to around 5MB) that persists across tabs and sessions until explicitly cleared, accessed via localStorage.getItem and setItem.' },
    { term: 'Media Query', definition: 'A CSS feature (@media) that applies rules based on viewport characteristics such as width, orientation, or prefers-colour-scheme — the foundation of responsive design.' },
    { term: 'Mobile-First', definition: 'A design approach where base styles target small screens and larger layouts are layered on with min-width media queries, producing leaner CSS and better defaults.' },
    { term: 'Package Manager (npm, pnpm, Yarn)', definition: 'A tool for installing, versioning, and managing JavaScript dependencies via package.json and a lockfile — pnpm and Yarn are faster alternatives to npm with deduplicated stores.' },
    { term: 'Promise', definition: 'A JavaScript object representing the eventual completion (or failure) of an asynchronous operation, handled with then/catch or async/await.' },
    { term: 'Prototype', definition: 'The mechanism by which JavaScript objects inherit properties and methods — every object has an internal prototype link that is walked during property lookup.' },
    { term: 'Responsive Design', definition: 'A design approach where layouts adapt fluidly to different screen sizes using flexible grids, media queries, and scalable images.' },
    { term: 'Screen Reader', definition: 'Assistive software (VoiceOver, NVDA, JAWS) that reads page content aloud and exposes structure, labels, and ARIA states to blind and low-vision users.' },
    { term: 'Semantic HTML', definition: 'Using HTML elements that convey meaning — <nav>, <main>, <article>, <button> — rather than generic <div> and <span> wrappers, improving accessibility and SEO.' },
    { term: 'Session Storage', definition: 'A browser key-value store scoped to a single tab that is cleared when the tab closes — useful for ephemeral UI state.' },
    { term: 'this', definition: 'A JavaScript keyword whose value depends on how a function is called — it can refer to the calling object, the global object, undefined (in strict mode), or a bound value.' },
    { term: 'Throttle', definition: 'A technique that limits how often a function can run within a time window — useful for scroll and mousemove handlers where continuous firing would overwhelm the browser.' },
    { term: 'Version Control (Git)', definition: 'A system that tracks changes to files over time and supports collaboration through branches, commits, merges, and pull requests.' },
    { term: 'Viewport', definition: 'The visible area of a web page in the browser window — controlled on mobile with the <meta name="viewport"> tag to ensure correct scaling.' },
    { term: 'XMLHttpRequest (XHR)', definition: 'The original browser API for asynchronous HTTP requests — still supported but largely superseded by fetch in modern code.' },
  ],
  mid: [
    { term: 'Bundler', definition: 'A tool (Vite, Webpack, esbuild, Rollup) that combines JavaScript modules, CSS, and assets into optimised files for production delivery.' },
    { term: 'Code Splitting', definition: 'Dividing application code into smaller chunks that load on demand, reducing the initial JavaScript payload and improving time-to-interactive.' },
    { term: 'Component', definition: 'A self-contained, reusable piece of UI that encapsulates its own markup, styles, and logic — the core building block of modern frontend frameworks.' },
    { term: 'Controlled Input', definition: 'A form input whose value is driven by component state — every keystroke updates state and re-renders with the new value, giving full control over input behaviour.' },
    { term: 'CSS Custom Properties', definition: 'Author-defined CSS variables declared with --name and read with var(--name), enabling runtime theming and shared design tokens.' },
    { term: 'CSS Grid', definition: 'A two-dimensional CSS layout system for arranging content in rows and columns, with named grid areas and powerful alignment controls.' },
    { term: 'CSS Modules', definition: 'A convention where CSS files are scoped per-component by automatically generating unique class names at build time, preventing global style collisions.' },
    { term: 'CSS-in-JS', definition: 'An approach (styled-components, Emotion, vanilla-extract) where styles are authored in JavaScript and scoped per component, often with dynamic prop-driven theming.' },
    { term: 'Custom Hook', definition: 'A reusable JavaScript function prefixed with use that calls other React hooks — the idiomatic way to share stateful logic between components.' },
    { term: 'Dynamic Import', definition: 'The import() expression that loads a module asynchronously at runtime, returning a promise — the primitive behind route-level code splitting and lazy loading.' },
    { term: 'Emotion', definition: 'A performant CSS-in-JS library that supports both styled-components-style and css prop APIs, with first-class theming and server-side rendering.' },
    { term: 'esbuild', definition: 'An extremely fast JavaScript and TypeScript bundler written in Go, used directly or as the transform engine inside Vite and other modern tools.' },
    { term: 'Form Validation', definition: 'Checking user input against rules (required, pattern, length, custom) before submission — handled client-side for UX and always re-checked on the server.' },
    { term: 'Generic (TypeScript)', definition: 'A type parameter (e.g. Array<T>, Promise<T>) that lets functions and types work with multiple input types while preserving type information.' },
    { term: 'GraphQL', definition: 'A query language and runtime for APIs where the client specifies exactly the fields it needs in a single request — typically consumed from React via Apollo Client or urql.' },
    { term: 'Hook', definition: 'A React primitive (useState, useEffect, useMemo) that lets function components manage state and side effects without class syntax.' },
    { term: 'Jotai', definition: 'An atomic state management library for React where state is composed from small, independently subscribed atoms, avoiding re-render cascades.' },
    { term: 'JSX (JavaScript XML)', definition: 'A syntax extension that lets you write HTML-like markup inside JavaScript — compiled by Babel or SWC into React.createElement (or the runtime jsx) calls.' },
    { term: 'Lazy Loading', definition: 'Deferring the load of resources — images, routes, components — until they are needed, commonly via React.lazy, IntersectionObserver, or loading="lazy".' },
    { term: 'Next.js', definition: 'A React metaframework offering file-based routing, SSR, SSG, ISR, API routes, and the App Router with React Server Components.' },
    { term: 'PostCSS', definition: 'A tool for transforming CSS with JavaScript plugins — used for autoprefixing, nesting, custom property polyfills, and the Tailwind pipeline.' },
    { term: 'Props', definition: 'The read-only inputs passed from a parent component to a child, used to configure appearance and behaviour without coupling to parent internals.' },
    { term: 'React', definition: 'A JavaScript library for building UIs through composable components and a declarative render model, backed by a virtual DOM and reconciler.' },
    { term: 'React Hook Form', definition: 'A performant React form library that uses uncontrolled inputs and refs to minimise re-renders while providing validation and error handling APIs.' },
    { term: 'React Router', definition: 'The de facto client-side routing library for React SPAs, mapping URL paths to components with nested routes, loaders, and actions.' },
    { term: 'React Testing Library', definition: 'A testing utility that encourages asserting on rendered output as a user would — querying by role, label, and text rather than implementation details.' },
    { term: 'Reconciliation', definition: 'The React algorithm that diffs the new virtual DOM tree against the previous one and applies the minimal set of DOM mutations.' },
    { term: 'Redux', definition: 'A predictable state container where state lives in a single store and is updated by pure reducers in response to dispatched actions — Redux Toolkit is the modern API.' },
    { term: 'Rollup', definition: 'A JavaScript bundler focused on producing small, tree-shaken ES module output — widely used for publishing libraries and under the hood in Vite production builds.' },
    { term: 'Routing', definition: 'The mechanism that maps URL paths to views or components in a single-page application, typically with history API integration and nested layouts.' },
    { term: 'SPA (Single-Page Application)', definition: 'A web app that loads one HTML shell and updates the view via client-side JavaScript and the history API, avoiding full page reloads.' },
    { term: 'State', definition: 'Data that changes over time within a component or application, driving re-renders when updated through hooks, stores, or signals.' },
    { term: 'styled-components', definition: 'A CSS-in-JS library that uses tagged template literals to attach styles to React components, with prop-driven styling and full theme support.' },
    { term: 'SWR', definition: 'A lightweight React data fetching library from Vercel that implements the stale-while-revalidate pattern, giving instant cached responses while refreshing in the background.' },
    { term: 'Tailwind CSS', definition: 'A utility-first CSS framework where layouts are composed from small, single-purpose classes — with a JIT engine that generates only the styles you use.' },
    { term: 'Tanstack Query', definition: 'A server-state library (formerly React Query) that handles fetching, caching, background refetching, and mutation lifecycle for remote data in React, Solid, Svelte, and Vue.' },
    { term: 'Tree-Shaking', definition: 'A bundler optimisation that eliminates unused exports from the final bundle by statically analysing ES module imports.' },
    { term: 'Type Inference', definition: 'TypeScripts ability to deduce types from context — initial values, return statements, generic arguments — without explicit annotations.' },
    { term: 'TypeScript', definition: 'A typed superset of JavaScript that adds static type checking at compile time, catching bugs early and improving editor tooling and documentation.' },
    { term: 'Uncontrolled Input', definition: 'A form input that stores its own value in the DOM, read via a ref on submit — simpler than controlled inputs when you do not need per-keystroke state.' },
    { term: 'useCallback', definition: 'A React hook that memoises a function identity across renders so long as dependencies are unchanged — used to stabilise props for memoised children.' },
    { term: 'useContext', definition: 'A React hook that reads the current value of a Context object, enabling components deep in the tree to access shared values without prop drilling.' },
    { term: 'useEffect', definition: 'A React hook that runs side effects (data fetching, subscriptions, DOM mutations) after render and cleans them up when dependencies change or the component unmounts.' },
    { term: 'useMemo', definition: 'A React hook that caches an expensive computed value between renders while its dependencies are unchanged — a targeted optimisation, not a default.' },
    { term: 'useRef', definition: 'A React hook that returns a mutable ref object whose .current persists across renders — used for DOM references and values that must not trigger re-renders.' },
    { term: 'useState', definition: 'The foundational React hook that declares a piece of local component state and returns the current value along with an updater function.' },
    { term: 'Utility Type', definition: 'A built-in TypeScript type (Partial, Pick, Omit, Record, ReturnType) that transforms existing types to reduce boilerplate.' },
    { term: 'Virtual DOM', definition: 'An in-memory representation of the UI that React diffs against the previous snapshot to compute minimal DOM updates.' },
    { term: 'Vite', definition: 'A modern frontend build tool that serves source files over native ES modules in development for near-instant startup, and bundles with Rollup for production.' },
    { term: 'Webpack', definition: 'A mature, highly configurable module bundler with a vast loader and plugin ecosystem — still common in large apps, though newer projects often prefer Vite or Turbopack.' },
    { term: 'XState', definition: 'A library for modelling complex UI behaviour as finite state machines and statecharts, making impossible states impossible and flows visualisable.' },
    { term: 'Zod', definition: 'A TypeScript-first schema validation library that validates runtime data (form inputs, API responses) and infers static types from the same schema.' },
    { term: 'Zustand', definition: 'A minimal React state management library that exposes a hook-based store with no provider, selectors, or boilerplate reducers.' },
  ],
  senior: [
    { term: 'Bundle Analyser', definition: 'A tool (webpack-bundle-analyzer, rollup-plugin-visualizer, source-map-explorer) that visualises module sizes in a bundle, revealing heavy dependencies and duplication.' },
    { term: 'CLS (Cumulative Layout Shift)', definition: 'A Core Web Vital measuring unexpected layout movement during page load — kept under 0.1 by reserving space for images, ads, and late-loading content.' },
    { term: 'Component Library', definition: 'A published package of reusable UI components (buttons, inputs, modals) with consistent API, theming, and accessibility guarantees — the implementation layer of a design system.' },
    { term: 'Core Web Vitals (CWV)', definition: 'Googles headline user-experience metrics — LCP, INP, and CLS — used both as search ranking signals and as product performance KPIs.' },
    { term: 'Custom Element', definition: 'A user-defined HTML tag registered via customElements.define, extending the browser with reusable, framework-agnostic components.' },
    { term: 'Cypress', definition: 'An end-to-end testing framework that runs inside the browser with time-travel debugging, automatic waiting, and a rich UI — strong for complex user journeys.' },
    { term: 'Design System', definition: 'A coordinated set of design tokens, components, patterns, and documentation that drives consistency across products — typically owned by a platform or design team.' },
    { term: 'Design Tokens', definition: 'Named, platform-agnostic design decisions (colour.primary.500, space.md, radius.sm) stored as data and transformed into CSS variables, Tailwind config, iOS, and Android outputs.' },
    { term: 'Figma', definition: 'A collaborative design tool whose variables, components, and auto-layout map directly to frontend primitives — often the source of truth for design tokens and handoff.' },
    { term: 'Hydration', definition: 'Attaching JavaScript event handlers and state to server-rendered HTML on the client so static markup becomes interactive — variants include progressive, selective, and resumable.' },
    { term: 'INP (Interaction to Next Paint)', definition: 'A Core Web Vital replacing FID that measures the latency of user interactions across the entire page lifetime — good scores are under 200ms.' },
    { term: 'ISR (Incremental Static Regeneration)', definition: 'A rendering model that serves cached static pages and transparently regenerates them in the background on a time or on-demand trigger — combining SSG speed with fresh data.' },
    { term: 'LCP (Largest Contentful Paint)', definition: 'A Core Web Vital measuring when the largest visible element (hero image, headline) renders — kept under 2.5s by optimising critical resources and server response time.' },
    { term: 'Lighthouse', definition: 'An automated auditing tool from Google that scores pages on performance, accessibility, best practices, SEO, and PWA readiness — available in DevTools, CLI, and CI.' },
    { term: 'Micro-Frontend', definition: 'An architecture where a frontend is decomposed into smaller, independently deployable apps owned by different teams, composed at build, server, or runtime.' },
    { term: 'Module Federation', definition: 'A Webpack and Rspack feature that lets applications load JavaScript modules from other independently deployed builds at runtime — a common micro-frontend primitive.' },
    { term: 'Performance Budget', definition: 'An explicit limit on a performance metric or resource (JS KB, LCP seconds, image weight) that is enforced in CI to prevent regressions.' },
    { term: 'Playwright', definition: 'A cross-browser end-to-end testing framework with auto-waiting, tracing, parallel execution, and built-in accessibility and visual testing support.' },
    { term: 'PWA (Progressive Web App)', definition: 'A web app that uses a service worker, manifest, and modern platform APIs to deliver app-like experiences — installable, offline-capable, and push-enabled.' },
    { term: 'Service Worker', definition: 'A script that runs in a separate browser thread and intercepts network requests, enabling offline support, background sync, and push notifications.' },
    { term: 'Shadow DOM', definition: 'A browser primitive that encapsulates a subtree of DOM and CSS inside a host element, providing style and markup isolation for Web Components.' },
    { term: 'SSG (Static Site Generation)', definition: 'Pre-rendering pages to static HTML at build time, combining the speed and cacheability of static hosting with dynamic data sources.' },
    { term: 'SSR (Server-Side Rendering)', definition: 'Rendering pages to HTML on the server per request before sending to the client, improving first paint, SEO, and behaviour on slow devices.' },
    { term: 'Storybook', definition: 'A workshop environment for building, documenting, and visually testing components in isolation — widely used as the living documentation for design systems.' },
    { term: 'Streaming SSR', definition: 'A rendering technique where HTML is flushed to the client in chunks as it is produced, with Suspense boundaries allowing slower parts to arrive later.' },
    { term: 'TTFB (Time to First Byte)', definition: 'The latency between a navigation request and the first byte of response — the foundation of LCP, driven by server processing, edge location, and cache hits.' },
    { term: 'Virtual Regression Testing', definition: 'Capturing screenshots of components or pages and comparing them pixel-by-pixel against baselines — tools include Chromatic, Percy, and Playwrights toHaveScreenshot.' },
    { term: 'Vitest', definition: 'A Vite-native unit testing framework with a Jest-compatible API, ESM-first design, native TypeScript, and significantly faster startup than Jest.' },
    { term: 'WCAG (Web Content Accessibility Guidelines)', definition: 'The W3C standard defining accessibility success criteria across Perceivable, Operable, Understandable, and Robust principles — Level AA is the common legal and organisational target.' },
    { term: 'Web Component', definition: 'A suite of browser standards — Custom Elements, Shadow DOM, HTML Templates — for building reusable, framework-agnostic UI elements.' },
    { term: 'Web Worker', definition: 'A browser API that runs JavaScript in a background thread, enabling CPU-intensive work without blocking the main UI thread.' },
    { term: 'WebAssembly (Wasm)', definition: 'A binary instruction format that runs at near-native speed in the browser — used to bring CPU-intensive code (codecs, games, simulation) written in Rust, C, or C++ to the web.' },
  ],
}
