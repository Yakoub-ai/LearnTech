export default {
  beginner: [
    { term: 'HTML Element (Tag, Attribute, Content)', definition: 'The building block of a web page: an opening tag (<p>), optional attributes (class="intro"), content (text or nested elements), and a closing tag (</p>). Void elements like <img> and <br> have no closing tag.' },
    { term: 'Semantic HTML (<header>, <nav>, <main>, <article>, <footer>)', definition: 'HTML elements that convey meaning about the structure of a page rather than just appearance. They improve accessibility, SEO, and code readability over generic <div> elements.' },
    { term: 'Heading Hierarchy (h1-h6)', definition: 'Six levels of heading elements that define a document\'s outline. Use one <h1> per page for the main title, and nest subsequent headings (<h2>-<h6>) in order without skipping levels.' },
    { term: 'Anchor Tag (<a>) and Hyperlinks', definition: 'The <a> element creates clickable links using the href attribute. Links can point to other pages, sections within a page (via #id), email addresses (mailto:), or phone numbers (tel:).' },
    { term: 'Image (<img>) and Alt Text', definition: 'The <img> element embeds images using the src attribute. The alt attribute provides a text description for screen readers and displays when the image fails to load — required for accessibility.' },
    { term: 'Form Elements (<input>, <select>, <textarea>)', definition: 'Interactive elements for collecting user data. <input> supports many types (text, email, password, checkbox), <select> creates dropdowns, and <textarea> allows multi-line text input.' },
    { term: 'CSS Selector (Element, Class, ID)', definition: 'Patterns that target HTML elements for styling. Element selectors (p) target tag names, class selectors (.name) target class attributes, and ID selectors (#name) target unique IDs.' },
    { term: 'Box Model (Content, Padding, Border, Margin)', definition: 'Every element is a rectangular box: content is the inner area, padding is space between content and border, border is the visible edge, and margin is space outside the border. Use box-sizing: border-box to include padding and border in width.' },
    { term: 'CSS Specificity', definition: 'The algorithm that determines which CSS rule wins when multiple rules target the same element. Inline styles > IDs > classes/attributes/pseudo-classes > elements. Equal specificity falls back to source order.' },
    { term: 'Display Property (block, inline, inline-block, none)', definition: 'Controls how an element participates in layout. block takes full width; inline flows with text (no width/height); inline-block flows with text but accepts dimensions; none removes it from layout.' },
    { term: 'Color Values (hex, rgb, hsl)', definition: 'Ways to specify colors in CSS: hex codes (#ff0000), rgb/rgba functions (rgb(255, 0, 0)), and hsl/hsla functions (hsl(0, 100%, 50%)). Modern CSS also supports the color() function and named colors.' },
    { term: 'Units (px, em, rem, %, vw/vh)', definition: 'px is an absolute unit; em is relative to the parent font size; rem is relative to the root font size; % is relative to the parent dimension; vw/vh are 1% of the viewport width/height.' },
  ],
  mid: [
    { term: 'Flexbox (display: flex, justify-content, align-items, flex-wrap, gap)', definition: 'A one-dimensional layout system for distributing space along a row or column. justify-content aligns items along the main axis, align-items along the cross axis, and gap sets spacing between items.' },
    { term: 'CSS Grid (grid-template, fr unit, grid-area, auto-fill/auto-fit)', definition: 'A two-dimensional layout system using rows and columns. The fr unit distributes available space fractionally. auto-fill/auto-fit enable responsive grids without media queries.' },
    { term: 'Media Queries (@media)', definition: 'Conditional rules that apply styles based on device characteristics — e.g., @media (max-width: 768px) targets narrow viewports. The foundation of responsive design.' },
    { term: 'CSS Custom Properties (Variables)', definition: 'Reusable values defined with the -- prefix and accessed via var() — e.g., --color-primary: #3b82f6. They cascade, can be scoped, and can be changed at runtime with JavaScript.' },
    { term: 'Pseudo-Classes (:hover, :focus, :nth-child)', definition: 'Selectors that target elements based on their state or position. :hover for mouse-over, :focus for keyboard/input focus, :nth-child(n) for position-based selection within a parent.' },
    { term: 'Pseudo-Elements (::before, ::after)', definition: 'Virtual elements inserted before or after an element\'s content via CSS. They require a content property and are commonly used for decorative elements, icons, and clearfixes.' },
    { term: 'Transitions and Animations (@keyframes)', definition: 'transition smoothly interpolates property changes on state changes (e.g., hover). @keyframes defines multi-step animations applied with the animation property for more complex motion.' },
    { term: 'Position Property (relative, absolute, fixed, sticky)', definition: 'Controls how an element is positioned: relative offsets from its normal position, absolute positions relative to the nearest positioned ancestor, fixed relative to the viewport, and sticky toggles between relative and fixed on scroll.' },
    { term: 'Responsive Images (<picture>, srcset)', definition: 'Techniques for serving appropriately sized images: srcset lets the browser choose based on viewport width or pixel density, and <picture> allows art direction with different sources for different conditions.' },
    { term: 'BEM Naming Convention', definition: 'A CSS methodology that structures class names as Block__Element--Modifier — e.g., card__title--highlighted — to create predictable, maintainable, and self-documenting stylesheets.' },
    { term: 'CSS Preprocessors (Sass/SCSS)', definition: 'Tools that extend CSS with variables, nesting, mixins, and functions, compiling to standard CSS. SCSS is the most popular syntax, though native CSS now supports many of these features.' },
    { term: 'z-index and Stacking Context', definition: 'z-index controls overlap order of positioned elements, but only within the same stacking context. New stacking contexts are created by position + z-index, opacity < 1, transforms, and other properties.' },
  ],
  senior: [
    { term: 'CSS Container Queries (@container)', definition: 'Allow styling elements based on the size of their container rather than the viewport, enabling truly reusable components that adapt to their available space regardless of where they are placed.' },
    { term: 'CSS Cascade Layers (@layer)', definition: 'A mechanism to organize CSS into explicit layers with defined priority — e.g., @layer base, components, utilities — giving authors control over the cascade order independent of specificity or source order.' },
    { term: 'CSS Nesting', definition: 'Native CSS support for nesting selectors inside parent rules, similar to Sass — e.g., .card { & .title { color: blue } } — reducing repetition and improving readability without a preprocessor.' },
    { term: ':has() Relational Pseudo-Class', definition: 'A selector that targets an element based on its descendants or siblings — e.g., .card:has(img) styles cards that contain images. Often called the "parent selector" as it enables previously impossible upward selections.' },
    { term: 'CSS Logical Properties (inline/block vs left/right)', definition: 'Properties like margin-inline-start, padding-block-end, and inline-size that adapt to writing direction and text flow, replacing physical properties (left, right, top, bottom) for internationalized layouts.' },
    { term: 'Subgrid', definition: 'A CSS Grid feature where a child grid can adopt the track sizing of its parent grid using grid-template-rows: subgrid or grid-template-columns: subgrid, enabling alignment across nested grids.' },
    { term: 'Scroll-Driven Animations', definition: 'CSS animations tied to scroll progress using animation-timeline: scroll() or view(), allowing effects like parallax, progress indicators, and reveal-on-scroll without JavaScript.' },
    { term: 'View Transitions API', definition: 'A browser API that enables smooth animated transitions between DOM states or page navigations by automatically cross-fading old and new snapshots, with CSS pseudo-elements for customizing the animation.' },
    { term: 'content-visibility for Performance', definition: 'A CSS property that skips rendering of off-screen elements (content-visibility: auto), dramatically improving initial page render time for long pages by only painting content as it nears the viewport.' },
    { term: 'CSS color-mix() and Relative Color Syntax', definition: 'Modern color functions: color-mix() blends two colors in a specified color space, and relative color syntax (e.g., oklch(from var(--base) l calc(c * 1.2) h)) derives new colors by modifying components of existing ones.' },
    { term: 'Accessibility (ARIA Roles, Landmarks, Live Regions)', definition: 'ARIA attributes supplement HTML semantics for assistive technology: roles (role="dialog") define purpose, landmark roles structure the page, and live regions (aria-live) announce dynamic content changes to screen readers.' },
    { term: 'Progressive Enhancement and Graceful Degradation', definition: 'Progressive enhancement builds a baseline experience that works everywhere, then layers on advanced features for capable browsers. Graceful degradation starts with the full experience and provides fallbacks. Use @supports to feature-detect CSS.' },
  ],
}
