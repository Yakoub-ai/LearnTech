export default {
  beginner: [
    { term: 'AI (Artificial Intelligence)', definition: 'Any technique that enables a machine to perform tasks that normally require human intelligence — including rule-based systems, classical ML, deep learning, and generative models.' },
    { term: 'Anthropic API', definition: 'The HTTP interface for calling Anthropic Claude models, accessed via the anthropic Python or TypeScript SDK using an API key and a messages endpoint.' },
    { term: 'API Key', definition: 'A secret credential that authenticates requests to an LLM provider. Stored in environment variables or a secrets manager — never hardcoded or committed to source control.' },
    { term: 'Assistant Message', definition: 'In a chat-style LLM API, the message role representing the model\'s response. Historical assistant messages are replayed with each request so the model sees its prior turns.' },
    { term: 'Autoregressive Generation', definition: 'The process by which an LLM produces output one token at a time, feeding each generated token back as input to predict the next until a stop condition is met.' },
    { term: 'Chat Completion', definition: 'The standard LLM API endpoint that takes a list of system, user, and assistant messages and returns a generated assistant response — e.g. OpenAI /v1/chat/completions, Anthropic /v1/messages.' },
    { term: 'Chatbot', definition: 'An application that maintains a multi-turn conversation with a user by sending prior messages back to an LLM on each turn, since LLM APIs are stateless.' },
    { term: 'Claude', definition: 'Anthropic\'s family of LLMs (Haiku, Sonnet, Opus) known for long context windows, strong reasoning, and a safety-focused training approach based on Constitutional AI.' },
    { term: 'Completion', definition: 'The text an LLM generates in response to a prompt. Billed per output token and bounded by the model\'s max_tokens parameter.' },
    { term: 'Context Window', definition: 'The maximum number of tokens an LLM can consider in one request, covering the system prompt, conversation history, retrieved context, and the generated response.' },
    { term: 'Deep Learning', definition: 'A subset of machine learning that uses layered neural networks to learn patterns directly from raw data — the family of techniques that produced modern LLMs.' },
    { term: 'Embedding', definition: 'A dense numerical vector representation of text (or images, audio) that captures semantic meaning, enabling similarity comparison and semantic search.' },
    { term: 'Few-Shot Prompting', definition: 'Providing two to five input-output examples in the prompt so the model infers the desired format and style — often more reliable than verbose prose instructions.' },
    { term: 'Foundation Model', definition: 'A large model pre-trained on broad data (text, code, images) that can be adapted to many downstream tasks. Examples include GPT-4, Claude, Gemini, and Llama.' },
    { term: 'Generative AI', definition: 'A class of AI systems that produce new content — text, images, audio, video, code — rather than only classifying or predicting. LLMs are the text-generation branch.' },
    { term: 'GPT-4', definition: 'OpenAI\'s frontier LLM family (including GPT-4o and GPT-4 Turbo), accessed through the OpenAI API and widely used for general reasoning, coding, and multimodal tasks.' },
    { term: 'Hallucination', definition: 'When an LLM generates plausible-sounding but factually incorrect or fabricated output not grounded in its training data or the provided context.' },
    { term: 'Inference', definition: 'Running a trained model on new input to produce output. Calling an LLM API is inference — model weights are fixed and no learning takes place.' },
    { term: 'JSON (JavaScript Object Notation)', definition: 'The standard text-based data format for LLM API requests and responses. Also the most common structured output format requested from models.' },
    { term: 'Llama', definition: 'Meta\'s family of open-weight LLMs (Llama 2, Llama 3) that can be self-hosted, fine-tuned, and deployed without a paid API, popular in on-premise and regulated environments.' },
    { term: 'LLM (Large Language Model)', definition: 'A neural network trained on massive text corpora that generates, summarises, translates, and reasons about natural language. Examples include GPT-4, Claude, and Llama.' },
    { term: 'Machine Learning (ML)', definition: 'A subset of AI in which systems learn patterns from examples rather than following hand-coded rules. Supervised, unsupervised, and reinforcement learning are the three main paradigms.' },
    { term: 'Max Tokens', definition: 'An API parameter that caps the length of the generated response. Used to control cost, latency, and to prevent runaway outputs.' },
    { term: 'Message Role', definition: 'The label assigned to each entry in a chat-style request — system, user, or assistant — which tells the model how to interpret that content.' },
    { term: 'Mistral', definition: 'A French AI company whose open-weight models (Mistral 7B, Mixtral) are popular for self-hosted inference and cost-sensitive production use cases.' },
    { term: 'Model Parameter', definition: 'A numerical weight or bias inside the model learned during training. Frontier LLMs have tens to hundreds of billions of parameters.' },
    { term: 'OpenAI API', definition: 'The HTTP interface for calling OpenAI models (GPT-4, GPT-4o, o-series), accessed via the openai SDK with an API key and endpoints like /chat/completions and /embeddings.' },
    { term: 'Prompt', definition: 'The input text sent to an LLM — typically a combination of system instructions, retrieved context, conversation history, and the current user message.' },
    { term: 'Prompt Engineering', definition: 'The practice of crafting prompts to guide an LLM toward accurate, relevant, well-structured outputs. Core techniques include zero-shot, few-shot, role, and chain-of-thought prompting.' },
    { term: 'Python', definition: 'The dominant language for AI engineering — every major LLM SDK, vector database client, and ML framework targets it first.' },
    { term: 'REST API', definition: 'An HTTP-based architectural style using GET, POST, PUT, DELETE on resource URLs with JSON payloads. All major LLM APIs (OpenAI, Anthropic, Google) are REST APIs.' },
    { term: 'Role Prompting', definition: 'Framing the model as a specific persona — e.g. a senior claims analyst — to influence vocabulary, tone, and the technical depth of its responses.' },
    { term: 'Streaming', definition: 'Returning tokens from the API as they are generated rather than after the full response is complete, dramatically reducing perceived latency in user-facing applications.' },
    { term: 'System Prompt', definition: 'An initial instruction that sets the model\'s role, behaviour, constraints, and response format for the entire conversation. Persists across all turns.' },
    { term: 'Temperature', definition: 'A sampling parameter (typically 0–2) controlling output randomness. Low values produce deterministic, focused responses; high values increase variation and creativity.' },
    { term: 'Tiktoken', definition: 'OpenAI\'s open-source tokeniser library that lets you count tokens locally before sending a request — useful for cost estimation and context window management.' },
    { term: 'Token', definition: 'The smallest unit of text an LLM processes — roughly three-quarters of a word in English. Token count drives both API cost and context window limits.' },
    { term: 'Tokenisation', definition: 'The process of splitting text into tokens using subword algorithms such as byte-pair encoding. Whitespace, punctuation, and casing all affect the result.' },
    { term: 'Transformer', definition: 'The neural network architecture underlying modern LLMs, using self-attention to process all input tokens in parallel and capture long-range dependencies.' },
    { term: 'User Message', definition: 'In a chat-style LLM API, the role representing end-user input. Distinct from the system message, which sets global behaviour the user should not override.' },
    { term: 'Virtual Environment', definition: 'An isolated Python runtime (venv, conda, uv) that keeps project-specific dependencies separate — essential for managing conflicting versions of LLM SDKs and ML libraries.' },
    { term: 'Zero-Shot Prompting', definition: 'Asking the model to perform a task directly without providing examples. Works for straightforward tasks where the training data gives the model enough context.' },
  ],
  mid: [
    { term: 'Agent Loop', definition: 'The iterative cycle in which an LLM-based agent perceives state, decides on an action, invokes a tool, observes the result, and repeats until the task is complete or a limit is hit.' },
    { term: 'Approximate Nearest Neighbour (ANN)', definition: 'A class of algorithms (HNSW, IVF, Annoy) that trade exact results for dramatically faster vector similarity search — the backbone of production vector databases.' },
    { term: 'BM25', definition: 'A classical lexical ranking function that scores documents by term frequency and inverse document frequency. Often combined with vector search in hybrid retrieval.' },
    { term: 'Chain-of-Thought (CoT)', definition: 'A prompting technique that asks the model to reason step by step before producing the final answer, reliably improving accuracy on multi-step tasks at the cost of longer outputs.' },
    { term: 'Chunking', definition: 'Splitting documents into smaller segments for embedding and retrieval in RAG systems. Strategies include fixed-size, sentence-aware, semantic, and recursive splitting.' },
    { term: 'Context Engineering', definition: 'The discipline of deciding what information to place in the context window, in what order, and in what form — as important as the instructions themselves in complex applications.' },
    { term: 'Cosine Similarity', definition: 'A scale-invariant measure of the angle between two vectors, used to rank embeddings by semantic closeness. A score of 1.0 means identical direction; 0 means unrelated.' },
    { term: 'Embedding Model', definition: 'A model that converts text (or images, audio) into a fixed-dimensional vector. Examples include text-embedding-3-small, Cohere embed, and open-source bge and e5 families.' },
    { term: 'Eval (Evaluation Harness)', definition: 'An automated test suite that runs prompts against a held-out dataset and scores outputs — using exact match, rubric-based judging, or LLM-as-judge — to detect regressions.' },
    { term: 'Function Calling', definition: 'An LLM capability where the model outputs structured JSON to invoke registered functions or APIs, enabling it to take actions beyond plain text generation.' },
    { term: 'Golden Dataset', definition: 'A curated set of question-answer-context triples representing the target behaviour, used as the evaluation ground truth for a RAG system or fine-tuned model.' },
    { term: 'Guardrails', definition: 'Input and output filters that prevent LLMs from producing harmful, off-topic, or policy-violating content. May be rule-based, classifier-based, or LLM-as-judge.' },
    { term: 'HNSW (Hierarchical Navigable Small World)', definition: 'A graph-based approximate nearest neighbour index used by pgvector, Qdrant, and Weaviate to deliver millisecond vector search at scale.' },
    { term: 'Hugging Face', definition: 'The central hub for open-source models, datasets, and the transformers, datasets, and PEFT libraries — the default starting point for self-hosted LLM work.' },
    { term: 'Hybrid Search', definition: 'A retrieval strategy that combines vector (semantic) search with keyword (BM25) search and reranks the merged results — typically outperforming either method alone.' },
    { term: 'In-Context Learning', definition: 'The ability of an LLM to pick up a task from examples supplied in the prompt at inference time, without any weight updates. The mechanism behind few-shot prompting.' },
    { term: 'JSON Mode', definition: 'A model setting (or structured output API) that guarantees the response is syntactically valid JSON, often constrained to a provided schema — eliminating parsing errors in production pipelines.' },
    { term: 'LangChain', definition: 'A Python/JavaScript framework for composing LLM applications — providing abstractions for prompts, chains, retrievers, agents, and memory. Popular for rapid prototyping of RAG and agent systems.' },
    { term: 'LangGraph', definition: 'A stateful agent framework from the LangChain team that models workflows as explicit directed graphs with typed state, conditional edges, loops, and checkpointing.' },
    { term: 'LlamaIndex', definition: 'A Python framework focused on ingesting, indexing, and querying data for RAG — with strong defaults for chunking, hierarchical indexes, and query decomposition.' },
    { term: 'LoRA (Low-Rank Adaptation)', definition: 'A parameter-efficient fine-tuning technique that trains small rank-decomposition matrices attached to attention layers instead of updating full model weights, cutting compute by orders of magnitude.' },
    { term: 'Lost in the Middle', definition: 'A well-documented failure mode where LLMs attend less reliably to information placed in the middle of a long context than to content at the beginning or end.' },
    { term: 'MCP (Model Context Protocol)', definition: 'An open protocol introduced by Anthropic that standardises how LLM applications connect to external tools, resources, and data sources through a common server interface.' },
    { term: 'Multimodal Model', definition: 'A model that accepts and/or produces more than one modality — text, images, audio, video. GPT-4o, Claude 3.5, and Gemini are multimodal text-vision models.' },
    { term: 'Output Parser', definition: 'A component that converts raw LLM output into a validated typed object (using Pydantic, Zod, or regex), with retry logic when the response fails schema checks.' },
    { term: 'PEFT (Parameter-Efficient Fine-Tuning)', definition: 'A family of fine-tuning methods (LoRA, QLoRA, prefix tuning, adapters) that update a small number of parameters instead of the full model, making fine-tuning practical on commodity hardware.' },
    { term: 'Prompt Injection', definition: 'An attack in which malicious content in user input or retrieved data causes an LLM to deviate from its intended instructions — the signature security threat for LLM applications.' },
    { term: 'Prompt Template', definition: 'A parameterised string with placeholders for variables (user query, retrieved context, examples) that is filled in at runtime — version-controlled and tested like code.' },
    { term: 'QLoRA', definition: 'A memory-efficient fine-tuning technique combining LoRA with 4-bit quantisation of the base model, enabling fine-tuning of 70B-class models on a single consumer GPU.' },
    { term: 'RAG (Retrieval-Augmented Generation)', definition: 'An architecture that grounds LLM responses in an external knowledge base by retrieving relevant chunks at query time and injecting them into the prompt context.' },
    { term: 'RAGAS', definition: 'An open-source evaluation framework for RAG pipelines that computes faithfulness, answer relevancy, context precision, and context recall — automatable with LLM-as-judge.' },
    { term: 'ReAct (Reasoning and Acting)', definition: 'An agent prompting pattern that interleaves explicit thought steps with tool calls, making the reasoning trace inspectable and improving reliability on multi-step tasks.' },
    { term: 'Reranker', definition: 'A second-stage model (typically a cross-encoder such as Cohere Rerank or bge-reranker) that re-scores the top-k retrieved chunks for precision before they are injected into the context.' },
    { term: 'Retriever', definition: 'The component of a RAG pipeline responsible for finding relevant documents for a query — usually a vector store, sometimes combined with a keyword index in hybrid search.' },
    { term: 'Semantic Search', definition: 'Finding content by meaning rather than keywords, using embeddings and vector similarity so paraphrases and synonyms still surface the right results.' },
    { term: 'Structured Output', definition: 'Constraining LLM responses to conform to a schema (JSON, XML, regex grammar) using function calling, JSON mode, or guided decoding — essential for programmatic downstream use.' },
    { term: 'Tool Use', definition: 'The pattern in which an LLM produces a structured call to an external function or API, the host application executes it, and the result is fed back to the model for the next step.' },
    { term: 'Top-P (Nucleus Sampling)', definition: 'A sampling parameter that restricts token choices to the smallest set whose cumulative probability exceeds p, trimming the long tail while preserving diversity.' },
    { term: 'Vector Database', definition: 'A database optimised for storing and querying high-dimensional embedding vectors using ANN indexes. Examples include Pinecone, Weaviate, Qdrant, Chroma, and pgvector.' },
  ],
  senior: [
    { term: 'Adversarial Example', definition: 'An input crafted to cause a model to produce a specific wrong output — in LLMs, typically a jailbreak string or a suffix that bypasses safety training.' },
    { term: 'AI Alignment', definition: 'The field focused on ensuring AI systems behave in accordance with human values and intentions, especially as capabilities scale beyond reliable human oversight.' },
    { term: 'AI Governance', definition: 'The organisational framework for managing AI risk — covering use-case registries, risk classification, bias audits, data provenance, access controls, and regulatory compliance.' },
    { term: 'AI Register', definition: 'An internal inventory of every AI use case in the organisation, classified by risk level, that drives the governance obligations required under policies such as the EU AI Act.' },
    { term: 'Batch Inference', definition: 'Submitting many prompts in a single asynchronous job for offline processing — offered at a discount (typically 50%) by OpenAI, Anthropic, and most open-source serving stacks.' },
    { term: 'Constitutional AI', definition: 'Anthropic\'s alignment technique where a model is trained to critique and revise its own outputs against a written set of principles, reducing reliance on human preference labels.' },
    { term: 'Cost Per Token', definition: 'The unit economics of LLM usage — separate input and output prices (e.g. USD per million tokens). Drives architectural decisions around caching, model routing, and context compression.' },
    { term: 'Data Contamination', definition: 'When evaluation data has leaked into a model\'s training set, making benchmark scores misleadingly high. A recurring risk with public benchmarks on frontier models.' },
    { term: 'Differential Privacy', definition: 'A mathematical framework for training or querying models with provable guarantees that individual records cannot be identified from outputs, via calibrated noise injection.' },
    { term: 'Distillation', definition: 'Training a smaller student model to mimic a larger teacher, reducing inference cost while preserving most output quality — common for shipping fine-tuned behaviour into edge-deployable models.' },
    { term: 'EU AI Act', definition: 'The EU\'s risk-based AI regulation (in force August 2024, high-risk rules from August 2026) that classifies insurance underwriting and claims as high-risk and mandates conformity assessments.' },
    { term: 'Indirect Prompt Injection', definition: 'An attack where malicious instructions are hidden in data the agent retrieves — a webpage, document, or email — rather than in the user\'s message, hijacking the agent\'s reasoning loop.' },
    { term: 'Inference Latency', definition: 'End-to-end time from request to final token, dominated by time-to-first-token and per-token generation speed. Optimised through streaming, KV cache, speculative decoding, and continuous batching.' },
    { term: 'KV Cache', definition: 'The stored key and value tensors from prior tokens that let a transformer avoid recomputing attention on the full prompt for each new token — the dominant memory cost during inference.' },
    { term: 'Mixture of Experts (MoE)', definition: 'An architecture that activates only a subset of specialised sub-networks per token (e.g. Mixtral, GPT-4), delivering large-model quality at a fraction of the dense inference cost.' },
    { term: 'Model Router', definition: 'A layer that dispatches each request to the cheapest or best-suited model based on difficulty, task type, or latency budget — used to cut costs without degrading end-user quality.' },
    { term: 'Model Serving', definition: 'Deploying models behind APIs with batching, auto-scaling, GPU management, and latency controls. Stacks include vLLM, TGI, Triton, and SageMaker Inference.' },
    { term: 'Multi-Agent System', definition: 'An architecture where specialised agents (planner, researcher, writer, critic) collaborate via a supervisor or shared state — increasing capability at the cost of debugging and coordination overhead.' },
    { term: 'OWASP Top 10 for LLMs', definition: 'The community-maintained taxonomy of the most critical LLM security risks — covering prompt injection, insecure output handling, training data poisoning, and supply chain vulnerabilities.' },
    { term: 'Quantisation', definition: 'Reducing a model\'s numerical precision (FP16 to 8-bit, 4-bit, or lower) to shrink memory footprint and speed up inference with minimal quality loss. GGUF and AWQ are common formats.' },
    { term: 'Red Teaming', definition: 'Systematically probing an AI system with adversarial prompts, jailbreaks, and edge cases to discover vulnerabilities and failure modes before release — the AI analogue of penetration testing.' },
    { term: 'RLHF (Reinforcement Learning from Human Feedback)', definition: 'A training technique where a reward model learned from human preference comparisons guides the LLM to produce more helpful, harmless, and honest outputs. The mechanism behind instruction-tuned models.' },
    { term: 'Secure AI Framework (SAIF)', definition: 'A structured nine-area framework for securing AI systems across user awareness, prompt and output validation, context and memory, pipelines, agent IAM, separation of duties, observability, compute, and model selection.' },
    { term: 'Self-Attention', definition: 'The transformer mechanism that lets each token weigh the relevance of every other token in the input, capturing long-range dependencies and enabling the emergent capabilities of modern LLMs.' },
    { term: 'Synthetic Data', definition: 'Training or evaluation data generated by an LLM rather than collected from the real world — used to bootstrap fine-tuning sets, augment rare cases, or build evaluation rubrics at scale.' },
  ],
}
