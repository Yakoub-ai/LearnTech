export default {
  beginner: [
    { term: 'Variable (let, const, var)', definition: 'Named storage for values. Use const for values that won\'t be reassigned, let for those that will. Avoid var in modern code.' },
    { term: 'Data Types', definition: 'JavaScript has 7 primitives (string, number, bigint, boolean, undefined, null, symbol) and one complex type (object). Arrays and functions are objects.' },
    { term: 'Function', definition: 'A reusable block of code. Can be declared (function name() {}), expressed (const fn = function() {}), or arrowed (const fn = () => {}).' },
    { term: 'Array', definition: 'An ordered list of values accessed by zero-based index, with built-in methods like map(), filter(), reduce(), push(), and slice().' },
    { term: 'Object', definition: 'A collection of key-value pairs (properties and methods). The fundamental data structure for organising related data in JavaScript.' },
    { term: 'DOM Manipulation', definition: 'Using JavaScript to read and modify the HTML document — querySelector(), createElement(), addEventListener(), and modifying element properties.' },
    { term: 'Callback', definition: 'A function passed as an argument to another function, to be called later — the foundational pattern for asynchronous JavaScript.' },
    { term: 'Template Literal', definition: 'A string delimited by backticks (`) that supports multi-line text and embedded expressions: `Hello, ${name}!`.' },
    { term: 'Truthy / Falsy', definition: 'Values that coerce to true or false in boolean context. Falsy: false, 0, "", null, undefined, NaN. Everything else is truthy.' },
    { term: 'JSON', definition: 'JavaScript Object Notation — a lightweight text format for data exchange. Parsed with JSON.parse() and serialised with JSON.stringify().' },
  ],
  mid: [
    { term: 'Promise', definition: 'An object representing the eventual completion (or failure) of an async operation, with .then(), .catch(), and .finally() methods.' },
    { term: 'async / await', definition: 'Syntactic sugar over Promises that makes asynchronous code read like synchronous code. async functions always return a Promise.' },
    { term: 'Closure', definition: 'A function that retains access to variables from its outer (enclosing) scope even after the outer function has returned.' },
    { term: 'Destructuring', definition: 'A syntax for unpacking values from arrays ([a, b] = arr) or properties from objects ({name, age} = obj) into distinct variables.' },
    { term: 'Spread / Rest Operator', definition: 'The ... syntax: spread expands iterables into individual elements; rest collects remaining elements into an array or object.' },
    { term: 'Module (import / export)', definition: 'ES Modules allow splitting code into files. Use export to expose values and import to consume them, enabling tree-shaking by bundlers.' },
    { term: 'Event Loop', definition: 'The mechanism that allows JavaScript to perform non-blocking I/O by offloading tasks and processing callbacks from the task queue when the call stack is empty.' },
    { term: 'this Keyword', definition: 'A context-dependent reference: in methods it refers to the object, in arrow functions it inherits from the enclosing scope, in global scope it\'s window/globalThis.' },
    { term: 'Error Handling', definition: 'Using try/catch/finally blocks and custom Error subclasses to gracefully handle runtime errors and rejected Promises.' },
    { term: 'Map / Set', definition: 'Built-in collection types: Map stores key-value pairs (any type as key), Set stores unique values. Both are iterable and have O(1) lookups.' },
  ],
  senior: [
    { term: 'Prototype Chain', definition: 'JavaScript\'s inheritance mechanism where objects delegate property lookups to their prototype, forming a chain ending at Object.prototype.' },
    { term: 'Proxy / Reflect', definition: 'Proxy wraps an object to intercept fundamental operations (get, set, delete). Reflect provides the default behaviour for those operations.' },
    { term: 'WeakMap / WeakRef', definition: 'Collections that hold weak references to keys/values, allowing garbage collection when no other references exist — useful for caches and metadata.' },
    { term: 'SharedArrayBuffer / Atomics', definition: 'APIs for sharing memory between the main thread and Web Workers, with Atomics providing thread-safe operations on shared data.' },
    { term: 'Symbol', definition: 'A primitive type for creating unique, non-string property keys. Well-known Symbols (Symbol.iterator, Symbol.toPrimitive) customise language behaviour.' },
    { term: 'Generator Function', definition: 'A function declared with function* that can pause (yield) and resume execution, enabling lazy sequences and custom iterators.' },
    { term: 'AbortController', definition: 'A built-in API for cancelling Promises, fetch requests, and event listeners via an AbortSignal, preventing resource leaks.' },
    { term: 'Structured Clone', definition: 'The structuredClone() API for deep-copying objects including Dates, RegExps, Maps, Sets, and ArrayBuffers — unlike JSON.parse(JSON.stringify()).' },
    { term: 'V8 Optimisation', definition: 'Understanding how the V8 engine (Chrome, Node) optimises JavaScript — hidden classes, inline caches, and deoptimisation triggers — to write performant code.' },
    { term: 'TC39 Process', definition: 'The standardisation process for ECMAScript (JavaScript) features, moving through stages 0-4 from proposal to specification inclusion.' },
  ],
}
