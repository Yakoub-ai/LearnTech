export default {
  beginner: [
    { term: 'API (Application Programming Interface)', definition: 'A contract that lets one program talk to another — on the web, usually a set of HTTP endpoints that accept requests and return structured data (typically JSON).' },
    { term: 'Array', definition: 'An ordered, indexable collection of values stored under a single name, used for lists of items such as users, rows, or search results.' },
    { term: 'Base64', definition: 'A text encoding that represents binary data using 64 printable characters, commonly used to embed files in JSON payloads or encode Basic Auth credentials.' },
    { term: 'Boolean', definition: 'A primitive data type with only two values — true or false — used to drive conditionals and flags.' },
    { term: 'Class', definition: 'A blueprint in object-oriented programming that defines the attributes and behaviour (methods) shared by its instances.' },
    { term: 'Client-Server Architecture', definition: 'A model where clients (browsers, mobile apps) request services from a centralised server that processes logic and returns responses over a network.' },
    { term: 'Compiler', definition: 'A tool that translates source code written in one language (e.g. Go, Rust) into a lower-level form — usually machine code or bytecode — before it runs.' },
    { term: 'Control Flow', definition: 'The order in which statements execute, directed by conditionals (if/else, switch) and loops (for, while).' },
    { term: 'Cookie', definition: 'A small piece of data the server asks the browser to store and send back on subsequent requests, commonly used for session identifiers and preferences.' },
    { term: 'Data Type', definition: 'A classification that specifies what kind of value a variable can hold — integer, string, boolean, float, array, object, and so on.' },
    { term: 'Database', definition: 'A structured system (e.g. Postgres, MySQL, MongoDB) for storing, retrieving, and managing data persistently so it survives restarts and can be queried efficiently.' },
    { term: 'Debugger', definition: 'A tool (e.g. pdb, debugpy, Chrome DevTools) that lets you pause a running program, inspect variables, and step through code line by line.' },
    { term: 'Django', definition: 'A batteries-included Python web framework offering an ORM, admin UI, auth, and templating — well suited to content-heavy sites and larger applications.' },
    { term: 'Endpoint', definition: 'A specific URL path on a server that accepts requests for a given resource or action, such as GET /users/42 or POST /orders.' },
    { term: 'Environment Variable', definition: 'A key-value pair supplied by the host operating system or container runtime — used to inject configuration and secrets (e.g. DATABASE_URL) without hardcoding them.' },
    { term: 'Error', definition: 'A condition that prevents code from completing normally. Backends typically catch errors, log them, and return an appropriate HTTP status to the client.' },
    { term: 'FastAPI', definition: 'A modern Python web framework built on Starlette and Pydantic that offers type-hint-driven validation, async support, and automatic OpenAPI docs.' },
    { term: 'Framework', definition: 'A reusable software skeleton (e.g. FastAPI, Django, Express, Spring Boot) that provides structure and utilities for routing, request parsing, and response formatting.' },
    { term: 'Function', definition: 'A named, reusable block of code that accepts inputs (parameters), performs a task, and optionally returns an output value.' },
    { term: 'Git', definition: 'A distributed version-control system used to track changes, branch off work, and collaborate on code through platforms like GitHub and GitLab.' },
    { term: 'Header', definition: 'A key-value pair attached to an HTTP request or response carrying metadata — Content-Type, Authorization, Accept, and Cache-Control are common examples.' },
    { term: 'HTTP (HyperText Transfer Protocol)', definition: 'The application-layer protocol that powers the web. Clients send requests using methods like GET, POST, PUT, and DELETE, and servers reply with a status code, headers, and a body.' },
    { term: 'HTTP Method', definition: 'The verb of an HTTP request that describes intent — GET reads, POST creates, PUT/PATCH updates, DELETE removes, and OPTIONS negotiates CORS.' },
    { term: 'HTTPS', definition: 'HTTP carried over TLS so that traffic is encrypted, authenticated, and tamper-evident. Treated as the default for any modern public API.' },
    { term: 'IDE (Integrated Development Environment)', definition: 'A tool such as VS Code, PyCharm, or IntelliJ that combines an editor, debugger, and build tooling to streamline development.' },
    { term: 'JSON (JavaScript Object Notation)', definition: 'A lightweight, text-based data interchange format using key-value pairs and arrays — the standard payload format for REST APIs.' },
    { term: 'Library', definition: 'A collection of pre-written code you import to avoid rebuilding common functionality — e.g. requests for HTTP calls or pandas for data manipulation.' },
    { term: 'Logging', definition: 'Recording structured information about program execution to files or a log aggregator so engineers can diagnose issues and audit behaviour.' },
    { term: 'Null', definition: 'A sentinel value (also None in Python, nil in Go, undefined in JS) indicating the absence of a value — frequent source of bugs if not handled explicitly.' },
    { term: 'OOP (Object-Oriented Programming)', definition: 'A paradigm that organises code into objects — bundles of data (attributes) and behaviour (methods) — using classes, inheritance, and encapsulation.' },
    { term: 'Path Parameter', definition: 'A value embedded in the URL path (e.g. /users/{id}) that identifies a specific resource being addressed.' },
    { term: 'Port', definition: 'A numeric endpoint on a host (e.g. 80, 443, 5432) that the operating system uses to route network traffic to a specific process.' },
    { term: 'Python', definition: 'A high-level, dynamically typed language popular for backend services, data work, and scripting — the reference language for this learning path.' },
    { term: 'Query Parameter', definition: 'A key-value pair appended to a URL after the ? character (e.g. ?page=2&limit=20), typically used for filtering, sorting, and pagination.' },
    { term: 'Request', definition: 'The message a client sends to a server, containing a method, URL, headers, and optionally a body.' },
    { term: 'Response', definition: 'The message a server sends back to the client, containing a status code, headers, and optionally a body.' },
    { term: 'REST API', definition: 'An architectural style for web services that uses standard HTTP methods on resource URLs and returns representations — usually JSON — of server-side state.' },
    { term: 'Server', definition: 'A long-running process (or the machine hosting it) that listens for incoming requests, executes business logic, and returns responses.' },
    { term: 'Status Code', definition: 'A three-digit number returned in an HTTP response indicating the result — 2xx success, 3xx redirection, 4xx client error, 5xx server error.' },
    { term: 'String', definition: 'A sequence of characters used to represent text, such as a user name, email address, or URL.' },
    { term: 'URL (Uniform Resource Locator)', definition: 'The addressable location of a resource on the web, composed of scheme, host, port, path, and optional query and fragment components.' },
    { term: 'Variable', definition: 'A named storage location in memory that holds a value which can change during program execution.' },
    { term: 'XML (eXtensible Markup Language)', definition: 'A verbose, tag-based data format used in legacy enterprise APIs (SOAP, older banking and government systems) and some configuration files.' },
    { term: 'YAML (YAML Ain\'t Markup Language)', definition: 'A human-friendly, indentation-based configuration format widely used for Docker Compose, Kubernetes manifests, and CI pipelines.' },
  ],
  mid: [
    { term: 'ACID', definition: 'The guarantees a relational transaction should provide — Atomicity, Consistency, Isolation, and Durability — so partial failures cannot leave the database in a corrupt state.' },
    { term: 'API Gateway', definition: 'A single entry point (e.g. Kong, AWS API Gateway, NGINX) that routes requests to downstream services and centralises concerns like auth, rate limiting, and TLS termination.' },
    { term: 'API Versioning', definition: 'A strategy for evolving an API without breaking existing clients — typically via URL path (/v2/), custom headers, or query parameters.' },
    { term: 'Authentication vs Authorisation', definition: 'Authentication proves who you are (identity); authorisation decides what you may do (permissions). Both are required, and they are usually implemented by distinct mechanisms.' },
    { term: 'Caching', definition: 'Storing frequently accessed data in a fast layer (e.g. Redis, Memcached, CDN) to reduce database load and latency. Common strategies include cache-aside, write-through, and write-behind.' },
    { term: 'CDN (Content Delivery Network)', definition: 'A globally distributed edge network (e.g. Cloudflare, Fastly, CloudFront) that caches static assets close to users to cut latency and offload origin servers.' },
    { term: 'CI/CD (Continuous Integration / Continuous Deployment)', definition: 'An automated pipeline (e.g. GitHub Actions, GitLab CI) that builds, tests, and deploys every commit — catching regressions early and shortening release cycles.' },
    { term: 'Connection Pool', definition: 'A pre-created set of database connections kept open and reused by the application, avoiding the cost of opening a fresh TCP/TLS handshake for every query.' },
    { term: 'CORS (Cross-Origin Resource Sharing)', definition: 'A browser security mechanism that uses server-sent headers (Access-Control-Allow-Origin, etc.) to decide which origins may call an API from client-side code.' },
    { term: 'CSRF (Cross-Site Request Forgery)', definition: 'An attack where a logged-in user\'s browser is tricked into sending an unwanted authenticated request. Mitigated with anti-CSRF tokens and SameSite cookies.' },
    { term: 'Dependency Injection', definition: 'A pattern where a component receives its collaborators (database, clients, config) from the outside rather than constructing them itself — easing testing and swapping implementations.' },
    { term: 'Docker', definition: 'A platform for building, shipping, and running applications in lightweight, isolated containers that bundle code with all its dependencies.' },
    { term: 'Docker Compose', definition: 'A tool for defining and running multi-container local stacks from a single YAML file — typical for standing up an app alongside Postgres, Redis, and queues in development.' },
    { term: 'Feature Flag', definition: 'A runtime switch (e.g. LaunchDarkly, Unleash) that turns functionality on or off without redeploying, enabling gradual rollouts, A/B tests, and quick kill switches.' },
    { term: 'GraphQL', definition: 'A query language for APIs that lets clients request exactly the data they need in a single request, backed by a typed schema and server-side resolvers.' },
    { term: 'gRPC', definition: 'A high-performance RPC framework using Protocol Buffers for serialisation and HTTP/2 for transport, enabling strongly-typed, bidirectional streaming between services.' },
    { term: 'Index', definition: 'An auxiliary data structure (usually a B-tree) on one or more database columns that speeds up reads at the cost of extra storage and slower writes.' },
    { term: 'Integration Test', definition: 'A test that exercises multiple components together — application plus real database or HTTP client plus server — catching wiring bugs that unit tests cannot.' },
    { term: 'JWT (JSON Web Token)', definition: 'A compact, URL-safe token carrying a signed JSON payload, commonly used for stateless authentication between client and server.' },
    { term: 'Message Queue', definition: 'Middleware (e.g. RabbitMQ, Amazon SQS) that decouples producers and consumers by storing messages until they are processed, smoothing load spikes and adding retry semantics.' },
    { term: 'Migration', definition: 'A versioned, scripted change to a database schema (e.g. Alembic, Flyway, Django migrations) checked into source control and applied in order across environments.' },
    { term: 'MongoDB', definition: 'A document-oriented NoSQL database storing BSON records inside collections, well suited to flexible or evolving schemas but weaker on multi-document transactions.' },
    { term: 'MySQL', definition: 'A widely deployed open-source relational database, often chosen for LAMP stacks and web-scale read-heavy workloads via replication.' },
    { term: 'N+1 Problem', definition: 'A performance bug where loading N parent rows causes N additional queries for their children. Fixed with eager loading, JOINs, or DataLoader-style batching.' },
    { term: 'NoSQL', definition: 'A family of non-relational databases (document, key-value, wide-column, graph) that trade some SQL guarantees for flexible schemas, horizontal scaling, or specialised access patterns.' },
    { term: 'OAuth 2.0', definition: 'An authorisation framework that lets third-party applications obtain limited access to a service on behalf of a user, using access and refresh tokens instead of credentials.' },
    { term: 'OpenAPI', definition: 'A specification (formerly Swagger) for describing HTTP APIs in YAML or JSON — drives auto-generated docs, client SDKs, and contract tests.' },
    { term: 'OpenID Connect', definition: 'An identity layer on top of OAuth 2.0 that adds a signed ID token, enabling single sign-on and standard user-info endpoints.' },
    { term: 'ORM (Object-Relational Mapping)', definition: 'A technique (e.g. SQLAlchemy, Django ORM, Prisma) that maps database tables to language objects, letting developers query and manipulate data without writing raw SQL.' },
    { term: 'OWASP Top 10', definition: 'A regularly updated list of the ten most critical web application security risks, including injection, broken authentication, and broken access control.' },
    { term: 'Pagination', definition: 'Splitting large result sets into pages using offset/limit or cursor-based strategies — cursors scale better and avoid skipping or duplicating rows under concurrent writes.' },
    { term: 'Postgres (PostgreSQL)', definition: 'A powerful open-source relational database known for strict standards compliance, rich data types (JSONB, arrays, geospatial), and strong concurrency via MVCC.' },
    { term: 'Pydantic', definition: 'A Python library that validates and serialises data from type hints, used by FastAPI to parse request bodies and shape response models.' },
    { term: 'Query Plan', definition: 'The execution strategy the database chooses for a query, visible via EXPLAIN/EXPLAIN ANALYZE — essential for diagnosing missing indexes and slow joins.' },
    { term: 'RBAC (Role-Based Access Control)', definition: 'An authorisation model where permissions are granted to roles (admin, editor, viewer) and users inherit permissions by being assigned roles.' },
    { term: 'Redis', definition: 'An in-memory key-value store used for caching, session storage, rate-limiting counters, pub/sub, and lightweight queues — prized for sub-millisecond latency.' },
    { term: 'Reverse Proxy', definition: 'A server (e.g. NGINX, HAProxy, Envoy) that sits in front of application servers and forwards client requests, often adding TLS termination, compression, and caching.' },
    { term: 'Sessions', definition: 'Server-side state associated with a user, usually keyed by a session ID stored in a cookie — an alternative to stateless JWT-based authentication.' },
    { term: 'SQL (Structured Query Language)', definition: 'The declarative language used to define, query, and modify relational databases — covering SELECT, INSERT, UPDATE, DELETE, and DDL statements like CREATE TABLE.' },
    { term: 'SQL Join', definition: 'A clause that combines rows from two or more tables based on a related column — INNER, LEFT, RIGHT, and FULL OUTER joins determine which unmatched rows are kept.' },
    { term: 'TLS (Transport Layer Security)', definition: 'The cryptographic protocol underlying HTTPS that provides encryption, server (and optionally client) authentication, and message integrity.' },
    { term: 'Transaction', definition: 'A unit of work executed against a database that either commits fully or rolls back entirely, preserving ACID guarantees across multiple statements.' },
    { term: 'Unit Test', definition: 'A narrow, fast test (e.g. pytest, JUnit) that verifies a single function or class in isolation, typically mocking external dependencies.' },
    { term: 'Webhook', definition: 'An HTTP callback fired by one service to another when an event occurs — the receiver exposes a URL and the sender POSTs the event payload, enabling push-style integrations without polling.' },
    { term: 'WebSocket', definition: 'A protocol that upgrades an HTTP connection to a persistent, full-duplex channel — used for chat, live dashboards, and real-time notifications.' },
  ],
  senior: [
    { term: 'BASE', definition: 'An alternative to ACID for distributed systems — Basically Available, Soft state, Eventual consistency — accepting temporary inconsistency in exchange for availability and scale.' },
    { term: 'Big O Notation', definition: 'A mathematical notation describing the upper bound of an algorithm\'s time or space complexity as input size grows — O(1), O(log n), O(n), O(n log n), O(n^2).' },
    { term: 'Blue-Green Deployment', definition: 'A release strategy running two identical production environments; traffic is switched from the old (blue) to the new (green) once verified, enabling instant rollback.' },
    { term: 'Canary Deployment', definition: 'Rolling out a new version to a small percentage of traffic first, then gradually increasing as metrics and error budgets hold — limits blast radius on regressions.' },
    { term: 'CAP Theorem', definition: 'A principle stating that a distributed system can guarantee at most two of three properties: Consistency, Availability, and Partition tolerance.' },
    { term: 'Circuit Breaker', definition: 'A resilience pattern that stops calling a failing dependency after a threshold of errors, returning fast failures until a probe indicates recovery.' },
    { term: 'Contract Test', definition: 'A test (e.g. Pact) that verifies a provider service still honours the schema and behaviour its consumers rely on — catches breaking changes before deployment.' },
    { term: 'CQRS (Command Query Responsibility Segregation)', definition: 'A pattern that separates read and write models, allowing each to be optimised independently for performance, scalability, and domain clarity.' },
    { term: 'DDD (Domain-Driven Design)', definition: 'A design approach that models software around the core business domain, using bounded contexts, aggregates, and a ubiquitous language shared with domain experts.' },
    { term: 'Distributed Tracing', definition: 'A technique (e.g. OpenTelemetry, Jaeger, Tempo) that follows a single request across services via propagated trace and span IDs, exposing end-to-end latency.' },
    { term: 'Event Sourcing', definition: 'A persistence model where state is derived by replaying an append-only log of domain events rather than storing the current state directly — gives full audit history and time travel.' },
    { term: 'Event-Driven Architecture', definition: 'A pattern where services communicate by producing and consuming events, enabling loose coupling, asynchronous processing, and easier horizontal scaling.' },
    { term: 'Eventual Consistency', definition: 'A consistency model in which replicas converge to the same state given enough time and no new writes — the typical trade-off in highly available distributed systems.' },
    { term: 'Graceful Shutdown', definition: 'A process that stops accepting new work, drains in-flight requests, closes connections cleanly, and then exits — critical during rolling deploys and autoscaling.' },
    { term: 'Health Check', definition: 'An endpoint (e.g. /healthz, /readyz) used by load balancers and orchestrators to decide whether an instance should receive traffic or be restarted.' },
    { term: 'Helm', definition: 'A Kubernetes package manager that templates manifests into reusable charts with values files, versioning, and release history — the standard way to ship k8s workloads.' },
    { term: 'Hexagonal Architecture', definition: 'A design style (also called Ports and Adapters) that isolates core business logic from infrastructure by communicating only through well-defined interfaces.' },
    { term: 'Horizontal vs Vertical Scaling', definition: 'Horizontal scaling adds more machines (scale out); vertical scaling adds more resources to a single machine (scale up). Stateless services favour horizontal scaling.' },
    { term: 'Idempotency', definition: 'A property of an operation where performing it multiple times produces the same result as performing it once — critical for safe retries in distributed systems.' },
    { term: 'Infrastructure as Code (IaC)', definition: 'Managing and provisioning infrastructure through machine-readable configuration files (e.g. Terraform, Pulumi, CloudFormation) instead of manual clicks.' },
    { term: 'Kafka', definition: 'A distributed, partitioned, append-only log used for high-throughput event streaming, log aggregation, and as the backbone of event-driven architectures.' },
    { term: 'Kubernetes', definition: 'An open-source platform for deploying, scaling, and operating containerised workloads, providing scheduling, service discovery, rolling updates, and self-healing.' },
    { term: 'Kubernetes Deployment', definition: 'A Kubernetes resource that declares the desired state for a set of Pods — replica count, image, rollout strategy — and lets the controller reconcile reality towards it.' },
    { term: 'Load Balancer', definition: 'A device or service (e.g. HAProxy, AWS ALB, Envoy) that distributes incoming traffic across multiple servers to avoid overloading any single one and to survive node failures.' },
    { term: 'Microservices', definition: 'An architectural style where an application is composed of small, independently deployable services, each owning its own data and communicating over the network.' },
    { term: 'Monolith', definition: 'A single deployable unit containing all business logic — simpler to build and operate early on, but harder to scale teams and releases beyond a certain size.' },
    { term: 'Observability', definition: 'The ability to understand a system\'s internal state from its external outputs — the three pillars are structured logs, metrics, and distributed traces.' },
    { term: 'OpenTelemetry', definition: 'A CNCF standard and SDK for generating and exporting logs, metrics, and traces in a vendor-neutral format, feeding backends like Jaeger, Prometheus, and Datadog.' },
    { term: 'Pub/Sub', definition: 'A messaging pattern where publishers emit events to topics and any number of subscribers receive them independently, enabling fan-out and decoupling.' },
    { term: 'Rate Limiting', definition: 'Controlling the number of requests a client can make within a time window — typically via token bucket or leaky bucket — to protect APIs from abuse and resource exhaustion.' },
    { term: 'Retry with Backoff', definition: 'Re-attempting a failed operation after progressively longer waits, usually with jitter, to ride out transient failures without hammering a struggling dependency.' },
    { term: 'Saga', definition: 'A pattern for coordinating long-running business transactions across services via a sequence of local transactions and compensating actions instead of distributed locks.' },
    { term: 'Secrets Management', definition: 'Securely storing and distributing credentials, tokens, and keys via tools like HashiCorp Vault, AWS Secrets Manager, or Kubernetes Secrets — never in source control.' },
    { term: 'Service Mesh', definition: 'A dedicated infrastructure layer (e.g. Istio, Linkerd) that handles service-to-service traffic, providing mTLS, load balancing, retries, and observability without changing application code.' },
    { term: 'SOLID', definition: 'Five object-oriented design principles — Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion — aimed at producing maintainable systems.' },
    { term: 'Stateless Service', definition: 'A service that keeps no per-request state in memory, delegating session and data persistence to external stores — the prerequisite for easy horizontal scaling.' },
    { term: 'System Design', definition: 'The process of defining a system\'s architecture — components, data flow, scaling strategy, and trade-offs — to meet functional and non-functional requirements.' },
    { term: 'Test Pyramid', definition: 'A guideline that a healthy test suite has many fast unit tests, fewer integration tests, and even fewer end-to-end tests — inverting this pyramid is a common anti-pattern.' },
  ],
}
