export default {
  beginner: [
    { term: 'SELECT Statement', definition: 'The fundamental query command that retrieves data from one or more tables. Supports column selection, expressions, and aliasing — e.g., SELECT name, age FROM users.' },
    { term: 'WHERE Clause', definition: 'Filters rows based on a condition — e.g., WHERE age > 18. Supports comparison operators, LIKE for pattern matching, IN for value lists, and BETWEEN for ranges.' },
    { term: 'JOIN (INNER, LEFT, RIGHT)', definition: 'Combines rows from two or more tables based on a related column. INNER JOIN returns only matches; LEFT JOIN includes all left-table rows; RIGHT JOIN includes all right-table rows.' },
    { term: 'INSERT / UPDATE / DELETE', definition: 'Data manipulation statements: INSERT adds new rows, UPDATE modifies existing rows (use WHERE to target specific rows), and DELETE removes rows from a table.' },
    { term: 'Primary Key', definition: 'A column (or combination of columns) that uniquely identifies each row in a table. Must be unique and NOT NULL. Most tables use an auto-incrementing integer or UUID.' },
    { term: 'Foreign Key', definition: 'A column that references the primary key of another table, establishing a relationship between the two tables and enforcing referential integrity.' },
    { term: 'NULL', definition: 'A special marker representing a missing or unknown value. NULL is not equal to anything — including itself — so comparisons require IS NULL or IS NOT NULL.' },
    { term: 'ORDER BY / GROUP BY', definition: 'ORDER BY sorts the result set by one or more columns (ASC or DESC). GROUP BY collapses rows with identical values into summary rows, typically used with aggregate functions.' },
    { term: 'Aggregate Functions (COUNT, SUM, AVG, MAX, MIN)', definition: 'Functions that compute a single value from a set of rows. COUNT counts rows, SUM/AVG compute totals/averages, and MAX/MIN find extreme values.' },
    { term: 'CREATE TABLE', definition: 'A DDL statement that defines a new table, specifying column names, data types, and constraints — e.g., CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(100)).' },
    { term: 'Data Types (INT, VARCHAR, DATE, BOOLEAN)', definition: 'Column type definitions that determine what values can be stored. INT for whole numbers, VARCHAR(n) for variable-length strings, DATE for calendar dates, BOOLEAN for true/false.' },
    { term: 'Index', definition: 'A data structure (typically B-tree) that speeds up data retrieval on specific columns at the cost of additional storage and slower writes. Essential for query performance.' },
  ],
  mid: [
    { term: 'Subquery (Scalar, Correlated)', definition: 'A query nested inside another query. Scalar subqueries return a single value; correlated subqueries reference the outer query and execute once per outer row.' },
    { term: 'Common Table Expression (CTE)', definition: 'A named temporary result set defined with the WITH keyword that exists for the duration of a single query, improving readability of complex queries.' },
    { term: 'Window Functions (ROW_NUMBER, RANK, LEAD/LAG, OVER/PARTITION BY)', definition: 'Functions that perform calculations across a set of rows related to the current row without collapsing them. The OVER clause defines the window; PARTITION BY groups within it.' },
    { term: 'HAVING Clause', definition: 'Filters groups after GROUP BY aggregation, as opposed to WHERE which filters individual rows before grouping — e.g., HAVING COUNT(*) > 5.' },
    { term: 'UNION / INTERSECT / EXCEPT', definition: 'Set operations that combine result sets: UNION merges (deduplicating by default), INTERSECT returns common rows, and EXCEPT returns rows in the first set but not the second.' },
    { term: 'Views', definition: 'Named stored queries that act as virtual tables. Views simplify complex joins, restrict access to sensitive columns, and provide a stable interface even when underlying tables change.' },
    { term: 'Stored Procedures', definition: 'Precompiled blocks of SQL and procedural logic stored in the database, callable by name. They reduce network round trips and encapsulate business logic at the database layer.' },
    { term: 'Triggers', definition: 'Database objects that automatically execute a specified block of SQL before or after INSERT, UPDATE, or DELETE events on a table — used for auditing, validation, or cascading changes.' },
    { term: 'Transactions (ACID)', definition: 'A unit of work that is Atomic (all or nothing), Consistent (maintains data integrity), Isolated (concurrent transactions don\'t interfere), and Durable (committed changes persist).' },
    { term: 'Normalization (1NF-3NF)', definition: 'The process of structuring tables to eliminate redundancy. 1NF requires atomic values, 2NF removes partial dependencies, and 3NF removes transitive dependencies.' },
    { term: 'EXPLAIN / Query Plan', definition: 'A command that shows the database engine\'s execution strategy for a query — revealing sequential scans, index usage, join methods, and estimated costs to identify performance bottlenecks.' },
    { term: 'Constraints (UNIQUE, CHECK, NOT NULL)', definition: 'Rules enforced at the database level: UNIQUE prevents duplicate values in a column, CHECK validates that values meet a condition, and NOT NULL forbids missing values.' },
  ],
  senior: [
    { term: 'Query Optimization (Index Strategy, Covering Index, Composite Index)', definition: 'Designing indexes to match query patterns: composite indexes cover multi-column WHERE/ORDER BY clauses, and covering indexes include all queried columns to avoid table lookups entirely.' },
    { term: 'Partitioning (Range, Hash, List)', definition: 'Splitting a large table into smaller physical segments. Range partitions by value intervals (e.g., dates), hash distributes evenly by hash function, and list partitions by explicit value sets.' },
    { term: 'Materialized Views', definition: 'Pre-computed query results stored as physical tables that can be refreshed on demand or on a schedule, trading storage space and freshness for dramatically faster reads on expensive queries.' },
    { term: 'Isolation Levels (READ COMMITTED, SERIALIZABLE, etc.)', definition: 'Transaction settings that control visibility of concurrent changes: READ UNCOMMITTED allows dirty reads, READ COMMITTED is the common default, REPEATABLE READ prevents non-repeatable reads, and SERIALIZABLE provides full isolation.' },
    { term: 'Deadlock Detection and Prevention', definition: 'Deadlocks occur when two transactions each hold locks the other needs. Databases detect them by identifying cycles in the wait-for graph and resolve them by rolling back one transaction.' },
    { term: 'Database Replication (Synchronous / Asynchronous)', definition: 'Copying data across database instances for availability and read scaling. Synchronous replication waits for confirmation from replicas (stronger consistency), while asynchronous replication is faster but risks lag.' },
    { term: 'Sharding Strategies', definition: 'Horizontally partitioning data across multiple database instances by a shard key (e.g., user ID). Strategies include hash-based, range-based, and geographic sharding, each with trade-offs in query routing and rebalancing.' },
    { term: 'Recursive CTEs', definition: 'CTEs that reference themselves to traverse hierarchical or graph-like data — e.g., org charts, bill-of-materials. Defined with an anchor member and a recursive member joined by UNION ALL.' },
    { term: 'JSON Functions in SQL', definition: 'Native operators for querying and manipulating JSON data stored in columns — e.g., PostgreSQL\'s ->> and jsonb_path_query, MySQL\'s JSON_EXTRACT() — enabling flexible semi-structured data within relational databases.' },
    { term: 'Full-Text Search', definition: 'Specialized indexing and querying for natural-language text using inverted indexes, tokenization, stemming, and ranking — e.g., PostgreSQL\'s tsvector/tsquery or MySQL\'s MATCH...AGAINST.' },
    { term: 'Row-Level Security', definition: 'A database feature (e.g., PostgreSQL RLS) that enforces access policies per row based on the current user or role, ensuring tenants or users can only see their own data without application-level filtering.' },
    { term: 'Database Migration Strategies', definition: 'Versioned, repeatable approaches to evolving a database schema over time — using tools like Flyway, Liquibase, or framework-specific migrations — with techniques such as expand-and-contract for zero-downtime changes.' },
  ],
}
