export default {
  beginner: [
    { term: 'Variable', definition: 'A name that refers to a value stored in memory. In Python, variables are dynamically typed — no explicit type declaration is needed.' },
    { term: 'Data Types', definition: 'Python\'s built-in types: int, float, str, bool, list, tuple, dict, set, and NoneType. Everything in Python is an object.' },
    { term: 'List', definition: 'An ordered, mutable collection of items accessed by index. Supports slicing, appending, and iteration.' },
    { term: 'Dictionary', definition: 'An insertion-ordered collection of key-value pairs accessed by key (guaranteed since Python 3.7). Keys must be hashable (strings, numbers, tuples).' },
    { term: 'Function (def)', definition: 'A reusable block of code defined with the def keyword that takes parameters and optionally returns a value.' },
    { term: 'Indentation', definition: 'Python uses whitespace indentation (typically 4 spaces) to define code blocks instead of braces, making structure part of the syntax.' },
    { term: 'Module', definition: 'A .py file containing Python code (functions, classes, variables) that can be imported into other files using the import statement.' },
    { term: 'pip', definition: 'Python\'s package installer that downloads and installs third-party libraries from the Python Package Index (PyPI).' },
    { term: 'String Formatting (f-strings)', definition: 'A syntax for embedding expressions inside string literals using f"..." with curly braces: f"Hello, {name}!".' },
    { term: 'Exception Handling', definition: 'Using try/except blocks to catch and handle errors gracefully instead of letting the program crash.' },
  ],
  mid: [
    { term: 'List Comprehension', definition: 'A concise syntax for creating lists: [expression for item in iterable if condition]. More readable than equivalent for-loops.' },
    { term: 'Generator', definition: 'A function that uses yield to produce values lazily one at a time, saving memory when processing large datasets.' },
    { term: 'Decorator', definition: 'A function that wraps another function to extend its behaviour without modifying its code, applied with the @decorator syntax.' },
    { term: 'Context Manager', definition: 'An object implementing __enter__ and __exit__ methods, used with the "with" statement for reliable resource management (files, connections).' },
    { term: 'Virtual Environment (venv)', definition: 'An isolated Python environment with its own packages and interpreter, preventing dependency conflicts between projects.' },
    { term: 'Type Hints', definition: 'Optional annotations (PEP 484) that specify expected types: def greet(name: str) -> str. Used by tools like mypy for static analysis.' },
    { term: 'OOP in Python', definition: 'Python supports classes with inheritance, polymorphism, encapsulation, and special (dunder) methods like __init__, __str__, and __repr__.' },
    { term: 'Lambda', definition: 'An anonymous, single-expression function: lambda x: x * 2. Often used with map(), filter(), and sorted().' },
    { term: 'async / await', definition: 'Python\'s syntax for writing asynchronous code using coroutines, enabling non-blocking I/O operations with asyncio.' },
    { term: 'Unit Testing (pytest)', definition: 'Writing automated tests using the pytest framework to verify individual functions and catch regressions early.' },
  ],
  senior: [
    { term: 'Metaclass', definition: 'A class whose instances are themselves classes. Metaclasses control class creation and can modify class behaviour at definition time.' },
    { term: 'Descriptor Protocol', definition: 'Objects implementing __get__, __set__, or __delete__ that customise attribute access — the mechanism behind property, classmethod, and staticmethod.' },
    { term: 'GIL (Global Interpreter Lock)', definition: 'A mutex in CPython that allows only one thread to execute Python bytecode at a time, limiting true parallelism in CPU-bound threaded code. Python 3.13+ offers an experimental free-threaded mode that removes the GIL.' },
    { term: 'Multiprocessing', definition: 'Running code in separate OS processes to bypass the GIL and achieve true parallelism for CPU-bound tasks.' },
    { term: 'C Extension', definition: 'A Python module written in C (or using Cython/pybind11) for performance-critical code, compiled into a shared library importable by Python.' },
    { term: 'Design Patterns', definition: 'Reusable solutions to common problems: Singleton, Factory, Observer, Strategy, etc. — applied idiomatically in Python.' },
    { term: 'Memory Management', definition: 'Python uses reference counting plus a cyclic garbage collector. Understanding __slots__, weakref, and memory profiling is key for large-scale applications.' },
    { term: 'Packaging & Distribution', definition: 'Creating installable packages using pyproject.toml, setuptools or Poetry, and publishing to PyPI for community or internal use.' },
    { term: 'Profiling & Optimisation', definition: 'Using tools like cProfile, line_profiler, and memory_profiler to identify bottlenecks and optimise hot paths.' },
    { term: 'Abstract Base Classes (ABC)', definition: 'Classes from the abc module that define interfaces — subclasses must implement abstract methods, enforcing contracts at instantiation time.' },
  ],
}
