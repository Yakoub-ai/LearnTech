export const topicQuizzes = {
  beginner: [
    {
      topicId: 'apis-what-they-are-and-why-every-service-has-one',
      topicTitle: 'APIs — What They Are and Why Every Service Has One',
      objectiveIndex: 4,
      questions: [
        {
          question: 'What is the primary purpose of an API (Application Programming Interface)?',
          options: [
            'To provide a formal contract so two pieces of software can communicate without exposing internal implementation details',
            'To give users direct access to a database so they can query it freely',
            'To replace the need for a backend server by running logic in the browser',
            'To store application state between requests on the client side',
          ],
          correctIndex: 0,
          explanation: 'An API is a formal agreement between two pieces of software about how they will communicate. It hides implementation details — the client only needs to know the agreed requests and response shapes, not how the server stores or processes data.',
        },
        {
          question: 'Which property of REST makes it easy to scale horizontally?',
          options: [
            'Statelessness — each request carries all information the server needs, so any replica can handle any request',
            'JSON formatting — binary data is too large for multiple servers to process efficiently',
            'A single endpoint — all traffic goes to one URL which simplifies load balancing',
            'Persistent connections — the server maintains a socket with each client',
          ],
          correctIndex: 0,
          explanation: "REST's statelessness means the server stores no session state between requests. Because every request is self-contained, any server replica can respond to any request, which is what makes horizontal scaling straightforward.",
        },
        {
          question: 'Why do real-time chat features require WebSockets rather than standard HTTP requests?',
          options: [
            'HTTP is a request-response protocol where the server cannot push data to the client unprompted; WebSockets maintain a persistent two-way connection',
            'HTTP does not support text data, only binary, making it unsuitable for chat messages',
            'WebSockets encrypt messages automatically whereas HTTP transmits everything in plain text',
            'HTTP connections time out after one second, which is too short for chat sessions',
          ],
          correctIndex: 0,
          explanation: "Standard HTTP requires the client to initiate every exchange. A WebSocket is like a phone call that never ends — once established, either side can send data at any time, which is what real-time features like chat require.",
        },
        {
          question: 'What does an HTTP 404 status code indicate?',
          options: [
            'The requested resource was not found on the server',
            'The server encountered an unexpected internal error',
            'The request succeeded and a new resource was created',
            'The client sent a request without valid authentication credentials',
          ],
          correctIndex: 0,
          explanation: '404 Not Found is a 4xx client error indicating that the server cannot locate the requested resource. Understanding status code classes (2xx success, 4xx client error, 5xx server error) is the first step to diagnosing production bugs from logs.',
        },
        {
          question: 'A mobile app and a web dashboard both need data from the same backend. What architectural benefit does an API provide in this scenario?',
          options: [
            'Both clients can consume the same API without duplicating server-side logic, because the API is a technology-agnostic contract',
            'The API automatically translates JSON into XML for the mobile app and HTML for the web dashboard',
            'The API stores separate copies of data for each client type to optimise response times',
            'The API ensures mobile and web users can never access the same resource simultaneously',
          ],
          correctIndex: 0,
          explanation: 'An API decouples the backend from any specific client. Whether the consumer is a mobile app, a web dashboard, or a third-party integration, the same endpoint serves the same data in the same format. This avoids duplicating business logic across client-specific backends.',
        },
        {
          question: 'What is the purpose of rate limiting on an API?',
          options: [
            'To prevent server overload by restricting how many requests a client can make within a time window',
            'To limit the size of the JSON response payload returned to the client',
            'To throttle the API so it responds no faster than a minimum latency',
            'To restrict which HTTP methods a client is allowed to use',
          ],
          correctIndex: 0,
          explanation: 'Rate limiting caps the number of requests a client can make in a given period (e.g., 100 requests per minute). This protects the server from abuse, accidental flooding from buggy clients, and denial-of-service scenarios where one consumer could degrade the experience for all others.',
        },
      ],
    },
    {
      topicId: 'programming-fundamentals-variables-data-types-and-control-flow',
      topicTitle: 'Programming Fundamentals – Variables, Data Types and Control Flow',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is a variable in a programming context?',
          options: [
            'A named location in memory that stores a value',
            'A function that transforms input data into output data',
            'A configuration file that controls program behaviour',
            'A connection string used to access a database',
          ],
          correctIndex: 0,
          explanation: 'A variable is a named location in memory that stores a value. The type of that value — integer, float, string, boolean — is its data type. Variables are the fundamental building blocks for storing and manipulating data in any program.',
        },
        {
          question: 'Which of the following best describes "control flow" in a program?',
          options: [
            'The order in which instructions execute, including branching with if/else and repeating with loops',
            'The speed at which data moves between the client and server over a network',
            'The method used to define the types of function parameters',
            'The mechanism for connecting multiple databases to a single application',
          ],
          correctIndex: 0,
          explanation: 'Control flow determines the order in which a program executes instructions. Rather than always running top to bottom, a program can branch (if/else) or loop (for/while), allowing it to respond differently based on conditions.',
        },
        {
          question: 'Why does understanding variable scope matter for backend security?',
          options: [
            'A variable defined inside a function is not accessible outside it, preventing sensitive data from leaking beyond its intended boundary',
            'Scope determines whether a variable is encrypted when stored in the database',
            'Variables with global scope are automatically validated against injection attacks',
            'Scope controls the HTTP methods a function can handle in a web framework',
          ],
          correctIndex: 0,
          explanation: 'Scope defines where in a program a variable can be accessed. A variable defined inside a function is not accessible outside it. Understanding scope prevents a class of security mistakes where sensitive data leaks beyond its intended boundary.',
        },
        {
          question: 'What is a common pitfall when using the assignment operator versus the equality operator?',
          options: [
            'Using = (assignment) when you meant == (equality comparison) produces incorrect conditional logic',
            'Using == instead of = causes the program to create an extra copy of the variable in memory',
            'The = operator only works with integers, not with strings or booleans',
            'Using == inside a loop causes an infinite loop because the condition never evaluates',
          ],
          correctIndex: 0,
          explanation: "Confusing = (assignment) with == (equality comparison) is one of the most common beginner mistakes. `if x = 5` assigns 5 to x rather than testing whether x equals 5, producing incorrect branching logic.",
        },
        {
          question: 'What is the difference between a for loop and a while loop?',
          options: [
            'A for loop iterates a known number of times (e.g., over a list); a while loop continues as long as a condition remains true, making it suitable when the number of iterations is unknown in advance',
            'A for loop can only iterate over numbers; a while loop can iterate over any data type including strings and lists',
            'A for loop always runs at least once; a while loop may never execute its body if the condition is false initially',
            'A for loop is faster than a while loop because the interpreter pre-computes the number of iterations',
          ],
          correctIndex: 0,
          explanation: 'A for loop is ideal when you know what you are iterating over — a list, a range of numbers, or a dictionary. A while loop is ideal when the exit condition depends on runtime logic — waiting for user input, reading until end-of-file, or retrying until a service responds.',
        },
      ],
    },
    {
      topicId: 'object-oriented-programming-classes-inheritance-and-encapsulation',
      topicTitle: 'Object-Oriented Programming – Classes, Inheritance and Encapsulation',
      objectiveIndex: 1,
      questions: [
        {
          question: 'Using the cookie-cutter analogy from the content, what does a class represent?',
          options: [
            'The blueprint (cookie cutter) that defines the shape and structure all instances will share',
            'A single running instance of the application with its own state',
            'The database schema that maps object properties to table columns',
            'The HTTP request body that carries data between the client and server',
          ],
          correctIndex: 0,
          explanation: 'A class is the blueprint — like a cookie cutter — that defines what data an object holds and what it can do. Each instance (each cookie stamped out) shares the same structure but has its own individual state.',
        },
        {
          question: 'What does encapsulation achieve in object-oriented design?',
          options: [
            'It hides the internal details of an object and exposes only what is necessary through a well-defined interface',
            'It allows a child class to inherit all methods and attributes from a parent class',
            'It enables multiple objects to respond to the same method call in different ways',
            'It converts object data to JSON format for transmission over HTTP',
          ],
          correctIndex: 0,
          explanation: 'Encapsulation hides internal implementation details and exposes only a well-defined interface. Data that cannot be reached externally cannot be tampered with externally — making encapsulation a direct precursor to secure coding.',
        },
        {
          question: 'When should you prefer composition over inheritance in object-oriented design?',
          options: [
            "When the relationship between classes is not a clear 'is-a' relationship, to avoid deep hierarchies that are hard to follow",
            'When you need to share state between multiple instances of the same class simultaneously',
            'When the child class must override every method from the parent class without exception',
            "When you want the child class to be stored in a separate database table from the parent",
          ],
          correctIndex: 0,
          explanation: "Inheritance models 'is-a' relationships. When the relationship is more like 'has-a' or 'uses-a', composition (building objects that contain other objects) produces more flexible, maintainable code than deep class hierarchies.",
        },
        {
          question: 'Which real-world backend constructs are examples of OOP in practice, as mentioned in the content?',
          options: [
            "Django's models, SQLAlchemy's mapped classes, and FastAPI's Pydantic models",
            'HTTP status codes, URL routing tables, and JSON serialisation formats',
            'Docker containers, environment variables, and CI/CD pipeline stages',
            'SQL transactions, database indexes, and foreign key constraints',
          ],
          correctIndex: 0,
          explanation: "Django's models, SQLAlchemy's mapped classes, and FastAPI's Pydantic models are all OOP in practice. Understanding classes, inheritance, and encapsulation is essential for reading, extending, and contributing to any substantial backend codebase.",
        },
        {
          question: 'What is polymorphism in object-oriented programming?',
          options: [
            'A way for objects of different types to respond to the same method call differently',
            'A pattern where a single object stores multiple types of data in one attribute',
            'A technique that converts objects between different programming languages at runtime',
            'The ability to create multiple instances of the same class simultaneously',
          ],
          correctIndex: 0,
          explanation: 'Polymorphism means "many forms." A method like `calculate_area()` can be called on a Circle, Rectangle, or Triangle — each class provides its own implementation. This allows code to work with objects generically (e.g., looping over shapes) without knowing the specific type at compile time.',
        },
      ],
    },
    {
      topicId: 'python-syntax-built-in-types-and-standard-library',
      topicTitle: 'Python – Syntax, Built-in Types and Standard Library',
      objectiveIndex: 2,
      questions: [
        {
          question: 'How does Python define code blocks, and what common pitfall does this introduce?',
          options: [
            'Python uses indentation to define code blocks; mixing tabs and spaces causes errors',
            'Python uses curly braces like JavaScript; forgetting to close a brace causes a syntax error',
            'Python uses semicolons to separate statements; omitting one causes an IndentationError',
            'Python uses the `begin` and `end` keywords; missing `end` causes an infinite loop',
          ],
          correctIndex: 0,
          explanation: 'Unlike languages that use braces, Python uses indentation to define code blocks. This keeps code visually clean but means that mixing tabs and spaces — which may look correct on screen — causes hard-to-diagnose errors.',
        },
        {
          question: 'What is a mutable default argument pitfall in Python function signatures?',
          options: [
            'Using a mutable default like `def foo(items=[])` causes the default list to be shared across all calls, accumulating state unexpectedly',
            'Using a mutable default prevents the function from accepting keyword arguments',
            'Mutable defaults are automatically deep-copied on each call, causing significant performance overhead',
            'Python raises a TypeError at definition time when a mutable object is used as a default argument',
          ],
          correctIndex: 0,
          explanation: 'In Python, default argument values are evaluated once at function definition, not on each call. A mutable default like `[]` is shared across all calls that use the default, so appending to it in one call affects all subsequent calls.',
        },
        {
          question: 'What is the difference between `==` and `is` in Python?',
          options: [
            '`==` checks value equality; `is` checks object identity (whether two names point to the exact same object in memory)',
            '`==` checks object identity; `is` checks whether two values are of the same type',
            '`is` is the correct operator for comparing strings; `==` should only be used for numbers',
            'They are interchangeable for all built-in types including lists, dicts, and strings',
          ],
          correctIndex: 0,
          explanation: '`==` tests whether two values are equal. `is` tests whether two variables refer to the exact same object in memory. Assuming they are interchangeable leads to subtle bugs, especially with strings and small integers that Python interns.',
        },
        {
          question: 'Why does Python\'s standard library reduce supply chain security risk?',
          options: [
            'Using well-tested standard library modules for common tasks (JSON parsing, file I/O, HTTP) reduces the number of third-party dependencies that could contain malicious code',
            'The standard library is written in C, which is immune to the classes of vulnerabilities found in Python code',
            'Standard library modules are automatically updated by the OS, unlike third-party packages which must be updated manually',
            'Python encrypts all data processed by standard library modules before sending it over the network',
          ],
          correctIndex: 0,
          explanation: "The standard library covers most common backend tasks without external dependencies. Every third-party dependency is a potential supply chain attack vector — using the standard library where it suffices reduces the attack surface.",
        },
        {
          question: 'What is a Python dictionary and when would you use one in a backend application?',
          options: [
            'A key-value data structure that provides O(1) average-case lookups; commonly used for mapping IDs to objects, storing configuration, and building JSON responses',
            'An ordered list of unique elements; commonly used for deduplicating database query results before returning them to the client',
            'A fixed-size array of bytes; commonly used for efficient binary data transfer between the server and the database',
            'A hierarchical tree structure; commonly used for representing nested URL routing tables in web frameworks',
          ],
          correctIndex: 0,
          explanation: 'A Python dict maps keys to values with average O(1) lookup time. In backend code, dicts are everywhere: parsing JSON request bodies produces dicts, building JSON responses uses dicts, and caching frequently accessed data in memory often uses a dict keyed by ID.',
        },
      ],
    },
    {
      topicId: 'http-the-requestresponse-cycle',
      topicTitle: 'HTTP – The Request/Response Cycle',
      objectiveIndex: 3,
      questions: [
        {
          question: 'What are the four main components of an HTTP request?',
          options: [
            'Method, URL, headers, and optionally a body',
            'Host, port, username, and password',
            'Status code, headers, body, and timestamp',
            'Version, payload, checksum, and response time',
          ],
          correctIndex: 0,
          explanation: 'An HTTP request consists of a method (what action to perform), a URL (which resource to act on), headers (metadata such as Content-Type or Authorization) and optionally a body (data sent to the server, typically for POST/PUT).',
        },
        {
          question: 'What is the difference between a 401 and a 403 HTTP status code?',
          options: [
            '401 Unauthorized means the request lacks valid authentication credentials; 403 Forbidden means the request is authenticated but the user does not have permission',
            '401 means the resource does not exist; 403 means the server encountered an internal error processing the request',
            '401 is a server error indicating the authentication service is down; 403 is a client error for sending malformed data',
            '401 and 403 are interchangeable; the distinction is purely conventional and has no technical significance',
          ],
          correctIndex: 0,
          explanation: '401 Unauthorized signals that the request lacks valid credentials — the client should authenticate. 403 Forbidden signals that the client is authenticated but does not have permission for this resource. Confusing them produces misleading API responses.',
        },
        {
          question: 'Why does HTTP\'s stateless nature make horizontal scaling possible?',
          options: [
            'Because each request is self-contained with all necessary information, any server replica can handle any request without needing shared session state',
            'Because HTTP compresses all request data, reducing the bandwidth needed to synchronise multiple servers',
            'Because stateless protocols automatically route requests to the server with the lowest current load',
            'Because HTTP version 2 introduced multiplexing, which allows one connection to serve multiple servers simultaneously',
          ],
          correctIndex: 0,
          explanation: "HTTP's statelessness means the server retains no memory of previous requests. Since every request carries all the information needed to process it, any replica in a load-balanced pool can handle any request — no sticky sessions required.",
        },
        {
          question: 'Which HTTP method should be used to partially update an existing resource?',
          options: [
            'PATCH — for partial updates to an existing resource',
            'POST — for all write operations including both creation and updates',
            'PUT — for partial updates; PATCH is reserved for full replacements',
            'GET — because updates should be encoded in the URL query string to be idempotent',
          ],
          correctIndex: 0,
          explanation: 'PATCH is the semantically correct method for partial updates. PUT replaces the entire resource. Using the wrong method signals poor API design and breaks clients that rely on correct method semantics for caching and retry logic.',
        },
        {
          question: 'What is the Content-Type header used for in an HTTP request?',
          options: [
            'It tells the server the format of the data in the request body, such as application/json or multipart/form-data',
            'It specifies the maximum size of the response the client can accept',
            'It indicates the programming language the client application was written in',
            'It defines the character encoding of the URL path',
          ],
          correctIndex: 0,
          explanation: 'The Content-Type header tells the server how to parse the request body. Sending JSON without setting Content-Type to application/json can cause the server to reject the request or misparse the data, leading to confusing 400 errors.',
        },
        {
          question: 'What does an HTTP 500 status code tell the client?',
          options: [
            'The client must resend the request with different headers',
            'The requested resource has been permanently moved to a new URL',
            'The server encountered an unexpected internal error while processing the request',
            'The client is not authenticated and must provide credentials',
          ],
          correctIndex: 2,
          explanation: '500 Internal Server Error is a generic server-side error indicating something went wrong that the server did not anticipate. Unlike 4xx errors which point to client mistakes, 5xx errors indicate the problem is on the server. In production, a 500 should trigger an alert because it usually means an unhandled exception.',
        },
      ],
    },
    {
      topicId: 'rest-apis-what-they-are-and-how-they-work',
      topicTitle: 'REST APIs – What They Are and How They Work',
      objectiveIndex: 4,
      questions: [
        {
          question: 'What does it mean for a REST API to be stateless?',
          options: [
            'All information needed to process a request must be contained within the request itself; the server stores no session state between requests',
            'The server never modifies any data — all operations are read-only to preserve the state of the database',
            'The API returns the same response regardless of authentication credentials provided',
            'Each resource URL remains constant and cannot change after the API is published',
          ],
          correctIndex: 0,
          explanation: 'REST statelessness means the server does not remember previous requests. Every request must include all the context needed — authentication tokens, resource identifiers, body data — so any server replica can handle it correctly.',
        },
        {
          question: 'How do HTTP methods map to CRUD operations in a RESTful API?',
          options: [
            'GET (read), POST (create), PUT/PATCH (update), DELETE (delete)',
            'GET (create), POST (read), PUT (delete), DELETE (update)',
            'POST (create and read), PUT (update and delete), GET (health check only)',
            'All CRUD operations use POST; the operation type is specified in the request body',
          ],
          correctIndex: 0,
          explanation: 'REST maps standard HTTP methods to CRUD operations: GET retrieves resources, POST creates them, PUT/PATCH updates them, and DELETE removes them. This uniform interface is what makes REST APIs predictable and easy to consume.',
        },
        {
          question: 'Which statement correctly describes the relationship between REST and HTTP?',
          options: [
            'REST is a set of architectural constraints on how to use HTTP — not a protocol of its own',
            'REST and HTTP are the same thing; REST is simply the official name for HTTP-based APIs',
            'REST is a newer version of HTTP that replaced HTTP/1.1 in modern web applications',
            'REST requires a separate binary transport layer beneath HTTP to achieve its performance guarantees',
          ],
          correctIndex: 0,
          explanation: 'REST (Representational State Transfer) is an architectural style — a set of constraints including statelessness, a uniform interface, and client-server separation — applied on top of HTTP. Not every HTTP API is RESTful.',
        },
        {
          question: 'What is the practical significance of idempotent HTTP methods like GET and PUT?',
          options: [
            'Calling them multiple times produces the same result as calling them once, making safe retries possible without risk of duplicate side effects',
            'They execute faster than non-idempotent methods because the server can cache the computation',
            'They require no authentication because their effects are reversible',
            'They can only be used with JSON request bodies; XML is not supported',
          ],
          correctIndex: 0,
          explanation: 'Idempotent methods produce the same result regardless of how many times they are called. GET, PUT, and DELETE are idempotent; POST is not. This property is important for safe client retries — retrying a GET after a network failure cannot cause harm.',
        },
        {
          question: 'What is the purpose of a resource identifier in a REST URL like /users/42?',
          options: [
            'It specifies the database engine to use for the query',
            '42 is the unique identifier of a specific user resource, allowing the server to locate and return that particular record',
            'It tells the server to return the first 42 results from the users collection',
            'It sets the cache duration for the response to 42 seconds',
          ],
          correctIndex: 1,
          explanation: 'In REST, URLs identify resources. /users is the collection, and /users/42 identifies one specific user. The server uses this identifier to look up the correct record. This convention makes APIs intuitive — consumers can predict how to access a specific resource without reading documentation.',
        },
      ],
    },
    {
      topicId: 'client-server-architecture',
      topicTitle: 'Client-Server Architecture',
      objectiveIndex: 5,
      questions: [
        {
          question: 'Why must the server always validate input data, even when the client has already validated it?',
          options: [
            'Client-side validation can be trivially bypassed by anyone with browser developer tools or a tool like curl, so server-side validation is the only reliable enforcement point',
            'Client-side validation introduces network latency, so the server must re-validate to correct any timing errors',
            'Browsers strip validation metadata from requests before sending them, so the server never receives the client\'s validation results',
            'Server validation is optional for internal applications where all clients are trusted employees',
          ],
          correctIndex: 0,
          explanation: "Any logic placed only on the client can be bypassed by anyone with browser dev tools or curl. The server is the single source of truth and must enforce every business rule and validate every input independently of what the client claims to have already checked.",
        },
        {
          question: 'In a typical web request, what happens after DNS resolves the domain to an IP address?',
          options: [
            'A TCP connection is established, the HTTP request is sent, the server processes it, and returns a response',
            'The browser immediately receives the HTML page from a global CDN cache without contacting the server',
            'The client sends the request directly to the database to fetch the required data',
            'The server pushes all available resources to the client before the client has sent any request',
          ],
          correctIndex: 0,
          explanation: 'After DNS resolution, the client establishes a TCP connection to the server\'s IP address, sends the HTTP request, the server (application layer) processes it applying business logic, and returns an HTTP response.',
        },
        {
          question: 'Why is it a security mistake to implement security checks only on the client side?',
          options: [
            'Because the server is where sensitive data lives and business logic executes — the server must never trust the client to have "already checked" anything',
            'Because modern browsers automatically disable JavaScript security checks to improve page load speed',
            'Because client-side security consumes too much CPU, degrading user experience on mobile devices',
            'Because security checks written in JavaScript cannot access the operating system APIs needed to enforce them',
          ],
          correctIndex: 0,
          explanation: 'The server is responsible for enforcing every business rule. A client that claims "I already validated this" cannot be trusted — the client is under the user\'s control. Relying on client-side checks is equivalent to having no server-side checks at all.',
        },
        {
          question: 'What is the correct role of the server in a client-server architecture?',
          options: [
            'The server stores data, enforces business rules, and responds to client requests; multiple clients can connect simultaneously and the server is the single source of truth',
            'The server only serves static files; all business logic runs in the client\'s browser using JavaScript',
            'The server initiates connections to clients to push updates whenever data changes',
            'The server delegates all data validation to the client and only handles database reads and writes',
          ],
          correctIndex: 0,
          explanation: 'The server runs application logic, enforces business rules, controls database access, and responds to requests. It never trusts the client. Multiple clients (web, mobile, third-party) all connect to the same server which is the authoritative source of truth.',
        },
        {
          question: 'What is the "three-tier architecture" commonly used in web applications?',
          options: [
            'A pattern with three servers: one for authentication, one for business logic, and one for logging',
            'A model where the application is split into frontend, middleware proxy, and CDN tiers',
            'A pattern that separates the presentation tier (client), application tier (server logic), and data tier (database)',
            'A deployment model using three identical server replicas behind a load balancer',
          ],
          correctIndex: 2,
          explanation: 'The three-tier architecture cleanly separates concerns: the presentation tier handles the user interface, the application tier runs business logic and processes requests, and the data tier persists and retrieves data. This separation allows each tier to be developed, scaled, and secured independently.',
        },
      ],
    },
    {
      topicId: 'databases-what-they-are-and-why-backends-need-them',
      topicTitle: 'Databases – What They Are and Why Backends Need Them',
      objectiveIndex: 6,
      questions: [
        {
          question: 'Why do backend applications require a database rather than storing data in server memory?',
          options: [
            'Data in memory is lost when the server process stops; a database provides durable storage that survives restarts',
            'Databases compress data automatically, reducing storage costs compared to in-memory storage',
            'Server memory is reserved for the operating system and cannot be used by application code',
            'Storing data in memory exposes it to SQL injection attacks; databases prevent this by design',
          ],
          correctIndex: 0,
          explanation: 'Without a database, any data processed by the server lives only in memory and is lost the moment the process stops. Databases provide persistence — data written survives server restarts, crashes, and deployments.',
        },
        {
          question: 'What is the key difference between a relational and a non-relational database?',
          options: [
            'Relational databases store data in structured tables with defined relationships; non-relational databases use flexible schemas with varied storage models such as documents or key-value pairs',
            'Relational databases store data on disk; non-relational databases store data only in memory and lose it on restart',
            'Relational databases are for read-only analytics; non-relational databases are for write-heavy transactional workloads',
            'Relational databases require a separate server; non-relational databases are embedded directly in the application process',
          ],
          correctIndex: 0,
          explanation: 'Relational databases (PostgreSQL, MySQL) organise data in structured tables with defined relationships enforced by keys. Non-relational databases (MongoDB, Redis) use flexible schemas — documents, key-value pairs, graphs — suited to different access patterns.',
        },
        {
          question: 'What is the role of a primary key in a relational database?',
          options: [
            'It uniquely identifies each row in a table, ensuring every record can be referenced without ambiguity',
            'It encrypts the row data to protect it from unauthorised access',
            'It determines the order in which rows are returned by a SELECT query',
            'It links two tables together by referencing the primary key of a related table',
          ],
          correctIndex: 0,
          explanation: 'A primary key uniquely identifies each row in a table. Every row needs a unique identifier so that foreign keys in other tables can reference it precisely, and so that update and delete operations target the correct record.',
        },
        {
          question: 'Why should you not use a relational database as a message queue or cache?',
          options: [
            'Dedicated tools like Redis (cache) and RabbitMQ (queue) are purpose-built for those access patterns and will outperform a relational database used outside its intended use case',
            'Relational databases do not support the DELETE operation needed to remove consumed messages from a queue',
            'Using a database as a cache violates the three-tier architecture and is prohibited by most cloud providers',
            'Message queues require a NoSQL database; relational databases cannot store the unstructured message payloads they contain',
          ],
          correctIndex: 0,
          explanation: 'Redis is optimised for low-latency key-value access and expiry-based caching. RabbitMQ and similar tools provide reliable message delivery semantics. Using a relational database for these patterns leads to polling overhead, locking contention, and poor performance.',
        },
        {
          question: 'What is a foreign key in a relational database?',
          options: [
            'A column that stores an encrypted version of the primary key for security purposes',
            'A special index that speeds up DELETE operations on child tables',
            'A column in one table that references the primary key of another table, creating a relationship between the two',
            'A unique constraint applied to multiple columns simultaneously to form a composite key',
          ],
          correctIndex: 2,
          explanation: 'A foreign key creates a link between two tables. For example, an orders table might have a user_id column that references the id column in the users table. The database enforces this relationship — you cannot insert an order referencing a user that does not exist.',
        },
      ],
    },
    {
      topicId: 'web-frameworks-fastapi-and-django',
      topicTitle: 'Web Frameworks — FastAPI and Django',
      objectiveIndex: 7,
      questions: [
        {
          question: 'What happens in the request lifecycle of a web framework when a client sends an HTTP request?',
          options: [
            'The framework matches the URL to a route, runs the handler function, and sends the HTTP response back to the client',
            'The framework forwards the raw request directly to the database without any routing logic',
            'The framework encrypts the request body before passing it to the application code for processing',
            'The framework checks the client\'s IP address against a whitelist before allowing any handler function to run',
          ],
          correctIndex: 0,
          explanation: 'When a client sends an HTTP request, the framework: (1) matches the URL to a route, (2) runs the corresponding handler function with the parsed request data, and (3) serialises the return value and sends it back as an HTTP response.',
        },
        {
          question: 'How does FastAPI use Python type hints to benefit API development?',
          options: [
            'It uses type hints and Pydantic models to automatically validate request data and generate interactive API documentation — if the client sends invalid data, FastAPI returns a clear error without custom validation code',
            'Type hints in FastAPI are purely cosmetic; the framework ignores them at runtime and relies on manual validation in each handler function',
            'FastAPI compiles type-hinted functions to C extensions at startup, which is the source of its performance advantage over Flask',
            'Type hints allow FastAPI to automatically generate SQL schemas from endpoint definitions without using a separate ORM',
          ],
          correctIndex: 0,
          explanation: 'FastAPI uses Python type hints combined with Pydantic to validate incoming request data automatically. Invalid requests are rejected with a clear 422 response before your handler code runs. The same type information is used to generate Swagger UI documentation at /docs.',
        },
        {
          question: 'When should you choose Django over FastAPI for a Python backend project?',
          options: [
            'When you need a complete web application with user management, an admin interface, server-rendered pages, and database interaction out of the box',
            'When you need to build a high-performance REST API with minimal overhead and automatic documentation generation',
            'When your project will exclusively serve mobile clients that only consume JSON and never need HTML pages',
            'When the team is experienced with FastAPI and wants to avoid learning a new framework for microservices',
          ],
          correctIndex: 0,
          explanation: "Django's 'batteries included' philosophy provides an ORM, admin interface, authentication, and form handling out of the box — making it the fastest path to a working application with user management. FastAPI is better when your primary goal is a lightweight, API-first service.",
        },
        {
          question: 'What is the Model-View-Template (MVT) pattern used by Django?',
          options: [
            'Models define database tables, Views handle request logic, and Templates render the HTML returned to the client',
            'Models define HTTP routes, Views contain the database schema, and Templates manage caching strategies',
            'Models handle authentication, Views serve static files, and Templates define the API response structure',
            'Models contain business rules, Views handle CSS styling, and Templates manage user session state',
          ],
          correctIndex: 0,
          explanation: "Django's MVT pattern separates concerns: Models define the data structure and database tables, Views contain the request handling and business logic, and Templates render HTML responses. For API-only projects, Django REST Framework adds serialisers and API views.",
        },
        {
          question: 'What is middleware in a web framework like Django or FastAPI?',
          options: [
            'A database driver that translates ORM queries into raw SQL for the configured database engine',
            'Code that runs before or after every request, used for cross-cutting concerns like logging, authentication, and CORS headers',
            'A testing utility that mocks external API calls during unit tests',
            'A template engine that converts server-side data into HTML before sending the response',
          ],
          correctIndex: 1,
          explanation: 'Middleware sits in the request/response pipeline and processes every request before it reaches the handler and every response before it reaches the client. Common uses include adding authentication checks, logging request metadata, setting CORS headers, and compressing response bodies.',
        },
        {
          question: 'What is routing in a web framework?',
          options: [
            'The process of configuring DNS records to point a domain name to the server IP address',
            'The mechanism that distributes requests across multiple server instances',
            'The mapping of URL patterns to handler functions that process the request and return a response',
            'The serialisation of Python objects into JSON format for the HTTP response body',
          ],
          correctIndex: 2,
          explanation: 'Routing is how the framework knows which function to call for a given URL. When a request arrives at /users/42, the router matches this pattern to the handler function responsible for retrieving a user by ID. Without routing, the framework would not know how to dispatch incoming requests.',
        },
      ],
    },
  ],
  mid: [
    {
      topicId: 'designing-apis-like-a-senior-engineer',
      topicTitle: 'Designing APIs Like a Senior Engineer',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is the core insight that separates a well-designed API from one that merely works?',
          options: [
            'An API is a contract — it must be predictable, consistent, and designed from the consumer\'s perspective rather than the implementation\'s perspective',
            'A well-designed API uses binary serialisation formats like Protocol Buffers instead of JSON to reduce payload size',
            'A well-designed API exposes every database table directly so consumers can construct any query they need',
            'A well-designed API version-locks all responses so consumers never need to handle multiple response shapes',
          ],
          correctIndex: 0,
          explanation: 'Senior engineers design APIs as contracts — not just working endpoints. The consumer experience (predictability, consistency, clear error messages) determines whether an API ages well or accumulates workarounds over time.',
        },
        {
          question: 'What problem does GraphQL\'s single-endpoint model solve that REST struggles with?',
          options: [
            'Over-fetching (receiving more data than needed) and under-fetching (needing multiple requests to build one view) when different clients have varying data requirements',
            'The inability of REST to support authentication, which GraphQL solves with its built-in token validation layer',
            'REST\'s lack of support for binary file uploads, which GraphQL handles natively through its mutation type',
            'The latency introduced by HTTP/1.1 keep-alive connections, which GraphQL avoids by using UDP',
          ],
          correctIndex: 0,
          explanation: 'GraphQL lets each client request exactly the fields it needs, solving over-fetching (too much data) and under-fetching (too little data requiring multiple round trips). This is particularly valuable when web and mobile clients need different data shapes from the same underlying resource.',
        },
        {
          question: 'Why is gRPC preferred over REST for high-throughput microservice-to-microservice communication?',
          options: [
            'gRPC uses Protocol Buffers (a binary format faster than JSON) and code generation, making it more efficient for internal service communication where human readability is less important',
            'gRPC uses plain text encoding which is faster to parse than JSON\'s nested structure',
            'gRPC runs over UDP instead of TCP, eliminating the overhead of connection establishment',
            'gRPC automatically encrypts all messages using AES-256, whereas REST transmits data in plain text',
          ],
          correctIndex: 0,
          explanation: "gRPC uses Protocol Buffers — a compact binary format — instead of JSON, and generates typed client/server code from a schema definition. This makes it significantly faster and more efficient than REST for internal service communication where developer ergonomics matter less than throughput.",
        },
        {
          question: 'What does the "design phase to deployment" principle mean for API design?',
          options: [
            'API design decisions should be made before writing code, not after — designing APIs after implementation means the API reflects the implementation rather than the consumer\'s needs',
            'APIs must be designed exclusively by senior engineers during the planning phase; junior engineers should only implement the decisions',
            'The design phase must produce a fully working prototype before any deployment pipeline can be configured',
            'All API endpoints must be deployed to a staging environment before the design can be considered final',
          ],
          correctIndex: 0,
          explanation: 'A common mistake is writing code first and treating the API shape as an afterthought. When you design the API after the implementation, the result reflects internal data structures and implementation details rather than what consumers actually need.',
        },
      ],
    },
    {
      topicId: 'restful-api-design-resources-verbs-and-status-codes',
      topicTitle: 'RESTful API Design – Resources, Verbs and Status Codes',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is the correct URL pattern for a RESTful endpoint that retrieves a specific order?',
          options: [
            '/orders/42 — plural noun for the collection, then the identifier',
            '/getOrder?id=42 — verb in the path to indicate the read operation',
            '/order/42 — singular noun because only one order is being retrieved',
            '/orders/getById/42 — action nested under the resource for clarity',
          ],
          correctIndex: 0,
          explanation: 'REST uses plural nouns for collections (`/orders`) and appends the identifier for specific resources (`/orders/42`). Actions are expressed through HTTP methods, not embedded in the path — `/getOrder` and `/createOrder` are anti-patterns.',
        },
        {
          question: 'Which status code should be returned after successfully creating a new resource, and what additional header should accompany it?',
          options: [
            '201 Created, with a Location header pointing to the URL of the new resource',
            '200 OK, with the new resource\'s ID included in the response body',
            '204 No Content, because the server has nothing to return after a creation operation',
            '202 Accepted, to indicate the creation is being processed asynchronously',
          ],
          correctIndex: 0,
          explanation: '201 Created signals successful resource creation and should include a Location header pointing to the new resource\'s URL (e.g., `Location: /orders/43`). Returning 200 for creates is a common API design mistake that makes it harder for clients to distinguish creation from retrieval.',
        },
        {
          question: 'Why is using a GET request that modifies server state considered an API design error?',
          options: [
            'GET must be safe and idempotent — clients, caches, and crawlers assume GET requests can be repeated without side effects, so a state-modifying GET will cause unintended consequences',
            'GET requests cannot carry a request body, so there is no way to specify which resource to modify',
            'GET responses are always cached by browsers, which would cause the modification to be skipped on subsequent requests',
            'The HTTP specification forbids GET requests from contacting the database layer of an application',
          ],
          correctIndex: 0,
          explanation: 'GET is defined as safe (no side effects) and idempotent (multiple identical calls produce the same result). Browsers, CDNs, and API clients rely on this guarantee — a state-modifying GET can be triggered accidentally by prefetching, crawlers, or retry logic.',
        },
        {
          question: 'What is the recommended maximum nesting depth for RESTful resource URLs?',
          options: [
            'One level of nesting — `/orders/42/items` is acceptable; deeper nesting like `/orders/42/items/7/details/history` creates overly complex URLs',
            'Three levels — this provides enough granularity for any real-world domain model',
            'No nesting at all — all resources should be top-level to keep URLs simple',
            'Unlimited nesting — each additional level adds precision and makes the API more self-documenting',
          ],
          correctIndex: 0,
          explanation: 'Nest resources only one level deep. `/orders/42/items` is a clean way to express items belonging to an order. Deeper nesting like `/orders/42/items/7/details/history` becomes difficult to read and usually indicates the design should be restructured.',
        },
      ],
    },
    {
      topicId: 'api-versioning-and-documentation',
      topicTitle: 'API Versioning and Documentation',
      objectiveIndex: 1,
      questions: [
        {
          question: 'Which API versioning strategy is the most visible and easiest to test?',
          options: [
            'URL path versioning — `/v1/users` — because the version is explicit in the URL and can be tested directly in a browser or with curl',
            'Header versioning — `Accept: application/vnd.api.v1+json` — because it keeps URLs clean and does not pollute the resource identifier',
            'Query parameter versioning — `?version=1` — because it requires no changes to the base URL and is backward compatible',
            'Semantic versioning in the response body — because it allows clients to detect the version from the response without any configuration',
          ],
          correctIndex: 0,
          explanation: 'URL path versioning (`/v1/users`) is the most common and easiest to work with. The version is immediately visible in logs, browser address bars, and curl commands — no special headers or query parameters needed to test it.',
        },
        {
          question: 'Which of the following is a breaking change that requires incrementing the API version?',
          options: [
            'Removing a field from a response body — existing clients that read that field will break',
            'Adding a new optional field to a response body — existing clients that ignore unknown fields will not be affected',
            'Adding a new endpoint to the API — existing clients are not aware of the new endpoint so cannot break',
            'Adding a new optional query parameter — existing clients that do not send it will receive the same default behaviour',
          ],
          correctIndex: 0,
          explanation: 'Removing a response field, changing a field\'s type, changing a status code\'s meaning, or removing an endpoint are breaking changes — existing clients will break. Adding new optional fields, parameters, or endpoints is non-breaking and does not require a version bump.',
        },
        {
          question: 'What is OpenAPI (formerly Swagger) and why does it matter for API teams?',
          options: [
            'A standard machine-readable format for describing REST APIs that enables automatic documentation generation, client code generation, and contract testing',
            'An open-source web framework for Python that generates REST endpoints from database schemas automatically',
            'A security protocol that verifies the identity of API consumers before granting access to protected endpoints',
            'A load balancing tool that distributes API traffic across multiple server instances based on version headers',
          ],
          correctIndex: 0,
          explanation: 'OpenAPI is a standard for describing REST APIs in a machine-readable YAML or JSON format. It enables tools to automatically generate interactive documentation (like Swagger UI), generate typed client SDKs, and run contract tests that verify the API matches its specification.',
        },
        {
          question: 'What is the main problem with maintaining API documentation separately from the code?',
          options: [
            'The documentation and code inevitably drift apart — the docs describe an API that no longer matches the actual implementation',
            'Separate documentation files cannot be committed to version control alongside the code',
            'Maintaining separate docs requires a dedicated technical writer, adding headcount costs',
            'Separate documentation cannot be indexed by search engines, reducing its discoverability',
          ],
          correctIndex: 0,
          explanation: 'Documentation maintained separately from code requires manual synchronisation after every code change. In practice, this synchronisation is skipped under time pressure, causing the docs to describe endpoints, fields, and behaviours that no longer exist in the actual API.',
        },
      ],
    },
    {
      topicId: 'authentication-and-authorisation-oauth-20-openid-connect-and-jwt',
      topicTitle: 'Authentication and Authorisation – OAuth 2.0, OpenID Connect and JWT',
      objectiveIndex: 4,
      questions: [
        {
          question: 'What is the critical distinction between authentication and authorisation?',
          options: [
            'Authentication verifies who a user is; authorisation decides what an authenticated user is allowed to do',
            'Authentication grants access to resources; authorisation verifies the user\'s identity via a token',
            'Authentication is performed by the client; authorisation is performed by the database',
            'Authentication uses JWTs; authorisation uses session cookies — they are different token formats for the same concept',
          ],
          correctIndex: 0,
          explanation: 'Authentication answers "who are you?" — verifying identity. Authorisation answers "what are you allowed to do?" — enforcing permissions. Conflating them leads to systems that verify identity but fail to enforce access control.',
        },
        {
          question: 'What does a JWT payload contain, and why should sensitive data never be placed there?',
          options: [
            'Claims such as `sub`, `exp`, `iat`, and roles — but the payload is only base64-encoded, not encrypted, so anyone who holds the token can read it',
            'An encrypted blob containing the user\'s full profile, readable only by the server that issued the token',
            'A cryptographic hash of the user\'s password, used to verify identity without transmitting credentials',
            'The server\'s public key certificate, which clients use to verify the token\'s authenticity',
          ],
          correctIndex: 0,
          explanation: 'A JWT has three parts: header (algorithm), payload (claims), and signature. The payload is base64-encoded — trivially decodable by anyone. Only the signature is cryptographically protected. Storing sensitive data like passwords or credit card numbers in the payload exposes it to anyone who intercepts or holds the token.',
        },
        {
          question: 'Why is the Implicit OAuth 2.0 grant type deprecated and no longer recommended?',
          options: [
            'It returned access tokens directly in the URL fragment, exposing them in browser history and server logs; the Authorization Code with PKCE flow is the secure replacement for public clients',
            'It required the client to store a client secret, which is impossible to secure in a mobile application',
            'It used symmetric encryption that is vulnerable to brute-force attacks with modern hardware',
            'It relied on third-party cookies which are blocked by modern browsers by default',
          ],
          correctIndex: 0,
          explanation: 'The Implicit flow returned access tokens directly in the redirect URL fragment, making them visible in browser history, referrer headers, and server logs. The Authorization Code flow with PKCE achieves the same goal (no client secret required) without exposing tokens in URLs.',
        },
        {
          question: 'What does OAuth 2.0 grant and what does OpenID Connect add on top of it?',
          options: [
            'OAuth 2.0 grants authorisation (access to resources); OpenID Connect adds authentication by providing an ID token that contains the user\'s verified identity',
            'OAuth 2.0 provides authentication (who the user is); OpenID Connect adds authorisation (what they can do) through role-based scopes',
            'OAuth 2.0 manages session tokens; OpenID Connect manages API keys for machine-to-machine communication',
            'OAuth 2.0 is an encryption protocol; OpenID Connect is a compression protocol that reduces token size',
          ],
          correctIndex: 0,
          explanation: "OAuth 2.0 answers 'has this user granted access?' but does not tell you who the user is. OpenID Connect adds an ID token (a JWT containing identity claims) on top of OAuth 2.0, making it an authentication protocol. Using an OAuth access token alone as proof of identity is a security mistake.",
        },
        {
          question: 'What is the purpose of a refresh token, and why should it be stored differently from an access token?',
          options: [
            'A refresh token is used to obtain a new access token without re-authenticating the user; it must be stored securely (e.g., in an HTTP-only cookie or server-side session) because it has a longer lifespan and grants the ability to mint new access tokens',
            'A refresh token encrypts the access token for transport over insecure networks; both tokens can be stored in localStorage because they are encrypted',
            'A refresh token is a backup copy of the access token used when the primary token is corrupted during network transmission',
            'A refresh token revokes all previously issued access tokens; it is stored in the URL query string so the server can read it on every request',
          ],
          correctIndex: 0,
          explanation: 'Access tokens are short-lived (minutes to hours) to limit damage if stolen. When they expire, the refresh token obtains a new one without forcing the user to log in again. Because a refresh token effectively grants long-lived access, storing it in localStorage (accessible to XSS attacks) is a critical vulnerability — it should be in an HTTP-only, secure cookie or stored server-side.',
        },
        {
          question: 'Why is storing session state inside a JWT considered an anti-pattern for most applications?',
          options: [
            'JWTs are immutable once signed — you cannot invalidate or update the session without waiting for the token to expire, making instant logout and permission changes impossible without additional server-side infrastructure',
            'JWTs are limited to 256 bytes of payload, which is too small to store meaningful session data like user preferences',
            'JWTs transmit session data in plaintext HTTP headers, which violates GDPR requirements for encrypting personal data in transit',
            'JWTs require a shared secret across all services, and rotating that secret invalidates every active session simultaneously',
          ],
          correctIndex: 0,
          explanation: 'A signed JWT cannot be modified or revoked after issuance. If a user changes their role or is banned, the JWT still carries the old claims until it expires. To support instant invalidation, you need a server-side revocation list or token blacklist — at which point you have re-invented server-side sessions with extra complexity. Use JWTs for short-lived, stateless authentication tokens, not as general-purpose session stores.',
        },
      ],
    },
    {
      topicId: 'relational-databases-tables-keys-and-normalisation',
      topicTitle: 'Relational Databases – Tables, Keys and Normalisation',
      objectiveIndex: 2,
      questions: [
        {
          question: 'What does Third Normal Form (3NF) require that Second Normal Form (2NF) does not?',
          options: [
            '3NF additionally requires that no non-key column depends on another non-key column — eliminating transitive dependencies',
            '3NF requires that every table have a composite primary key instead of a single-column key',
            '3NF requires that all data types be numeric; string columns are only permitted in first normal form',
            '3NF additionally requires that foreign key constraints be enforced with CASCADE DELETE rules',
          ],
          correctIndex: 0,
          explanation: '2NF requires that every non-key column depends on the whole primary key. 3NF goes further: no non-key column may depend on another non-key column (transitive dependency). 3NF eliminates update anomalies where changing one piece of data in one row requires changes across many rows.',
        },
        {
          question: 'How do database indexes improve read performance, and what is their cost?',
          options: [
            'Indexes allow the database engine to find matching rows without scanning the entire table, dramatically speeding up reads — at the cost of slightly slower writes and additional storage',
            'Indexes compress table data to fit more rows in memory, improving read speed at the cost of CPU time for decompression',
            'Indexes replicate the table across multiple database nodes, improving read throughput at the cost of network bandwidth',
            'Indexes cache query results in memory, improving read speed at the cost of returning stale data until the cache expires',
          ],
          correctIndex: 0,
          explanation: 'An index is a separate data structure (typically a B-tree) that maps column values to row locations. It allows the database to jump directly to matching rows rather than scanning every row. The trade-off: each index must be updated on every INSERT, UPDATE, and DELETE.',
        },
        {
          question: 'What does a foreign key constraint enforce in a relational database?',
          options: [
            'Referential integrity — you cannot insert a row referencing a non-existent parent row, and you cannot delete a parent row that has dependent children unless cascade rules permit it',
            'Data encryption — the foreign key column is automatically encrypted using the parent table\'s primary key as the encryption key',
            'Query performance — the database uses the foreign key to automatically create an index on the referencing column',
            'Column uniqueness — no two rows in the child table can reference the same parent row',
          ],
          correctIndex: 0,
          explanation: 'A foreign key constraint is the database enforcing your business rules. It prevents orphaned data by rejecting inserts that reference non-existent parents and rejecting deletes of parents that have dependent children (unless CASCADE rules allow it).',
        },
        {
          question: 'Why is storing comma-separated values in a single column considered a schema design anti-pattern?',
          options: [
            'It violates First Normal Form (1NF) by storing non-atomic values, making it impossible to filter, join, or aggregate on individual values without complex string parsing',
            'Comma-separated values take more storage space than normalised rows because the delimiter characters add overhead',
            'Most relational databases cannot store strings containing commas without escaping them, causing data corruption',
            'Comma-separated columns cannot be indexed, so any query filtering on individual values will always perform a full table scan',
          ],
          correctIndex: 0,
          explanation: '1NF requires each column to hold atomic (indivisible) values. Storing comma-separated values means you cannot filter for a specific value using standard SQL operators, cannot enforce referential integrity on individual values, and must do expensive string manipulation for any query touching those values.',
        },
      ],
    },
    {
      topicId: 'sql-queries-joins-indexes-and-transactions',
      topicTitle: 'SQL – Queries, Joins, Indexes and Transactions',
      objectiveIndex: 2,
      questions: [
        {
          question: 'What is the difference between WHERE and HAVING in a SQL query?',
          options: [
            'WHERE filters individual rows before grouping; HAVING filters groups after aggregation with GROUP BY',
            'WHERE filters columns to include in the result; HAVING filters rows based on the primary key value',
            'WHERE is used with SELECT statements; HAVING is used exclusively with UPDATE and DELETE statements',
            'WHERE executes on the database server; HAVING executes on the application server after the result set is returned',
          ],
          correctIndex: 0,
          explanation: 'WHERE is applied before GROUP BY, filtering which rows are included in the grouping. HAVING is applied after GROUP BY, filtering which groups appear in the result. You cannot use aggregate functions (COUNT, SUM) in a WHERE clause — that is what HAVING is for.',
        },
        {
          question: 'What type of JOIN returns all rows from the left table plus matched rows from the right table, with NULL for unmatched right-side columns?',
          options: [
            'LEFT JOIN (also called LEFT OUTER JOIN)',
            'INNER JOIN',
            'FULL OUTER JOIN',
            'CROSS JOIN',
          ],
          correctIndex: 0,
          explanation: 'A LEFT JOIN returns every row from the left table. For rows with a matching row in the right table, the right-side columns are populated. For rows with no match, the right-side columns are NULL. This is useful for finding records in one table that have no corresponding record in another.',
        },
        {
          question: 'What are the ACID properties of a database transaction and why do they matter?',
          options: [
            'Atomicity (all-or-nothing), Consistency (valid state before and after), Isolation (concurrent transactions do not interfere), Durability (committed data survives failures) — together they ensure data integrity under failures',
            'Authentication, Compression, Indexing, and Distribution — the four components of a high-performance database query execution plan',
            'Availability, Consistency, Integrity, and Durability — the same as the CAP theorem but applied to individual rows rather than distributed nodes',
            'Atomicity (each query is a single operation), Caching (results are cached), Indexing (all columns are indexed), and Deletion (old rows are purged automatically)',
          ],
          correctIndex: 0,
          explanation: 'ACID guarantees that transactions are safe under concurrent access and failures. Atomicity ensures all statements in a transaction succeed or all are rolled back. Isolation ensures concurrent transactions do not corrupt each other\'s data. Durability ensures committed data persists even after a crash.',
        },
        {
          question: 'Why is concatenating user input directly into a SQL query a critical security vulnerability?',
          options: [
            'It enables SQL injection — an attacker can inject malicious SQL that executes with the database\'s full permissions, potentially exfiltrating or destroying all data',
            'It prevents the database from using indexes on the query, causing full table scans and severe performance degradation',
            'It violates the SQL standard, causing the query to return unpredictable results across different database engines',
            'It doubles the memory usage of the query because the input string is parsed twice by the database engine',
          ],
          correctIndex: 0,
          explanation: 'SQL injection is one of the most damaging attack types. If user input like `\' OR 1=1 --` is concatenated into a query, the attacker can bypass WHERE clauses, extract entire tables, or drop databases. Parameterised queries prevent this by separating SQL code from data.',
        },
        {
          question: 'A query that works fine in development suddenly takes 30 seconds in production. The only difference is that the production table has 5 million rows instead of 500. What is the most likely cause?',
          options: [
            'The production server has a slower CPU and cannot parse SQL as quickly as the development machine',
            'The query is missing an index on the column used in the WHERE clause, forcing a full table scan on 5 million rows',
            'Production databases disable query caching by default, whereas development databases cache all query results in memory',
            'The query uses a LEFT JOIN, which is inherently O(n²) and should be replaced with a subquery for large tables',
          ],
          correctIndex: 1,
          explanation: 'Without an index on the filtered column, the database must examine every row in the table (a full table scan). At 500 rows this is imperceptible; at 5 million rows it becomes catastrophic. Adding an index on the WHERE clause column allows the database to locate matching rows in O(log n) time instead of O(n), often reducing the query from seconds to milliseconds.',
        },
        {
          question: 'What is the difference between a database deadlock and a lock wait timeout, and how should your application handle each?',
          options: [
            'A deadlock occurs when two transactions each hold a lock the other needs, creating a cycle that can never resolve; a lock wait timeout occurs when a transaction waits too long for a single lock — deadlocks require retry logic, timeouts may indicate a long-running transaction that needs investigation',
            'A deadlock only occurs with row-level locks; a lock wait timeout only occurs with table-level locks — they require different isolation levels to resolve',
            'A deadlock is a permanent failure that requires a database restart; a lock wait timeout resolves itself when the blocking transaction commits',
            'A deadlock prevents all reads and writes to the database; a lock wait timeout only prevents writes while reads continue unaffected',
          ],
          correctIndex: 0,
          explanation: 'A deadlock is a circular dependency: Transaction A holds Lock 1 and waits for Lock 2, while Transaction B holds Lock 2 and waits for Lock 1. The database detects this and kills one transaction. Your application should catch the deadlock error and retry. A lock wait timeout means a transaction waited too long for a lock held by another — this often signals a long-running query or missing index that should be investigated.',
        },
      ],
    },
    {
      topicId: 'web-security-owasp-top-10-for-backend-developers',
      topicTitle: 'Web Security – OWASP Top 10 for Backend Developers',
      objectiveIndex: 3,
      questions: [
        {
          question: 'Which OWASP Top 10 category is ranked A01 and considered the most common critical finding?',
          options: [
            'Broken Access Control — users able to access resources or actions they are not authorised to access',
            'Injection — unsanitised user input interpreted as a database command',
            'Cryptographic Failures — transmitting sensitive data without adequate encryption',
            'Security Misconfiguration — default credentials left in place and unnecessary services exposed',
          ],
          correctIndex: 0,
          explanation: 'A01 Broken Access Control is the top-ranked risk. It occurs when authorisation checks are missing or incorrect, allowing users to access resources or perform actions beyond their permissions. The fix is to enforce authorisation checks server-side on every request.',
        },
        {
          question: 'What does OWASP A10 – Server-Side Request Forgery (SSRF) describe?',
          options: [
            'The server makes HTTP requests to a URL supplied or influenced by the user, potentially reaching internal services or cloud metadata endpoints not intended to be public',
            'An attacker forges the server\'s SSL certificate to intercept HTTPS traffic between the server and its clients',
            'The server sends requests on behalf of one user that actually modify another user\'s data due to missing session validation',
            'An attacker forces the server to serve malicious JavaScript by injecting it into server-side rendered HTML templates',
          ],
          correctIndex: 0,
          explanation: 'SSRF occurs when the server fetches a URL that the attacker controls. The attacker can point it to internal services (e.g., `http://169.254.169.254/latest/meta-data/` for AWS credentials), bypassing network-level access controls because the request originates from a trusted server.',
        },
        {
          question: 'Why should passwords be hashed with bcrypt or Argon2 rather than MD5 or SHA-1?',
          options: [
            'bcrypt and Argon2 are purpose-built password hashing algorithms with a tunable work factor that makes brute-force attacks computationally expensive; MD5 and SHA-1 are fast hashing algorithms that can be brute-forced at billions of attempts per second',
            'bcrypt and Argon2 encrypt the password so it can be decrypted for comparison; MD5 and SHA-1 only produce one-way hashes that cannot be compared',
            'MD5 and SHA-1 produce hashes that are too short (16 and 20 bytes) to store in a VARCHAR column; bcrypt always produces a 60-character string that fits comfortably',
            'bcrypt and Argon2 automatically salt passwords with a server-specific key that prevents the hash from being compared across servers',
          ],
          correctIndex: 0,
          explanation: 'MD5 and SHA-1 are fast — an attacker with a GPU can test billions of guesses per second. bcrypt and Argon2 are slow by design; the work factor can be increased as hardware improves. A bcrypt hash that takes 100ms to verify can take centuries to brute-force.',
        },
        {
          question: 'What is the OWASP A06 risk and how should it be mitigated in a CI/CD pipeline?',
          options: [
            'Vulnerable and Outdated Components — using dependencies with published CVEs; mitigate by running automated dependency scanning tools like `pip-audit` or Dependabot in CI and applying updates regularly',
            'Software and Data Integrity Failures — not verifying the integrity of code during deployment; mitigate by signing every commit with GPG keys before merging',
            'Identification and Authentication Failures — weak session management; mitigate by enforcing multi-factor authentication for all CI/CD pipeline triggers',
            'Security Logging and Monitoring Failures — insufficient audit trails; mitigate by archiving all build logs to an immutable storage bucket',
          ],
          correctIndex: 0,
          explanation: 'A06 covers outdated dependencies with known vulnerabilities. Automated scanning tools (`pip-audit`, Dependabot, Snyk) integrated into CI pipelines flag vulnerable packages before they reach production. The fix is to update dependencies regularly and not ignore scanner alerts.',
        },
      ],
    },
    {
      topicId: 'graphql-schema-resolvers-and-when-to-use-it',
      topicTitle: 'GraphQL – Schema, Resolvers and When to Use It',
      objectiveIndex: 5,
      questions: [
        {
          question: 'What are the three operation types in a GraphQL schema?',
          options: [
            'Query (fetch data), Mutation (modify data), Subscription (real-time event stream)',
            'GET (fetch data), POST (modify data), PATCH (partial update)',
            'Select (read), Insert (create), Delete (remove)',
            'Request (initiate), Response (receive), Stream (subscribe)',
          ],
          correctIndex: 0,
          explanation: 'GraphQL defines three operation types: Query for reading data, Mutation for creating, updating, or deleting data, and Subscription for establishing a real-time event stream. This differs from REST where the HTTP method (GET, POST, PUT, DELETE) determines the operation type.',
        },
        {
          question: 'What is the N+1 problem in GraphQL and how does DataLoader solve it?',
          options: [
            'Each field resolver runs independently, so fetching a list of N items triggers N additional database queries (one per item); DataLoader batches all those queries into a single database call',
            'GraphQL executes N+1 times slower than REST because the schema parser adds overhead proportional to the number of fields requested',
            'N+1 refers to GraphQL\'s limit of N nested query levels plus one subscription; DataLoader extends this limit by pre-fetching nested data',
            'The N+1 problem occurs when a client sends duplicate queries; DataLoader deduplicates them by comparing query hashes',
          ],
          correctIndex: 0,
          explanation: "In a naive GraphQL implementation, resolving `author` for each of N posts triggers one database query per post — N+1 total queries. DataLoader collects all the IDs requested during a single execution tick and issues a single batched query, then distributes the results back to each resolver.",
        },
        {
          question: 'Why does GraphQL\'s use of HTTP POST for all queries complicate caching?',
          options: [
            'Standard HTTP GET-based caching (CDNs, browser cache, proxy cache) does not apply to POST requests by default, so GraphQL APIs need application-level caching strategies',
            'POST requests are rejected by most corporate firewalls, forcing GraphQL to fall back to GET requests which are slower',
            'HTTP POST limits request body size to 64KB, which prevents complex GraphQL queries from being sent',
            'CDNs cannot parse GraphQL query syntax so they cache all responses with the same key, causing cache collisions',
          ],
          correctIndex: 0,
          explanation: 'HTTP caching infrastructure (browsers, CDNs, reverse proxies) caches GET requests by URL. Since GraphQL sends all queries as POST requests to a single endpoint, these standard caching layers cannot differentiate between queries — you need response caching at the application level instead.',
        },
        {
          question: 'In which scenario should you prefer REST over GraphQL?',
          options: [
            'A simple CRUD API with few consumer types where the added complexity of schema management and resolver implementation is not justified',
            'When multiple mobile and web clients each need a different subset of the same data to avoid over-fetching',
            'When the API needs to support real-time event streams using the Subscription operation type',
            'When strong typing and auto-generated client SDKs are more important than backward compatibility',
          ],
          correctIndex: 0,
          explanation: 'GraphQL adds complexity — schema definition, resolver implementation, N+1 mitigation, and query depth limiting. For a simple CRUD API with one or two consumer types, REST is simpler to build, document, and cache. GraphQL pays off when you have multiple clients with genuinely different data needs.',
        },
        {
          question: 'Why is query depth limiting important in a public-facing GraphQL API?',
          options: [
            'Without depth limiting, a malicious client can craft a deeply nested query that causes exponential resolver execution, consuming server resources and potentially causing a denial-of-service',
            'Deep queries return too much data for the client to parse, causing the browser to crash due to memory exhaustion',
            'GraphQL schemas cannot represent relationships deeper than five levels, so depth limiting prevents schema validation errors',
            'Deep queries bypass the authentication middleware because each nested level runs in a separate execution context',
          ],
          correctIndex: 0,
          explanation: 'A GraphQL API that exposes circular relationships (user -> friends -> friends -> friends -> ...) can be exploited by a query that nests dozens of levels deep, triggering an exponential number of resolver calls. Query depth limiting rejects queries exceeding a configured depth before execution begins, preventing this denial-of-service vector.',
        },
        {
          question: 'What is a GraphQL fragment and what problem does it solve for client developers?',
          options: [
            'A fragment is a reusable set of fields that can be included in multiple queries, eliminating field duplication when several queries or components need the same subset of a type\'s fields',
            'A fragment is a server-side cache partition that stores pre-computed query results for frequently requested field combinations',
            'A fragment is a security boundary that restricts which fields a specific API consumer is allowed to request based on their access token scope',
            'A fragment is a pagination cursor that splits large result sets into smaller chunks for incremental loading on the client',
          ],
          correctIndex: 0,
          explanation: 'Fragments let you define a reusable set of fields once — for example, `fragment UserBasic on User { id, name, email }` — and spread it into multiple queries or mutations with `...UserBasic`. This avoids duplicating the same field list across queries and keeps client code DRY, especially when multiple UI components need the same data shape from the same type.',
        },
      ],
    },
    {
      topicId: 'system-design-basics-load-balancing-caching-and-queues',
      topicTitle: 'System Design Basics – Load Balancing, Caching and Queues',
      objectiveIndex: 7,
      questions: [
        {
          question: 'What is the difference between a message queue and a pub/sub event bus?',
          options: [
            'A queue delivers each message to exactly one consumer; a pub/sub bus delivers each message to all subscribers',
            'A queue is synchronous and blocks the producer until the consumer acknowledges; a pub/sub bus is always asynchronous',
            'A queue stores messages in memory only; a pub/sub bus persists messages to disk for replay',
            'A queue is used for inter-service communication; a pub/sub bus is used only for browser-to-server communication',
          ],
          correctIndex: 0,
          explanation: "In a message queue (like RabbitMQ or SQS), each message is delivered to one consumer — work queue semantics. In a pub/sub bus (like Kafka topics or SNS), each message is delivered to all subscribers — broadcast semantics. Choosing between them depends on whether the event represents work to be done once or a notification that many services need.",
        },
        {
          question: 'What is idempotency in message consumers, and why is it required?',
          options: [
            'A message may be delivered more than once (at-least-once delivery); an idempotent consumer handles duplicate messages safely without producing duplicate side effects',
            'An idempotent consumer processes messages in the exact order they were produced, without reordering',
            'Idempotency means the consumer acknowledges every message immediately, even before processing it, to prevent queue backlog',
            'An idempotent consumer batches all messages received within a time window and processes them as a single transaction',
          ],
          correctIndex: 0,
          explanation: 'Most message brokers guarantee at-least-once delivery — a message may be redelivered after a network failure or consumer crash. An idempotent consumer produces the same result whether it processes a message once or multiple times (e.g., using the message ID to detect and discard duplicates).',
        },
        {
          question: 'What is a cache stampede and how can it be prevented?',
          options: [
            'When the cache is cold (after a restart or flush), many simultaneous requests all miss and flood the database at once; prevention strategies include locking (only one request rebuilds the cache) or staggered TTLs',
            'When TTL values are set too short, the cache evicts entries before they are used, causing requests to permanently bypass caching',
            'When two services write different values for the same cache key simultaneously, causing corrupted data to be served',
            'When a cache entry grows too large and consumes all available memory, evicting all other entries at once',
          ],
          correctIndex: 0,
          explanation: 'After a cache flush or restart, the first wave of traffic hits empty cache entries and all requests fall through to the database simultaneously. This thundering herd can overwhelm the database. Solutions include mutex locking on cache population, probabilistic early expiry, or warming the cache before traffic is routed to the instance.',
        },
        {
          question: 'Why do message queues improve system resilience when a downstream service is slow?',
          options: [
            'The queue decouples the producer from the consumer — the fast producer writes to the queue at its own rate while the slow consumer processes at its pace, preventing backpressure from cascading upstream',
            'The queue compresses messages to reduce the payload size that the slow consumer must deserialise',
            'The queue automatically retries failed messages with exponential backoff, giving the slow consumer time to recover',
            'The queue routes messages to the fastest available consumer instance using load-balancing algorithms',
          ],
          correctIndex: 0,
          explanation: "Without a queue, a slow downstream service blocks the upstream producer — a synchronous HTTP call to a slow service degrades the caller's latency. A queue buffers the work: the producer writes and returns immediately, and the consumer processes when it can. This is the core resilience benefit of asynchronous messaging.",
        },
      ],
    },
    {
      topicId: 'docker-containers-images-and-compose',
      topicTitle: 'Docker – Containers, Images and Compose',
      objectiveIndex: 6,
      questions: [
        {
          question: 'Using the Fireship analogy from the content, what is the relationship between a Dockerfile, an image, and a container?',
          options: [
            'Dockerfile is the blueprint, image is the template (a built artefact), and container is the running process instantiated from that template',
            'Dockerfile is the container, image is the running process, and a template is the configuration used to deploy to production',
            'Dockerfile is the virtual machine, image is the operating system installed on it, and container is the application process running inside',
            'Dockerfile is the network configuration, image is the volume mount definition, and container is the load balancer routing traffic to the service',
          ],
          correctIndex: 0,
          explanation: 'A Dockerfile contains the instructions to build an image. Running `docker build` produces an image — a portable, immutable artefact. Running `docker run` on an image starts a container — the live, isolated process. Multiple containers can be started from the same image.',
        },
        {
          question: 'Why should dependency installation be placed before copying application code in a Dockerfile?',
          options: [
            'Each Dockerfile instruction creates a cached layer; putting slow-changing steps (installing dependencies) before fast-changing steps (copying code) means Docker reuses the dependency layer cache on most builds',
            'Docker requires dependencies to be available before application code can be parsed for syntax errors during the build',
            'Placing dependency installation after code copying causes a circular reference that Docker\'s layer resolver cannot handle',
            'Application code cannot reference installed packages at build time unless they are installed first in a prior instruction',
          ],
          correctIndex: 0,
          explanation: 'Docker caches each layer. If you copy application code before installing dependencies, every code change invalidates the dependency installation layer and forces a full reinstall. By installing dependencies first, the expensive layer is only rebuilt when the requirements file changes.',
        },
        {
          question: 'What is the purpose of a Docker volume and when is it required?',
          options: [
            'A volume persists data outside the container\'s writable layer; without one, data written inside a running container is lost when the container is removed',
            'A volume mounts the host machine\'s entire filesystem into the container so the application can access all host files',
            'A volume is required to expose a container\'s port on the host machine; without it, no external traffic can reach the container',
            'A volume encrypts data written by the container to protect it from other containers running on the same host',
          ],
          correctIndex: 0,
          explanation: "A container's writable layer is ephemeral — it is destroyed when the container is removed. For stateful services like databases, a Docker volume stores data on the host (or a network storage provider) independently of the container lifecycle, ensuring data survives container restarts and replacements.",
        },
        {
          question: 'Why should secrets like database passwords never be baked into a Docker image?',
          options: [
            'Secrets stored in image layers are visible in the image history (`docker history`) and to anyone who can pull the image from a registry; they must be injected at runtime via environment variables or a secrets manager',
            'Docker automatically strips environment variables from images during the push process, so secrets would be lost before the container starts',
            'Baking secrets into images prevents the Docker daemon from optimising layer caching, increasing build times significantly',
            'Container registries reject images that contain string patterns matching common secret formats to enforce security compliance',
          ],
          correctIndex: 0,
          explanation: "Every `RUN`, `ENV`, and `COPY` instruction in a Dockerfile creates a layer that is stored in the image. Even if you delete a secret in a later layer, it remains readable in the earlier layer's history. Anyone with pull access to the image can extract it with `docker history --no-trunc`.",
        },
        {
          question: 'What is a multi-stage Docker build and what problem does it solve?',
          options: [
            'A multi-stage build uses multiple FROM instructions in a single Dockerfile, allowing you to compile code in a build stage and copy only the final artefact into a minimal runtime image — dramatically reducing the final image size and attack surface',
            'A multi-stage build runs the same Dockerfile on multiple CPU architectures simultaneously, producing platform-specific images in a single build command',
            'A multi-stage build splits the Dockerfile into separate files for development, staging, and production, each with its own base image and configuration',
            'A multi-stage build executes each RUN instruction in a separate container and merges the results, enabling parallel layer construction for faster builds',
          ],
          correctIndex: 0,
          explanation: 'Without multi-stage builds, the final image includes compilers, build tools, and intermediate files that are only needed during compilation. A multi-stage build uses one stage to compile (e.g., with gcc or npm) and a second stage with a minimal base image (e.g., alpine) that copies only the compiled binary or bundled files. This can reduce image size from gigabytes to megabytes.',
        },
        {
          question: 'What does `docker compose up` do differently from running multiple `docker run` commands manually?',
          options: [
            'It reads a `docker-compose.yml` file that defines multiple services, their networks, volumes, and dependencies, then starts all of them with a single command — ensuring consistent, reproducible multi-container environments',
            'It combines multiple Dockerfiles into a single image containing all services, reducing the total number of containers needed',
            'It runs each service in the same container to share memory and reduce inter-process communication latency',
            'It automatically deploys containers to a Kubernetes cluster instead of the local Docker daemon, enabling production-grade orchestration',
          ],
          correctIndex: 0,
          explanation: 'Docker Compose declaratively defines a multi-container application: which services to run, how they connect (shared networks), where data persists (volumes), and startup order (depends_on). Instead of remembering and executing ten `docker run` commands with the correct flags, `docker compose up` reproduces the entire environment from a version-controlled YAML file.',
        },
      ],
    },
    {
      topicId: 'testing-unit-tests-integration-tests-and-the-test-pyramid',
      topicTitle: 'Testing – Unit Tests, Integration Tests and the Test Pyramid',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What distinguishes a unit test from an integration test?',
          options: [
            'A unit test targets a single function in isolation with no external dependencies; an integration test verifies that multiple components work together, often involving real databases or external services',
            'A unit test is written before the code (TDD); an integration test is written after the feature is complete and deployed',
            'A unit test is written by the developer; an integration test is written by a dedicated QA engineer',
            'A unit test runs in the browser; an integration test runs on the server against production data',
          ],
          correctIndex: 0,
          explanation: 'Unit tests isolate the function under test — dependencies are mocked or stubbed. Integration tests exercise real interactions between components, such as an API endpoint that reads from a test database. Unit tests are fast and deterministic; integration tests are slower but verify real behaviour.',
        },
        {
          question: 'Why does the test pyramid recommend many unit tests and few end-to-end tests?',
          options: [
            'Unit tests are fast, cheap, and focused; end-to-end tests are slow, brittle, and expensive — inverting the pyramid makes CI slow and unreliable',
            'Unit tests can only run in production environments; end-to-end tests can run locally, so the pyramid reflects deployment constraints',
            'End-to-end tests cover more code paths per test, so fewer are needed to achieve complete code coverage',
            'The pyramid reflects writing speed: unit tests are quick to write so developers write more of them by default',
          ],
          correctIndex: 0,
          explanation: 'Unit tests run in milliseconds and pinpoint failures precisely. End-to-end tests start browsers, wait for services, and fail for non-deterministic reasons. A test suite dominated by E2E tests is slow to run, slow to diagnose, and frequently flaky — developers stop trusting and running it.',
        },
        {
          question: 'What is over-mocking and why is it a testing anti-pattern?',
          options: [
            'Over-mocking replaces so many real dependencies with fakes that the test only verifies the mocks were called correctly, not that the actual code produces correct behaviour',
            'Over-mocking uses too many `assert` statements per test, making the test brittle when implementation details change',
            'Over-mocking creates test files that are larger than the production code, increasing maintenance overhead',
            'Over-mocking runs the same test multiple times with different mock configurations, producing redundant coverage',
          ],
          correctIndex: 0,
          explanation: 'When you mock everything, the test verifies that "given mocked inputs, the code calls the mocked dependencies correctly" — which tells you nothing about whether the real system works. Good tests mock only the boundaries you cannot control (external APIs, system clock) and test real logic.',
        },
        {
          question: 'Why should tests be treated as production code?',
          options: [
            'Tests document expected behaviour and catch regressions; poorly written, unmaintained tests are ignored or deleted, eliminating the safety net that allows confident deployments',
            'Tests are deployed to production servers to run continuous health checks, so they must meet the same performance standards as application code',
            'Tests consume the same CI/CD pipeline resources as production deployments, so they must be optimised to the same degree',
            'Test code is reviewed by external auditors and included in compliance certifications, requiring the same documentation standards as production code',
          ],
          correctIndex: 0,
          explanation: 'Tests that are messy, flaky, or hard to understand are eventually disabled or deleted. When tests are gone, regressions go undetected. Treating tests as first-class code — clean naming, clear assertions, minimal duplication — ensures the test suite remains a reliable safety net.',
        },
        {
          question: 'What is a test fixture, and why should each test create its own rather than sharing mutable state across tests?',
          options: [
            'Shared mutable fixtures cause test interdependence — one test modifying a shared object can cause another test to fail or pass depending on execution order, producing flaky results that erode trust in the suite',
            'A test fixture is a mock server that simulates external API responses; sharing one across tests is fine because it is read-only',
            'A test fixture is the assertion library used by the test framework; each test must import its own copy to avoid version conflicts',
            'Shared fixtures consume more memory because Python copies the fixture object for each test that references it',
          ],
          correctIndex: 0,
          explanation: 'A test fixture is the setup state a test needs to run — objects, database rows, files. When tests share mutable state, they become order-dependent: test A modifies the shared data, causing test B to fail when run after A but pass when run alone. Isolating fixtures per test eliminates this class of flakiness entirely.',
        },
        {
          question: 'What is the purpose of code coverage metrics, and why can high coverage be misleading?',
          options: [
            'Coverage measures which lines of code are executed by tests, but 100% line coverage does not guarantee correctness — it only confirms the lines ran, not that the assertions verify meaningful behaviour or that edge cases are tested',
            'Coverage measures the percentage of test files relative to source files; a 1:1 ratio indicates complete coverage and eliminates the need for manual code review',
            'Coverage measures runtime performance of the test suite; high coverage means the tests run quickly and efficiently',
            'Coverage is only meaningful for integration tests; unit test coverage is always 100% by definition because each unit test targets a single function',
          ],
          correctIndex: 0,
          explanation: 'A test that calls a function without asserting anything achieves 100% coverage of that function while verifying nothing. Coverage is a useful indicator of untested areas (low coverage means risk), but high coverage does not mean the tests are good. Mutation testing — which verifies that tests fail when code is changed — is a more robust measure of test quality.',
        },
      ],
    },
    {
      topicId: 'error-handling-and-logging',
      topicTitle: 'Error Handling and Logging',
      objectiveIndex: 0,
      questions: [
        {
          question: 'Why should you catch specific exception types rather than using a bare `except:` clause in Python?',
          options: [
            'A bare `except:` catches every exception including SystemExit and KeyboardInterrupt, silently swallowing errors the developer did not anticipate and hiding bugs',
            'A bare `except:` is a syntax error in Python 3 and will prevent the application from starting',
            'Catching specific exceptions reduces the CPU overhead of exception handling by avoiding the cost of matching the exception hierarchy',
            'A bare `except:` prevents the logging module from capturing the exception\'s stack trace, making debugging impossible',
          ],
          correctIndex: 0,
          explanation: 'A bare `except:` catches everything — including `KeyboardInterrupt`, `SystemExit`, and programming errors you did not anticipate. This hides bugs by silently consuming exceptions that should propagate. Always catch the specific exception types you expect and handle intentionally.',
        },
        {
          question: 'What is structured logging and why is it preferred over free-form log strings?',
          options: [
            'Structured logging emits log entries as key-value pairs or JSON, allowing log management tools to search, filter, and aggregate on specific fields programmatically',
            'Structured logging enforces a maximum log message length to prevent storage overflow in production systems',
            'Structured logging automatically redacts sensitive fields like passwords before they are written to disk',
            'Structured logging compresses log data using a binary format, reducing storage costs by an order of magnitude',
          ],
          correctIndex: 0,
          explanation: 'Free-form text logs like "User 42 logged in" require regex parsing to extract the user ID. Structured logs emit `{"event": "user_login", "user_id": 42}` — a log management tool can filter `user_id=42` across millions of entries instantly. This is the difference between diagnosing an incident in minutes versus hours.',
        },
        {
          question: 'Why is logging sensitive data such as passwords, tokens, or PII a security and compliance risk?',
          options: [
            'Logs are often stored in less-secured locations, accessible to more people, and retained longer than necessary — exposing sensitive data in logs creates a secondary breach vector beyond the primary data store',
            'Logging sensitive data increases log volume to the point where storage costs exceed the application\'s budget',
            'Most log aggregation services reject log entries containing sensitive data, causing the application to crash when trying to write those entries',
            'Sensitive data in logs triggers automatic account suspension by cloud providers under their acceptable use policies',
          ],
          correctIndex: 0,
          explanation: 'Production logs are often accessible to the entire engineering team, forwarded to third-party log aggregators, and retained for months or years. A password or token that appears in a log is effectively exposed to everyone with log access. Real-world breaches have been caused by credentials appearing in log files.',
        },
        {
          question: 'What log level configuration is recommended for production environments and why?',
          options: [
            'INFO or WARNING — DEBUG in production floods storage with low-value messages and buries the ERROR entries you need to find; it also degrades performance',
            'DEBUG — production systems need maximum visibility; disk is cheap and the extra detail helps diagnose issues faster',
            'CRITICAL — only the most severe events should be logged in production to minimise storage costs and regulatory exposure',
            'ERROR — logging INFO or WARNING entries in production creates noise that makes it harder to identify genuine problems',
          ],
          correctIndex: 0,
          explanation: "DEBUG logging is verbose by design — it captures every operation for development-time diagnosis. In production, DEBUG floods storage, slows the application, and makes it harder to find the ERROR entries that matter. INFO or WARNING strikes the right balance between visibility and signal-to-noise ratio.",
        },
        {
          question: 'What is the "fail fast" principle in error handling, and when should you violate it?',
          options: [
            'Fail fast means detecting and reporting errors as early as possible rather than allowing corrupted state to propagate — but batch processing jobs should often collect errors and continue, reporting all failures at the end rather than aborting on the first one',
            'Fail fast means returning an HTTP 500 error immediately for any unexpected input; it should never be violated because users expect instant feedback',
            'Fail fast means terminating the entire application process on any uncaught exception; you violate it by adding try-catch blocks around critical sections',
            'Fail fast means skipping input validation to reduce latency; you violate it when the API is public-facing and must validate for security reasons',
          ],
          correctIndex: 0,
          explanation: 'Fail fast prevents corrupt data from spreading through the system — validating inputs at the boundary, checking preconditions at function entry, and rejecting invalid state immediately. The exception is batch processing: if you are importing 10,000 records and record 3 fails, aborting the entire job wastes the work done on the first 2 records. Collect the errors, process what you can, and report all failures together.',
        },
        {
          question: 'Why should error responses from an API include a machine-readable error code in addition to a human-readable message?',
          options: [
            'Human-readable messages may change wording across releases or be localised into different languages; a stable machine-readable code allows client applications to programmatically handle specific error conditions without parsing message text',
            'Machine-readable codes compress the response payload, reducing bandwidth usage for mobile clients on slow networks',
            'Error codes are required by the HTTP specification; responses without them are rejected by standards-compliant browsers',
            'Machine-readable codes allow the server to log errors more efficiently by replacing long messages with short integer identifiers',
          ],
          correctIndex: 0,
          explanation: 'If a client determines how to handle an error by parsing the message string ("Insufficient funds"), any change to the wording ("Not enough balance") breaks the client. A stable error code like `INSUFFICIENT_FUNDS` gives the client a reliable signal to branch on, while the human-readable message can evolve freely for user display purposes.',
        },
      ],
    },
  ],
  senior: [
    {
      topicId: 'system-design-30-core-concepts',
      topicTitle: 'System Design – 30 Core Concepts',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What does the CAP theorem state about distributed data stores?',
          options: [
            'A distributed data store can guarantee at most two of Consistency, Availability, and Partition Tolerance simultaneously',
            'A distributed data store must sacrifice performance for either security or consistency, but not both',
            'A distributed data store can achieve all three of Consistency, Availability, and Partition Tolerance if replicas are co-located',
            'A distributed data store guarantees Consistency and Availability as long as Partition Tolerance is disabled at the network layer',
          ],
          correctIndex: 0,
          explanation: 'The CAP theorem states that a distributed data store can guarantee at most two of Consistency (every read returns the most recent write), Availability (every request gets a non-error response), and Partition Tolerance (the system operates despite network failures). Because partitions are a real-world inevitability, the practical choice is between CP and AP.',
        },
        {
          question: 'What is the key difference between database sharding and database replication?',
          options: [
            'Sharding splits data across multiple nodes by a shard key to scale writes; replication copies data to read-only replicas to scale reads',
            'Sharding encrypts data before storage; replication compresses it to reduce storage costs',
            'Sharding is a backup strategy for disaster recovery; replication is for distributing load to geographically close users',
            'Sharding uses synchronous writes; replication uses asynchronous writes — they are the same technique with different consistency guarantees',
          ],
          correctIndex: 0,
          explanation: 'Sharding distributes different rows of data across multiple nodes using a shard key — it scales write throughput because each shard handles a subset of writes. Replication copies the same data to read-only replicas — it scales read throughput. These are two distinct and complementary strategies.',
        },
        {
          question: 'A proxy and a reverse proxy sit at different points in the network. What is the correct description of each?',
          options: [
            'A proxy intercepts outgoing requests from clients on their behalf; a reverse proxy intercepts incoming requests to servers, handling SSL termination, load balancing, and caching',
            'A proxy handles server-to-server communication; a reverse proxy handles client-to-client communication',
            'A proxy encrypts traffic end-to-end; a reverse proxy decrypts it at the client before forwarding',
            'A proxy caches static assets at the origin; a reverse proxy caches them at the edge CDN node',
          ],
          correctIndex: 0,
          explanation: 'A forward proxy sits in front of clients — it makes requests on their behalf, useful for anonymity or corporate filtering. A reverse proxy sits in front of servers — it receives client requests and forwards them to the appropriate backend, enabling SSL termination, load balancing, and response caching.',
        },
        {
          question: 'Why are message queues more than just a performance optimisation in distributed systems?',
          options: [
            'Queues decouple the rate of production from the rate of consumption, acting as a resilience buffer that absorbs spikes and prevents cascading failure when a downstream service is slow or unavailable',
            'Queues provide guaranteed message ordering across all consumers, removing the need for distributed locking',
            'Queues encrypt messages in transit, making them the only safe communication channel between microservices',
            'Queues replace the need for a database by persisting all application state as a stream of immutable messages',
          ],
          correctIndex: 0,
          explanation: 'A message queue decouples the producer from the consumer in both time and rate. If the consuming service goes down, the queue holds messages until it recovers. If a traffic spike occurs, the queue absorbs the burst rather than overwhelming the downstream service — this is a resilience mechanism, not just a throughput one.',
        },
        {
          question: 'What is the difference between horizontal scaling and vertical scaling, and why do large-scale systems favour horizontal?',
          options: [
            'Vertical scaling adds resources to a single machine (bigger CPU, more RAM); horizontal scaling adds more machines — horizontal is favoured because single-machine capacity has a hard ceiling and horizontal scaling provides redundancy against individual node failures',
            'Vertical scaling distributes load across regions; horizontal scaling distributes load within a single data centre — horizontal is cheaper because intra-DC bandwidth is free',
            'Vertical scaling adds read replicas; horizontal scaling adds write replicas — horizontal is favoured because writes are the bottleneck in most systems',
            'Vertical scaling requires no code changes; horizontal scaling requires rewriting the application in a distributed systems language like Erlang',
          ],
          correctIndex: 0,
          explanation: 'Vertical scaling (scaling up) means upgrading a single server — but there is a physical limit to how large a single machine can be, and it creates a single point of failure. Horizontal scaling (scaling out) adds more machines behind a load balancer, providing near-linear capacity growth and fault tolerance through redundancy. This is why cloud-native architectures are designed for horizontal scaling from the start.',
        },
        {
          question: 'Why is a write-through cache safer but slower than a write-back cache?',
          options: [
            'Write-through writes to both the cache and the backing store synchronously, ensuring consistency but adding latency; write-back writes only to the cache and asynchronously flushes to the store, risking data loss if the cache node crashes before the flush',
            'Write-through encrypts data before caching, adding security overhead; write-back stores data in plaintext for faster access',
            'Write-through replicates the cache entry to all nodes in the cluster; write-back only stores it on the local node, reducing network traffic',
            'Write-through invalidates the cache on every write, forcing a cache miss on the next read; write-back keeps the cache warm by deferring invalidation',
          ],
          correctIndex: 0,
          explanation: 'Write-through guarantees that the backing store always has the latest data because every write goes to both the cache and the store before returning success. Write-back (or write-behind) acknowledges the write as soon as the cache is updated, flushing to the store later. This is faster but creates a window where the cached data exists only in volatile memory — a cache crash during that window means data loss.',
        },
      ],
    },
    {
      topicId: 'real-world-system-design-uber-case-study',
      topicTitle: 'Real-World System Design – Uber Case Study',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is the correct order of steps in a structured system design answer, as demonstrated by the Uber case study?',
          options: [
            'Functional requirements → non-functional requirements → data model → API design → deep dives into critical flows',
            'Deep dives into critical flows → data model → API design → functional requirements → non-functional requirements',
            'Technology selection → data model → functional requirements → API design → non-functional requirements',
            'Non-functional requirements → technology selection → functional requirements → deep dives → data model',
          ],
          correctIndex: 0,
          explanation: 'The structured approach starts with functional requirements (what must the system do?), then non-functional requirements (how must it perform?), then models the data, designs the API, and finally deep-dives into the most challenging flows. This order ensures every component decision is grounded in requirements rather than technology preference.',
        },
        {
          question: 'Why do non-functional requirements shape every component selection in the Uber design, rather than being addressed after the design is complete?',
          options: [
            'Requirements like "100M+ daily users, 24/7 availability, and low latency on poor networks" eliminate entire classes of design choices from the start — you cannot retrofit those guarantees',
            'Non-functional requirements are set by the legal team and must be reviewed before any technical work begins',
            'Non-functional requirements determine the programming language used, which in turn constrains every other technology choice',
            'Non-functional requirements are only relevant for database selection; other components are chosen purely on functional grounds',
          ],
          correctIndex: 0,
          explanation: 'Non-functional requirements like scale, availability, and latency are not constraints to address after design — they eliminate architectural options upfront. Designing for 100M daily users with 24/7 availability rules out a single-server approach and shapes every protocol, database, and service boundary decision.',
        },
        {
          question: 'The Uber design must handle millions of drivers sending location updates every few seconds. Which real-time transport protocol is most appropriate and why?',
          options: [
            'WebSockets — they maintain a persistent bidirectional connection, avoiding the overhead of establishing a new HTTP connection for each location update',
            'Standard HTTP POST — it is universally supported and each update can be sent as a separate stateless request',
            'Server-Sent Events — they are designed for high-frequency bidirectional communication and handle reconnection automatically',
            'Long polling — it is the lowest-latency option because the server holds the connection open and pushes data the moment it is available',
          ],
          correctIndex: 0,
          explanation: 'WebSockets establish a persistent bidirectional connection once and keep it open. For millions of drivers sending updates every few seconds, avoiding the TCP handshake and HTTP header overhead of a new connection per update is critical — WebSockets provide this at scale.',
        },
        {
          question: 'A single "Request Ride" action in Uber involves fare estimation, geospatial queries, driver matching, and payment state — all of which must handle failure at any step. What design principle does this illustrate?',
          options: [
            'User-facing actions often span multiple services and state machines, requiring explicit failure handling at each step rather than assuming end-to-end success',
            'A monolithic architecture is preferable because it can execute all steps in a single database transaction with ACID guarantees',
            'Synchronous REST calls between services are the correct approach because each step must complete before the next can begin',
            'The ride request flow should be handled entirely client-side to reduce server load and simplify failure recovery',
          ],
          correctIndex: 0,
          explanation: 'A user tapping "Request Ride" triggers a distributed workflow across services and external systems. Each step can fail independently. Good system design makes these failure modes explicit — using retries, timeouts, and state machines — rather than treating the happy path as the only path.',
        },
      ],
    },
    {
      topicId: 'distributed-systems-cap-theorem-consistency-and-availability-trade-offs',
      topicTitle: 'Distributed Systems – CAP Theorem, Consistency and Availability Trade-offs',
      objectiveIndex: 0,
      questions: [
        {
          question: 'A social media feed can tolerate showing posts that are a few seconds old, but a banking ledger cannot tolerate showing an incorrect balance. Which CAP trade-off applies to each?',
          options: [
            'The social feed favours AP (availability over consistency) — eventual consistency is acceptable; the banking ledger favours CP (consistency over availability) — stale data cannot be tolerated',
            'The social feed favours CP — users expect feeds to always be current; the banking ledger favours AP — transactions must never be blocked',
            'Both systems should favour CP — financial regulation requires consistency and social platforms face legal liability for stale content',
            'Both systems should favour AP — modern users tolerate inconsistency and availability is always the higher business priority',
          ],
          correctIndex: 0,
          explanation: 'The correct trade-off depends on the business domain. Social feeds, search indexes, and recommendation data can tolerate a short window of staleness — AP is correct. Financial transactions and inventory counts cannot tolerate stale reads — CP is correct. The mistake is applying the same choice to every system.',
        },
        {
          question: 'How does the PACELC model extend the CAP theorem to provide more actionable guidance for everyday design decisions?',
          options: [
            'PACELC recognises that even when there is no partition (normal operation), you trade off latency against consistency — this latency/consistency trade-off is the more frequent design decision in practice',
            'PACELC adds a fourth dimension (Efficiency) to CAP, acknowledging that performance must be traded against all three CAP properties simultaneously',
            'PACELC replaces the binary CP/AP choice with a spectrum of five consistency levels: eventual, causal, session, monotonic, and linearisable',
            'PACELC extends CAP by requiring that all distributed systems document their partition behaviour in a machine-readable format',
          ],
          correctIndex: 0,
          explanation: 'CAP only describes behaviour during network partitions. PACELC observes that in the absence of a partition (which is most of the time), every distributed system still trades off Latency against Consistency. This is more actionable: every read/write configuration choice in a database is a latency-consistency trade-off.',
        },
        {
          question: 'What does "eventual consistency" guarantee, and when is it an inappropriate choice?',
          options: [
            'It guarantees all replicas will converge to the same state given enough time with no new writes; it is inappropriate when reading stale data would cause incorrect business outcomes, such as overselling inventory or double-spending funds',
            'It guarantees all replicas will converge within a fixed time window defined in the SLA; it is inappropriate when that window exceeds the user\'s session timeout',
            'It guarantees that reads will always return the value written by the most recent transaction; it is inappropriate when low latency is more important than correctness',
            'It guarantees that writes are synchronously replicated to a majority of nodes before acknowledging; it is inappropriate when write throughput must exceed 10,000 operations per second',
          ],
          correctIndex: 0,
          explanation: 'Eventual consistency means replicas will agree eventually — not immediately. For reads where the business can tolerate a brief window of staleness, this is fine. For inventory counts, financial balances, or any scenario where a stale read causes a business error, eventual consistency is the wrong choice.',
        },
        {
          question: 'Why is conflating CAP consistency with ACID consistency a dangerous mistake in system design?',
          options: [
            'CAP consistency means every node returns the most recent write (distributed agreement); ACID consistency means data integrity constraints are maintained within a single database — they are different properties sharing the same word',
            'CAP consistency is a stronger guarantee than ACID consistency, so a CAP-consistent system automatically satisfies ACID requirements',
            'They are effectively the same property — both require synchronous replication and immediate visibility of all writes across all nodes',
            'ACID consistency is a subset of CAP consistency; a CP system automatically provides ACID guarantees to all clients',
          ],
          correctIndex: 0,
          explanation: 'CAP\'s "C" means every read across all distributed nodes reflects the most recent write — a distributed coordination guarantee. ACID\'s "C" means data transitions from one valid state to another without violating integrity constraints — a single-database property. Confusing them leads to incorrect database selection and false assumptions about correctness guarantees.',
        },
        {
          question: 'What is a quorum in distributed consensus, and why is a majority (N/2 + 1) the minimum required?',
          options: [
            'A quorum is the minimum number of nodes that must agree for an operation to succeed; a majority ensures that any two quorums overlap by at least one node, guaranteeing that the latest write is always visible to a subsequent read quorum',
            'A quorum is the total number of nodes in the cluster; a majority is required because minority partitions cannot access the shared disk',
            'A quorum is the number of nodes that must be healthy for the cluster to accept new nodes; a majority prevents the cluster from growing beyond its capacity',
            'A quorum is the number of replicas that store a copy of each record; a majority is needed because the leader election algorithm requires an odd number of voters',
          ],
          correctIndex: 0,
          explanation: 'If you write to W nodes and read from R nodes, and W + R > N (total nodes), then the read quorum and write quorum must overlap — at least one node in the read set has the latest write. Setting both W and R to a majority (N/2 + 1) satisfies this condition. This is the mathematical foundation of quorum-based consistency in systems like Cassandra and DynamoDB.',
        },
        {
          question: 'What is a split-brain scenario in a distributed system, and how does fencing prevent data corruption?',
          options: [
            'Split-brain occurs when a network partition causes two nodes to both believe they are the leader, accepting conflicting writes simultaneously; fencing (e.g., STONITH or epoch-based tokens) ensures the old leader is forcibly isolated before the new leader accepts writes',
            'Split-brain occurs when a database shard receives more traffic than it can handle, causing it to split into two smaller shards; fencing limits the maximum shard size',
            'Split-brain is a memory corruption bug caused by concurrent garbage collection on two JVM threads; fencing adds memory barriers between threads',
            'Split-brain occurs when a client sends the same request to two different services; fencing deduplicates the requests using an idempotency key',
          ],
          correctIndex: 0,
          explanation: 'During a network partition, the old leader may still be accepting writes while the rest of the cluster has elected a new leader. Both leaders accept writes, producing conflicting data that cannot be automatically reconciled. Fencing mechanisms — such as STONITH (Shoot The Other Node In The Head) or monotonically increasing epoch/fence tokens — ensure that the old leader is either shut down or its writes are rejected by storage nodes that have seen a higher epoch.',
        },
      ],
    },
    {
      topicId: 'microservices-boundaries-communication-and-trade-offs-vs-monolith',
      topicTitle: 'Microservices – Boundaries, Communication and Trade-offs vs Monolith',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What is a "distributed monolith" and why is it considered the worst of both worlds?',
          options: [
            'A system split into multiple services that share a database or are so tightly coupled that they must be deployed together — it has all the operational complexity of microservices with none of the autonomy benefits',
            'A monolith deployed across multiple cloud regions for high availability — it has the simplicity of a monolith but the cost of a distributed system',
            'A microservices architecture where all services share the same runtime process but communicate via message queues',
            'A legacy monolith that has been incrementally migrated to microservices over several years, resulting in inconsistent patterns',
          ],
          correctIndex: 0,
          explanation: 'A distributed monolith occurs when you split a system into services but they remain tightly coupled — sharing a database, requiring coordinated deploys, or triggering cascading failures. You get the operational overhead of microservices (network calls, distributed tracing, service discovery) with none of the autonomy that justifies that overhead.',
        },
        {
          question: 'According to Conway\'s Law, what happens if service boundaries do not align with team boundaries?',
          options: [
            'The system architecture will mirror the communication structure of the organisation — misaligned boundaries create services that require constant cross-team coordination, negating microservice autonomy',
            'Service performance will degrade proportionally to the number of teams involved, due to coordination overhead in the deployment pipeline',
            'The codebase will automatically reorganise itself over time to match the team structure, creating technical debt in the interim',
            'Conway\'s Law only applies to monolithic architectures — microservices are immune because each service is independently owned',
          ],
          correctIndex: 0,
          explanation: "Conway's Law observes that a system's architecture tends to mirror the communication structure of the organisation that builds it. If the team owning the \"orders\" service must constantly coordinate with the team owning \"inventory\" to ship any feature, your service boundaries are wrong — they are creating, not eliminating, coordination costs.",
        },
        {
          question: 'When should you prefer asynchronous inter-service communication (events, message queues) over synchronous calls (REST, gRPC)?',
          options: [
            'For operations that do not require an immediate response to the caller — asynchronous communication reduces coupling and improves resilience because the producer does not need the consumer to be available',
            'Always — synchronous calls create tight coupling and should be avoided in any microservices architecture regardless of the use case',
            'Only when the payload exceeds 1MB — small payloads are better suited to synchronous REST calls due to lower message queue overhead',
            'When the downstream service has an SLA slower than 500ms — above this threshold, the caller should switch to a message queue automatically',
          ],
          correctIndex: 0,
          explanation: 'Synchronous calls couple the caller to the availability and latency of the callee — if the callee is slow or down, the caller is blocked or fails. Asynchronous messaging decouples them: the producer enqueues the message and continues; the consumer processes it when ready. This is the right choice for operations where the caller does not need an immediate answer.',
        },
        {
          question: 'What is the Strangler Fig pattern and why is it the preferred approach for migrating from a monolith to microservices?',
          options: [
            'New functionality is built as services while the monolith handles legacy paths, allowing incremental migration without a risky big-bang rewrite',
            'The monolith is split into microservices all at once during a planned downtime window to minimise the period of architectural inconsistency',
            'A strangler fig proxy is placed in front of the monolith and gradually routes traffic to microservices based on URL prefix matching rules',
            'The monolith is first containerised and then its modules are extracted one by one into separate Kubernetes pods over a fixed six-month migration',
          ],
          correctIndex: 0,
          explanation: 'The Strangler Fig pattern avoids the risk of a big-bang rewrite. New features are built as microservices from the start; existing monolith features are migrated incrementally over time. The monolith shrinks as services take over its responsibilities — like a strangler fig vine gradually replacing the tree it grows around.',
        },
        {
          question: 'What is the circuit breaker pattern, and why is it critical in a microservices architecture?',
          options: [
            'A circuit breaker monitors failures to a downstream service and stops sending requests after a threshold is reached, allowing the failing service time to recover instead of cascading the failure to the caller and its upstream dependents',
            'A circuit breaker encrypts inter-service communication and disconnects the circuit if the TLS handshake fails, preventing man-in-the-middle attacks',
            'A circuit breaker limits the number of concurrent requests a service can handle, rejecting excess traffic to prevent CPU saturation',
            'A circuit breaker is a Kubernetes resource that restarts a pod when its health check fails three consecutive times',
          ],
          correctIndex: 0,
          explanation: 'Without a circuit breaker, a failing downstream service causes the caller to accumulate waiting threads and eventually fail itself — a cascading failure that can take down the entire system. The circuit breaker trips after a threshold of failures, immediately returning an error (or fallback) to callers without attempting the network call. After a cooldown period, it allows a test request through — if it succeeds, the circuit closes again.',
        },
        {
          question: 'What problem does the Saga pattern solve that a distributed transaction cannot?',
          options: [
            'Sagas coordinate a multi-service business process using a sequence of local transactions with compensating actions for rollback, avoiding the need for distributed locks that reduce availability and throughput',
            'Sagas encrypt data in transit between microservices, which distributed transactions cannot do because they rely on plaintext two-phase commit messages',
            'Sagas provide exactly-once message delivery between services, whereas distributed transactions only guarantee at-most-once delivery',
            'Sagas allow services written in different programming languages to participate in the same transaction, whereas distributed transactions require a shared runtime',
          ],
          correctIndex: 0,
          explanation: 'Distributed transactions (e.g., two-phase commit) lock resources across multiple services until all participants agree, which destroys availability and throughput at scale. A Saga breaks the process into local transactions — each service commits locally, and if a later step fails, previously completed steps execute compensating actions (e.g., refund a payment). This trades strong consistency for availability, which is the right trade-off in most microservice architectures.',
        },
      ],
    },
    {
      topicId: 'domain-driven-design-bounded-contexts-and-aggregates',
      topicTitle: 'Domain-Driven Design – Bounded Contexts and Aggregates',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What problem does the Ubiquitous Language solve in DDD?',
          options: [
            'It prevents translation errors by ensuring developers, domain experts, and code all use the same terms for the same concepts — mismatches between business vocabulary and code vocabulary surface as bugs',
            'It provides a standardised API naming convention that all microservices in the organisation must follow to ensure interoperability',
            'It generates code documentation automatically from the domain model, keeping technical docs in sync with the business glossary',
            'It enforces consistent database column naming across all bounded contexts to prevent join complexity at the persistence layer',
          ],
          correctIndex: 0,
          explanation: 'When the business calls something a "shipment" and the code calls it an "order_delivery", every conversation requires mental translation — and those translations introduce bugs. The Ubiquitous Language means the same word is used in requirements, conversations, code variable names, and test descriptions, surfacing misunderstandings before they become defects.',
        },
        {
          question: 'A bounded context defines an explicit boundary within which a domain model is valid. Why should two bounded contexts never share a database schema?',
          options: [
            'A shared schema creates tight coupling — changes to the schema for one context break the other, defeating independent deployment and allowing one context\'s model to pollute the other\'s',
            'Sharing a schema between contexts is a security risk because it grants both services read access to each other\'s sensitive data',
            'Shared schemas cannot be versioned independently, making it impossible to roll back one context without rolling back the other',
            'Database vendors do not support concurrent connections from multiple services to the same schema without significant performance degradation',
          ],
          correctIndex: 0,
          explanation: 'A shared schema is a shared contract. When the Sales context needs to add a column for commission tracking, it risks breaking the Support context that reads the same table. Independent deployment — a core benefit of microservices — requires each bounded context to own its schema exclusively.',
        },
        {
          question: 'What is an Aggregate Root in DDD, and why must all changes to an aggregate pass through it?',
          options: [
            'The Aggregate Root is the sole entry point into a cluster of related domain objects; routing all changes through it ensures business invariants are always enforced within a single transaction',
            'The Aggregate Root is the database primary key that uniquely identifies a cluster of related rows across multiple tables',
            'The Aggregate Root is the top-level service in a microservices hierarchy, responsible for coordinating writes across all child services',
            'The Aggregate Root is a read-only projection of the aggregate state, used to produce Domain Events when the state changes',
          ],
          correctIndex: 0,
          explanation: 'An aggregate is a cluster of objects treated as a single unit for data changes. The Aggregate Root is the only object that external code can reference directly. All modifications must go through it so that the root can check invariants — for example, ensuring an Order never has a negative total — before persisting changes.',
        },
        {
          question: 'What is the Anti-Corruption Layer (ACL) in a DDD context map, and when should you use one?',
          options: [
            'A translation layer that prevents a foreign or legacy model from polluting the new bounded context\'s model — use it when integrating with a third-party API or legacy system whose model does not align with your domain',
            'A security layer that validates all incoming domain events for malicious payloads before they are processed by the aggregate',
            'A caching layer that absorbs high read traffic to a bounded context, preventing database load from corrupting query performance',
            'An automated code review tool that rejects pull requests containing direct database access from domain objects',
          ],
          correctIndex: 0,
          explanation: 'When you must integrate with a system whose model conflicts with yours — an external payment provider, a legacy ERP — an ACL translates between their concepts and yours. Without it, their model leaks into yours, forcing your domain objects to carry concepts that only make sense in the foreign system.',
        },
        {
          question: 'What is a Domain Event, and how does it enable loose coupling between bounded contexts?',
          options: [
            'A Domain Event is an immutable record that something meaningful happened in the domain (e.g., OrderPlaced); other bounded contexts subscribe to these events and react independently, removing the need for direct synchronous calls between contexts',
            'A Domain Event is a database trigger that fires when a row is inserted, updating related tables within the same schema to maintain referential integrity',
            'A Domain Event is a log entry written by the domain layer for debugging purposes; other contexts read the log file to synchronise their state',
            'A Domain Event is an HTTP webhook that notifies external systems of state changes; it is only used for third-party integrations, not internal bounded contexts',
          ],
          correctIndex: 0,
          explanation: 'Domain Events capture facts about what happened — OrderPlaced, PaymentReceived, InventoryReserved. Other bounded contexts subscribe to these events asynchronously and react according to their own model. The publishing context does not need to know who is listening or what they do with the event, which is the definition of loose coupling between contexts.',
        },
        {
          question: 'Why should you model a Value Object instead of using a primitive type for concepts like Money, EmailAddress, or Temperature?',
          options: [
            'Value Objects encapsulate validation and behaviour — a Money type prevents adding dollars to euros, while a raw float allows meaningless arithmetic; they also make the code self-documenting by replacing ambiguous primitives with domain-specific types',
            'Value Objects are required by the DDD specification; using primitives in a DDD codebase causes the framework to throw runtime exceptions',
            'Value Objects reduce memory usage because they are stored on the stack instead of the heap, unlike primitive wrapper types',
            'Value Objects enable database-level type checking — the ORM maps each Value Object to a database constraint that rejects invalid values at the storage layer',
          ],
          correctIndex: 0,
          explanation: 'Using a raw float for money allows bugs like adding USD and EUR without conversion, or storing a negative price without validation. A Money value object enforces currency matching, valid ranges, and correct rounding in one place. This is called "primitive obsession" — replacing it with a Value Object eliminates an entire class of bugs and makes the domain model explicit in the code.',
        },
      ],
    },
    {
      topicId: 'algorithms-complexity-big-o-and-when-it-matters-in-production',
      topicTitle: 'Algorithms – Complexity, Big O and When It Matters in Production',
      objectiveIndex: 0,
      questions: [
        {
          question: 'An API endpoint scans a list of active user IDs on every request to check if the caller is authorised. At 100 users this takes 0.1ms; at 10,000 users it takes 10ms; at 1,000,000 users it takes 1,000ms. What complexity class is this, and what is the fix?',
          options: [
            'O(n) linear — the fix is to store active user IDs in a hash map, reducing each lookup to O(1) average-case and eliminating the performance cliff as users grow',
            'O(n²) quadratic — the fix is to sort the list first and use binary search, reducing lookups to O(log n)',
            'O(log n) logarithmic — this is already efficient; no optimisation is needed for expected user growth',
            'O(n log n) linearithmic — the fix is to shard the user list across multiple nodes so each node only scans a fraction of the list',
          ],
          correctIndex: 0,
          explanation: 'Scanning a list for membership is O(n) — the time grows linearly with the number of items. At 1M users this becomes seconds per request, which is catastrophic. Storing the active user IDs in a hash set reduces each lookup to O(1) average-case regardless of the list size. This pattern — replacing list scans with hash map lookups — resolves a large class of production performance incidents.',
        },
        {
          question: 'What is amortised complexity and why does it explain why appending to a dynamic array is efficient despite occasional expensive resizes?',
          options: [
            'Amortised complexity averages the cost of all operations over a long sequence — dynamic array resizes are rare and their cost is spread across the many cheap appends that follow, making the average cost per append O(1)',
            'Amortised complexity measures the worst-case cost of a single operation and divides it by the number of CPU cores available, giving a parallelised cost estimate',
            'Amortised complexity is the cost per operation after compiler optimisations are applied — the JIT compiler eliminates resize overhead in hot loops',
            'Amortised complexity is a marketing term for the average-case complexity measured on a specific hardware benchmark, not a formal analytical technique',
          ],
          correctIndex: 0,
          explanation: 'When a dynamic array doubles in capacity on resize, the resize is O(n) but happens infrequently. Over a sequence of n appends, the total resize work is O(n) — meaning the average cost per append is O(1). This amortised analysis is why dynamic arrays are the default sequence type despite having occasional expensive operations.',
        },
        {
          question: 'In which production scenarios does algorithm complexity directly impact user experience, even when the database is well-indexed?',
          options: [
            'Batch jobs and ETL pipelines processing large files on a single node, report generation that grows with row count, and high-volume APIs where a small per-request inefficiency multiplies across millions of calls',
            'CRUD web APIs backed by a well-indexed relational database — database query time dominates, making application code complexity irrelevant in these cases',
            'Any scenario where the input size exceeds 1GB — below this threshold, O(n²) algorithms finish within acceptable time limits on modern hardware',
            'Containerised applications only — bare-metal deployments have enough CPU to absorb quadratic algorithms at typical production data volumes',
          ],
          correctIndex: 0,
          explanation: 'For most CRUD APIs, the database is the bottleneck and algorithm complexity in application code is secondary. But in batch processing, ETL, report generation, and high-volume hot paths, a poorly chosen algorithm can make the system completely unusable as data volume grows — no infrastructure investment can fix an O(n²) hot path processing 10M records.',
        },
        {
          question: 'Which of the following is an O(n²) pattern in code, and what is the correct diagnosis approach?',
          options: [
            'A nested loop where both loops iterate over the same collection is the classic O(n²) pattern — profile the application to confirm the hot path before optimising',
            'A recursive function that calls itself twice per invocation is always O(n²) — replace recursion with iteration to eliminate the quadratic behaviour',
            'Any use of a sort algorithm inside a loop is O(n²) — sort the collection once before the loop to reduce the total complexity',
            'A function that reads from a database inside a loop is O(n²) — move all database reads outside the loop to batch the queries',
          ],
          correctIndex: 0,
          explanation: 'Two nested loops over the same collection of n items perform n² iterations — the canonical O(n²) pattern. Profiling is the required first step before optimising: identify the actual hot path, confirm the complexity is the bottleneck, then fix it. Premature optimisation of non-hot paths wastes time and produces harder-to-read code.',
        },
        {
          question: 'Why does O(n log n) appear as the time complexity for efficient comparison-based sorting algorithms like merge sort, and can you do better?',
          options: [
            'Comparison-based sorting has a proven lower bound of O(n log n) because each comparison eliminates at most half of the remaining possible orderings; you can beat this only with non-comparison-based algorithms (counting sort, radix sort) that exploit constraints on the input',
            'O(n log n) is an implementation detail of the divide-and-conquer strategy; iterative sorting algorithms achieve O(n) by avoiding the recursive overhead',
            'O(n log n) is the average case; the worst case for merge sort is O(n²) when the input is already sorted in reverse order',
            'O(n log n) applies only to arrays; sorting linked lists is always O(n) because pointer manipulation is cheaper than element comparison',
          ],
          correctIndex: 0,
          explanation: 'There are n! possible orderings of n elements. Each comparison splits the remaining possibilities in half. To distinguish between n! orderings, you need at least log₂(n!) comparisons, which is O(n log n) by Stirling\'s approximation. This is a mathematical lower bound for comparison-based sorts. Non-comparison sorts like counting sort or radix sort bypass this limit by using the structure of the data (e.g., integer keys within a known range) to achieve O(n) time.',
        },
        {
          question: 'A developer replaces a HashMap with a TreeMap "for safety" in a hot path processing 10 million records. What is the likely performance impact and why?',
          options: [
            'HashMap provides O(1) average-case lookups; TreeMap provides O(log n) lookups — for 10 million records, each TreeMap lookup does approximately 23 comparisons versus 1 hash computation, potentially multiplying the total execution time of the hot path significantly',
            'TreeMap is faster because it keeps keys sorted, allowing binary search; HashMap must scan all entries to find a key due to potential hash collisions',
            'There is no performance difference — both data structures use hashing internally, but TreeMap additionally maintains insertion order',
            'HashMap uses more memory because it stores hash codes alongside keys; TreeMap stores only keys and pointers, making it more cache-friendly for large datasets',
          ],
          correctIndex: 0,
          explanation: 'A HashMap computes a hash and indexes into an array — O(1) on average. A TreeMap maintains a red-black tree and performs O(log n) comparisons per lookup. At 10 million entries, log₂(10⁷) is approximately 23. If this lookup is in a hot loop executed millions of times, the cumulative difference is enormous. Choose data structures based on access pattern requirements, not vague safety concerns.',
        },
      ],
    },
    {
      topicId: 'dynamic-programming-pattern-recognition-and-memoisation',
      topicTitle: 'Dynamic Programming – Pattern Recognition and Memoisation',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What two properties must a problem exhibit for dynamic programming to be applicable?',
          options: [
            'Optimal substructure (the optimal solution can be built from optimal solutions to subproblems) and overlapping subproblems (the same subproblems are recomputed repeatedly in a naive recursive approach)',
            'Polynomial time complexity (the naive solution must run in O(n^k) for some k) and deterministic output (the function must return the same result for the same input)',
            'Monotonic input (the problem input must be sortable) and bounded state space (the number of distinct subproblems must be finite and known in advance)',
            'A greedy choice property (a locally optimal choice leads to a globally optimal solution) and a recursive substructure (the problem can be split into identical subproblems of smaller size)',
          ],
          correctIndex: 0,
          explanation: 'DP applies when (1) the optimal answer to the full problem can be assembled from optimal answers to smaller subproblems (optimal substructure), and (2) a naive recursive solution recomputes the same subproblems many times (overlapping subproblems). Both conditions together are what DP exploits: solve each subproblem once, cache it, reuse the answer.',
        },
        {
          question: 'What is the difference between memoisation (top-down) and tabulation (bottom-up) in dynamic programming?',
          options: [
            'Memoisation writes a recursive solution and caches subproblem results as they are encountered; tabulation builds a results table iteratively from the smallest subproblems upward, never using recursion',
            'Memoisation stores results on disk for reuse across multiple program invocations; tabulation stores them in memory for the duration of a single function call',
            'Memoisation applies only to problems with a one-dimensional state space; tabulation is required when the state space has two or more dimensions',
            'Memoisation requires the state to be a primitive type; tabulation supports complex object types as cache keys through structural equality',
          ],
          correctIndex: 0,
          explanation: 'Top-down memoisation starts from the original problem, recurses into subproblems, and caches each result in a dictionary the first time it is computed. Bottom-up tabulation starts from base cases and fills a table iteratively. Both convert exponential naive solutions to polynomial time; tabulation avoids recursion depth limits and often has better cache locality.',
        },
        {
          question: 'Why is state definition described as the hardest part of solving a dynamic programming problem?',
          options: [
            'The state must capture exactly the information needed to uniquely describe a subproblem — too little information produces incorrect answers; too much information creates an enormous memoisation table that negates the efficiency gain',
            'The state must be expressed as a single integer index, requiring complex encoding of multi-dimensional information into a one-dimensional key',
            'The state definition determines the programming language that can be used — some languages cannot represent the required state types efficiently',
            'The state must be shared across all subproblems via a global variable, and defining it incorrectly causes race conditions in concurrent implementations',
          ],
          correctIndex: 0,
          explanation: 'The state is the "address" of a subproblem in the memoisation table. If the state omits information that affects the answer, different subproblems get the same key and produce incorrect results. If it captures too much information, the table becomes enormous — an exponential number of states — defeating the purpose of DP entirely.',
        },
        {
          question: 'When should you prefer a greedy algorithm over dynamic programming?',
          options: [
            'When the problem has the greedy choice property — a locally optimal decision at each step leads to a globally optimal solution — and this can be proven; DP adds unnecessary complexity when greedy suffices',
            'When the input size is small enough that the naive recursive solution runs within the time limit; DP optimisation is only necessary above a known threshold',
            'When the problem involves maximisation rather than minimisation; minimisation problems always require DP while maximisation can be solved greedily',
            'When the state space has more than two dimensions; greedy algorithms scale better than DP tables in high-dimensional problems',
          ],
          correctIndex: 0,
          explanation: 'Greedy algorithms are simpler and faster when they work — but they only work when a locally optimal choice at each step guarantees a globally optimal result (the greedy choice property). Jumping to DP when a greedy approach is correct adds unnecessary complexity. Verify the greedy property first; resort to DP when it cannot be proven.',
        },
      ],
    },
    {
      topicId: 'observability-structured-logging-metrics-and-distributed-tracing',
      topicTitle: 'Observability – Structured Logging, Metrics and Distributed Tracing',
      objectiveIndex: 0,
      questions: [
        {
          question: 'What are the three pillars of observability and what distinct question does each answer?',
          options: [
            'Logs answer "what happened?" (discrete events); metrics answer "how often and how fast?" (aggregated numbers over time); traces answer "where did time go and which service caused the failure?" (the request journey across services)',
            'Logs answer "how fast is the system?" (latency per request); metrics answer "what errors occurred?" (error event records); traces answer "who is using the system?" (user session tracking)',
            'Logs, metrics, and traces all answer the same question from different angles — they are redundant pillars and most teams can operate effectively with just one',
            'Logs answer "is the system up?" (availability signal); metrics answer "what code ran?" (execution trace); traces answer "how much does it cost?" (resource consumption per request)',
          ],
          correctIndex: 0,
          explanation: 'The three pillars are complementary, not redundant. Metrics tell you something is wrong (error rate spike). Logs tell you what happened at specific points (payment declined, order 42). Traces show the end-to-end journey of the failing request across all services. You need all three to diagnose production incidents efficiently.',
        },
        {
          question: 'What is a correlation ID (trace ID) and why is it essential in a microservices architecture?',
          options: [
            'A unique identifier generated at the entry point of a request and propagated through every downstream service call, log entry, and message — it is the thread that lets you reconstruct the full journey of a single request across ten services',
            'A unique identifier assigned to each microservice at startup that correlates its logs with its Kubernetes pod name in the centralised log aggregator',
            'A database transaction ID that correlates all SQL statements executed during a single request, enabling atomic rollback if any step fails',
            'A hash of the request payload used to deduplicate retried requests at the API gateway, preventing duplicate processing by downstream services',
          ],
          correctIndex: 0,
          explanation: 'In a distributed system, a single user request flows through many services, each writing its own logs. Without a correlation ID propagated through all of them, finding all the log entries related to one failing request requires forensic archaeology. With it, you filter every service\'s logs on the same trace ID and see the complete picture.',
        },
        {
          question: 'What does the RED method define, and why are percentile latencies (p95, p99) more useful than average latency for alerting?',
          options: [
            'RED defines Rate (requests per second), Errors (error rate), and Duration (latency distribution). Percentiles reveal the worst-case experience for a fraction of users — averages hide tail latency because a few very slow requests are diluted by many fast ones',
            'RED defines Reliability, Efficiency, and Durability — three SLO dimensions used to set error budget thresholds. Percentiles are preferred because they map directly to SLO breach conditions defined in contracts',
            'RED defines Requests, Events, and Dependencies — the three signal types that a distributed tracing tool captures. Percentiles are preferred because p50 equals the median and is the most statistically robust central tendency measure',
            'RED defines Resource usage, Error logs, and Deployment frequency — the DORA metrics for DevOps. Percentiles are used because average CPU utilisation masks burst behaviour that causes OOM errors',
          ],
          correctIndex: 0,
          explanation: 'Rate, Errors, Duration (RED) are the three key metrics for any service. Average latency is misleading: if 99% of requests complete in 10ms and 1% take 10 seconds, the average might be 110ms — masking the fact that 1 in 100 users has a terrible experience. p99 latency reveals that 10-second tail directly.',
        },
        {
          question: 'Why must trace context be explicitly propagated through asynchronous boundaries such as message queue consumers and scheduled jobs?',
          options: [
            'Asynchronous processing breaks the implicit call chain — without explicit propagation of the trace ID in message headers or job metadata, the trace terminates at the queue and the downstream processing becomes invisible in distributed tracing tools',
            'Message queues and scheduled jobs run in separate processes that cannot access the HTTP request context, so trace IDs must be re-generated at each boundary to avoid ID collisions',
            'Most message queue implementations strip custom headers for security reasons — trace context must be embedded in the message body and re-extracted by the consumer',
            'Trace context propagation is only required for synchronous gRPC calls — HTTP and message queue communication uses separate correlation mechanisms defined by the broker',
          ],
          correctIndex: 0,
          explanation: 'A distributed trace is a causal chain: service A calls B, B publishes a message, C consumes it and calls D. If the trace ID is not included in the message headers, the trace breaks at the queue — you see the producer\'s span and the consumer\'s span as two unrelated traces, making it impossible to follow the full journey of a single user action.',
        },
        {
          question: 'What is the difference between a counter metric and a histogram metric, and when should you choose each?',
          options: [
            'A counter tracks a cumulative total (e.g., total requests served) and only increases; a histogram records the distribution of values (e.g., request latency) in configurable buckets, enabling percentile calculations — use counters for rates and histograms for latency SLOs',
            'A counter measures the current value of a resource (e.g., current CPU usage); a histogram plots counter values over time as a bar chart for visual dashboards',
            'A counter is reset to zero on every scrape interval; a histogram preserves all historical values permanently, making it suitable for long-term capacity planning',
            'A counter can only track integer values; a histogram supports floating-point values — choose based on whether the metric is an integer or decimal',
          ],
          correctIndex: 0,
          explanation: 'Counters are monotonically increasing values — total HTTP requests, total errors, total bytes sent. You derive the rate (requests per second) by computing the difference over time. Histograms record individual observations into pre-defined buckets, enabling percentile queries like "what is the p99 latency?" Choosing the wrong type means you either cannot compute rates (if you used a histogram for counts) or cannot compute percentiles (if you used a counter for latency).',
        },
        {
          question: 'Why is alerting on symptoms (e.g., high error rate, slow response times) preferred over alerting on causes (e.g., high CPU, full disk)?',
          options: [
            'Symptom-based alerts fire only when users are actually affected, reducing alert fatigue; cause-based alerts often fire for conditions that the system handles gracefully (e.g., high CPU during a batch job) and miss novel failure modes that manifest as user-visible symptoms without an obvious cause',
            'Symptom-based alerts are cheaper to compute because they require fewer metrics; cause-based alerts need access to infrastructure-level metrics that cost more to collect and store',
            'Symptom-based alerts can be configured without engineering involvement because they use business-level language; cause-based alerts require deep system knowledge',
            'Symptom-based alerts are only useful for frontend services; backend services should always alert on causes because they have no direct user-facing symptoms',
          ],
          correctIndex: 0,
          explanation: 'High CPU is a cause — it might or might not affect users. High error rate is a symptom — users are definitely having a bad time. If you alert on causes, you get paged for harmless spikes and miss failures that manifest differently than expected. If you alert on symptoms, every alert means real user impact. Cause-based metrics are still valuable for diagnosis after a symptom-based alert fires.',
        },
      ],
    },
    {
      topicId: 'ai-assisted-development-for-backend-engineers',
      topicTitle: 'AI-Assisted Development for Backend Engineers',
      objectiveIndex: 0,
      questions: [
        {
          question: 'AI coding assistants produce "plausible-looking code" rather than guaranteed-correct code. What does this mean in practice for a backend engineer reviewing generated code?',
          options: [
            'Generated code can have subtle logic errors, insecure patterns, or violations of codebase conventions that are not apparent from a quick read — the engineer must understand and verify the code before accepting it',
            'Generated code is statistically correct 95% of the time, so reviewing only the first and last lines of each function is sufficient for production use',
            'Generated code uses different variable naming conventions than the codebase, which is a purely cosmetic issue that a linter can fix automatically',
            'Generated code performs well on simple inputs but fails only on edge cases that never occur in production, so acceptance testing in staging is sufficient verification',
          ],
          correctIndex: 0,
          explanation: 'LLMs are statistical completion engines — they produce what looks like correct code based on patterns in training data, not by reasoning about correctness. A function can appear correct, compile without errors, and pass basic tests while containing a security vulnerability or an off-by-one error that only manifests under specific conditions. Critical review is always required.',
        },
        {
          question: 'What is test-driven prompting and why does it improve the quality of AI-generated backend code?',
          options: [
            'Ask the AI to write tests first based on the specification, then generate the implementation — this forces the spec to be made explicit and provides an immediate mechanism for validating that the implementation behaves correctly',
            'Run the AI-generated code against the existing test suite and prompt the AI to fix any failing tests, repeating until all tests pass without human review of the fixes',
            'Write tests manually after receiving the generated implementation, which ensures the tests are written by a human rather than the AI that might miss the same edge cases in both',
            'Ask the AI to generate test data only, then write both tests and implementation manually — AI-generated executable code introduces too much security risk even for test files',
          ],
          correctIndex: 0,
          explanation: 'If you ask an AI to implement a function without specifying its exact behaviour, you get what the model infers you want — which may not match what you actually need. Asking for tests first forces you to state the expected inputs, outputs, and error conditions precisely. The tests then act as an executable specification that validates the generated implementation.',
        },
        {
          question: 'For which backend tasks are AI assistants well-suited, and for which should they be used with significant caution or not at all?',
          options: [
            'Well-suited: generating CRUD endpoints, writing unit tests, producing SQL for well-defined schemas, explaining libraries. Caution required: designing system architecture, writing security-sensitive code (auth, crypto) without expert review',
            'Well-suited: designing system architecture and security components, because the AI has been trained on security best practices. Caution required: generating boilerplate, because the AI produces code that does not match the codebase style',
            'Well-suited: all backend tasks equally — AI assistants are general-purpose tools with no meaningful task-specific quality variance. Caution required: frontend tasks, which the AI has less training data for',
            'Well-suited: anything involving the full codebase context, because the AI can reason about cross-service dependencies. Caution required: isolated functions, because the AI lacks context to make correct design decisions for small scopes',
          ],
          correctIndex: 0,
          explanation: 'AI assistants excel at bounded, well-defined tasks where correctness can be verified quickly: CRUD endpoints, test generation, SQL queries, code explanation. They are poorly suited for system architecture (they lack knowledge of your specific constraints and history) and security-sensitive code (subtle vulnerabilities require expert review that the AI cannot provide).',
        },
        {
          question: 'Why is it a data privacy risk to paste proprietary business logic or production credentials into a public AI coding assistant?',
          options: [
            'Public AI tools process input on external servers and may use submitted content to improve future models — proprietary logic and credentials sent to these tools are potentially exposed to the vendor and future model outputs',
            'Public AI tools cache all inputs in plaintext browser storage, which is accessible to other tabs running on the same domain',
            'Public AI tools automatically publish submitted code snippets to a public repository for community feedback and training data attribution',
            'Public AI tools are prohibited from processing code containing credentials by their terms of service, causing them to reject the request and log the attempted submission',
          ],
          correctIndex: 0,
          explanation: 'When you paste code into a public AI tool, that content is sent to the vendor\'s servers and may be used for model training. Proprietary business logic becomes accessible to the vendor; credentials pasted in error create a credential exposure incident. Always check your organisation\'s policy on approved AI tools and never paste secrets into any external service.',
        },
      ],
    },
  ],
  exams: {
    beginner: [
      {
        question: 'What is the primary purpose of an API (Application Programming Interface)?',
        options: [
          'To provide a formal contract so two pieces of software can communicate without exposing internal implementation details',
          'To give users direct access to a database so they can query it freely',
          'To replace the need for a backend server by running logic in the browser',
          'To store application state between requests on the client side',
        ],
        correctIndex: 0,
        explanation: 'An API is a formal agreement between two pieces of software about how they will communicate. It hides implementation details — the client only needs to know the agreed requests and response shapes, not how the server stores or processes data.',
      },
      {
        question: 'Which property of REST makes it easy to scale horizontally?',
        options: [
          'Statelessness — each request carries all information the server needs, so any replica can handle any request',
          'JSON formatting — binary data is too large for multiple servers to process efficiently',
          'A single endpoint — all traffic goes to one URL which simplifies load balancing',
          'Persistent connections — the server maintains a socket with each client',
        ],
        correctIndex: 0,
        explanation: "REST's statelessness means the server stores no session state between requests. Because every request is self-contained, any server replica can respond to any request, which is what makes horizontal scaling straightforward.",
      },
      {
        question: 'What does an HTTP 404 status code indicate?',
        options: [
          'The requested resource was not found on the server',
          'The server encountered an unexpected internal error',
          'The request succeeded and a new resource was created',
          'The client sent a request without valid authentication credentials',
        ],
        correctIndex: 0,
        explanation: '404 Not Found is a 4xx client error indicating the server cannot locate the requested resource. Understanding status code classes (2xx success, 4xx client error, 5xx server error) is the first step to diagnosing production bugs from logs.',
      },
      {
        question: 'What is the fundamental difference between a compiled language and an interpreted language?',
        options: [
          'A compiled language translates source code to machine code before execution; an interpreted language executes source code line by line at runtime via an interpreter',
          'A compiled language is faster to write but slower to run; an interpreted language is slower to write but faster to run',
          'A compiled language requires a virtual machine; an interpreted language runs directly on the operating system without any runtime layer',
          'A compiled language can only be used for backend systems; an interpreted language is only suitable for scripting and automation tasks',
        ],
        correctIndex: 0,
        explanation: 'Compilation translates the entire program into machine code before it runs — the resulting binary executes directly on the CPU. Interpretation reads and executes source code at runtime via an interpreter process. Each approach has trade-offs in startup time, runtime performance, and deployment workflow.',
      },
      {
        question: 'Why is a relational database the right default choice for most transactional business data?',
        options: [
          'Relational databases enforce schema, referential integrity, and ACID transactions — guarantees that prevent data corruption and ensure consistency for financial, order, and user data',
          'Relational databases store data as unstructured JSON documents, making them the most flexible option for evolving application schemas',
          'Relational databases automatically partition data across multiple nodes, providing horizontal scaling without any manual configuration',
          'Relational databases eliminate the need for an application layer by exposing SQL endpoints that clients can query directly over HTTP',
        ],
        correctIndex: 0,
        explanation: 'ACID properties (Atomicity, Consistency, Isolation, Durability) are what make a relational database trustworthy for transactional data. Without them, partial writes and concurrent access produce corrupted data. Most business applications — orders, payments, user accounts — require these guarantees.',
      },
      {
        question: 'What is the main advantage of using a web framework like FastAPI or Django over writing a raw HTTP server?',
        options: [
          'Frameworks provide routing, request parsing, validation, and authentication patterns out of the box, eliminating repetitive boilerplate so engineers can focus on business logic',
          'Frameworks automatically scale the application to handle millions of requests per second without any infrastructure configuration',
          'Frameworks replace the database layer entirely by generating SQL queries from API endpoint definitions automatically',
          'Frameworks prevent all security vulnerabilities by sandboxing application code from the operating system',
        ],
        correctIndex: 0,
        explanation: 'Writing a raw HTTP server requires handling parsing, routing, error responses, and security concerns manually. Frameworks solve these problems once and let the team focus on the unique business logic of the application. Django and FastAPI also provide community-tested patterns for authentication, validation, and serialisation.',
      },
      {
        question: 'What problem does client-server architecture solve compared to running all logic on a single machine?',
        options: [
          'It separates concerns — clients handle presentation, servers handle data and logic — allowing each to be updated, scaled, and secured independently',
          'It eliminates network latency by co-locating the client and server in the same data centre',
          'It removes the need for authentication because the server can verify the client\'s identity from its IP address',
          'It prevents data loss by automatically replicating all client-side state to the server in real time',
        ],
        correctIndex: 0,
        explanation: 'Client-server architecture separates responsibilities: clients handle user interaction and presentation, servers handle data storage and business logic. This separation allows the server to be scaled independently, updated without touching client devices, and secured at a centralised point.',
      },
      {
        question: 'What is Object-Oriented Programming (OOP) and what problem do classes solve?',
        options: [
          'OOP organises code around objects that bundle state (data) and behaviour (methods) together; classes are blueprints for creating objects, reducing duplication and making it easier to model real-world concepts',
          'OOP is a performance optimisation technique that compiles frequently used functions into machine code at runtime',
          'OOP is a database design methodology that maps classes directly to database tables to simplify persistence logic',
          'OOP is a network protocol that allows objects in different programming languages to call each other\'s methods over HTTP',
        ],
        correctIndex: 0,
        explanation: 'Object-Oriented Programming models the world as objects — entities that hold state and expose behaviour. A class is the template; each object is an instance. Bundling state with behaviour and using inheritance to share common logic reduces duplication and makes large codebases more navigable.',
      },
      {
        question: 'What is the difference between a unit test and an integration test?',
        options: [
          'A unit test verifies a single function or class in isolation with all dependencies mocked; an integration test verifies that multiple components (e.g., application code and a real database) work correctly together',
          'A unit test is run by the developer locally; an integration test is run by the CI pipeline and requires a dedicated test environment',
          'A unit test uses the production database; an integration test uses an in-memory database to avoid polluting production data',
          'A unit test checks the API response format; an integration test checks the SQL queries generated by the ORM layer',
        ],
        correctIndex: 0,
        explanation: 'Unit tests are fast and isolated — they verify logic without real databases, HTTP calls, or file I/O by replacing those dependencies with mocks. Integration tests are slower but more realistic — they exercise actual component interactions, catching configuration mismatches and query correctness that unit tests cannot detect.',
      },
      {
        question: 'Why is SQL injection one of the most critical web security vulnerabilities, and how is it prevented?',
        options: [
          'SQL injection allows attackers to manipulate database queries by embedding SQL code in user input; it is prevented by using parameterised queries or an ORM that never concatenates user input directly into SQL strings',
          'SQL injection allows attackers to crash the database server by sending excessively long strings; it is prevented by enforcing a maximum input length on all form fields',
          'SQL injection exploits weak password hashing by pre-computing common SQL keywords; it is prevented by using bcrypt or Argon2 for all stored passwords',
          'SQL injection is an attack that intercepts SQL traffic between the application and database; it is prevented by encrypting the database connection with TLS',
        ],
        correctIndex: 0,
        explanation: "If user input is concatenated into a SQL string — `\"SELECT * FROM users WHERE id = \" + userId` — an attacker can set userId to `1 OR 1=1` and receive all rows, or `1; DROP TABLE users` to delete data. Parameterised queries pass user input as a separate value, never as part of the SQL syntax, eliminating the vulnerability.",
      },
      {
        question: 'What is the purpose of structured error handling (try/catch or try/except) in backend code?',
        options: [
          'To distinguish expected error conditions (validation failures, missing resources) from unexpected failures, handle them appropriately, and return meaningful responses rather than crashing or exposing stack traces',
          'To automatically retry failed operations until they succeed, eliminating the need for manual retry logic elsewhere in the codebase',
          'To prevent all runtime errors by validating function inputs at compile time before the application starts',
          'To log every line of code as it executes, creating a full audit trail of the program\'s execution path for debugging',
        ],
        correctIndex: 0,
        explanation: 'Backend code encounters many expected failure conditions: a record is not found, a third-party API is unavailable, input fails validation. Structured error handling lets you catch these, return an appropriate status code and message to the client, and log the right level of detail — without crashing the server or exposing internal stack traces to users.',
      },
      {
        question: 'What is a Docker container and how does it differ from a virtual machine?',
        options: [
          'A container packages an application with its dependencies and shares the host OS kernel, making it lightweight and fast to start; a virtual machine includes a full guest OS, making it heavier but providing stronger isolation',
          'A container is a cloud-specific deployment unit managed by the provider; a virtual machine is a local development tool that simulates production infrastructure on a developer\'s laptop',
          'A container provides hardware-level isolation by running a separate kernel; a virtual machine shares the host kernel and provides only process-level isolation',
          'A container is stateless and loses all data when stopped; a virtual machine persists state between reboots because it has a full filesystem',
        ],
        correctIndex: 0,
        explanation: 'Containers share the host OS kernel and isolate only the application process and its filesystem. This makes containers start in milliseconds and use far less memory than VMs. VMs boot a full guest operating system, providing stronger isolation at the cost of minutes to start and gigabytes of overhead per instance.',
      },
    ],
    mid: [
      {
        question: 'What distinguishes a well-designed API from a poorly designed one at the senior/mid engineer level?',
        options: [
          'A well-designed API uses consistent naming conventions, appropriate HTTP verbs, meaningful status codes, versioning from the start, and clear documentation — making it predictable and easy to consume without reading the source code',
          'A well-designed API minimises the number of endpoints by combining multiple resources into single compound endpoints, reducing the total number of network requests a client must make',
          'A well-designed API uses GraphQL instead of REST — the query language eliminates over-fetching and under-fetching, which are the two primary causes of poorly designed APIs',
          'A well-designed API uses binary serialisation formats like Protocol Buffers instead of JSON, because JSON is too verbose for production APIs with high throughput requirements',
        ],
        correctIndex: 0,
        explanation: 'API design is a long-lived contract with consumers. Consistent naming, correct HTTP verb semantics (GET is safe and idempotent), meaningful status codes, versioning, and documentation make an API predictable — consumers can infer behaviour from conventions rather than needing to read the implementation. Poorly designed APIs create support burden and breaking changes.',
      },
      {
        question: 'What is the critical distinction between authentication and authorisation?',
        options: [
          'Authentication verifies who a user is; authorisation decides what an authenticated user is allowed to do',
          'Authentication grants access to resources; authorisation verifies the user\'s identity via a token',
          'Authentication is performed by the client; authorisation is performed by the database',
          'Authentication uses JWTs; authorisation uses session cookies — they are different token formats for the same concept',
        ],
        correctIndex: 0,
        explanation: 'Authentication answers "who are you?" — verifying identity. Authorisation answers "what are you allowed to do?" — enforcing permissions. Conflating them leads to systems that verify identity but fail to enforce access control properly.',
      },
      {
        question: 'Why do relational databases use indexes, and what is the trade-off of adding too many?',
        options: [
          'Indexes allow the database to locate rows without a full table scan, dramatically speeding up reads; the trade-off is that every index must be updated on each write, increasing write latency and storage usage',
          'Indexes enforce data uniqueness constraints; the trade-off is that they prevent the database from storing duplicate values that some applications require',
          'Indexes compress column data to reduce storage costs; the trade-off is that compressed data takes longer to decompress during query execution',
          'Indexes cache the most recently accessed rows in memory; the trade-off is that the cache becomes stale and must be invalidated after every write to the table',
        ],
        correctIndex: 0,
        explanation: 'Without an index, a database scans every row to find matches — O(n). An index (typically a B-tree) allows O(log n) lookup. But each index is a separate data structure maintained on every INSERT, UPDATE, and DELETE. Tables with too many indexes have slow writes. Index selectively on columns used in WHERE, JOIN, and ORDER BY clauses.',
      },
      {
        question: 'What is the difference between SQL JOIN types — INNER JOIN, LEFT JOIN, and FULL OUTER JOIN?',
        options: [
          'INNER JOIN returns only rows with matches in both tables; LEFT JOIN returns all rows from the left table and matched rows from the right (NULLs for no match); FULL OUTER JOIN returns all rows from both tables with NULLs where no match exists',
          'INNER JOIN is the fastest because it scans only the smaller table; LEFT JOIN is slower because it must scan both tables; FULL OUTER JOIN is only supported in PostgreSQL and not in MySQL',
          'INNER JOIN enforces referential integrity; LEFT JOIN allows orphaned rows; FULL OUTER JOIN is equivalent to a UNION of two INNER JOINs on the same tables',
          'INNER JOIN combines column schemas from both tables; LEFT JOIN keeps the left table schema only; FULL OUTER JOIN merges all columns and all rows into a single wide table',
        ],
        correctIndex: 0,
        explanation: 'JOIN type determines which rows appear in the result. INNER JOIN: only rows with a match in both tables. LEFT JOIN: all left rows, with NULLs in the right columns where there is no match — useful for "find users with no orders". FULL OUTER JOIN: all rows from both tables, NULLs on either side where no match exists.',
      },
      {
        question: 'What is OAuth 2.0 and how does it enable third-party authorisation without sharing passwords?',
        options: [
          'OAuth 2.0 is an authorisation framework that lets a user grant a third-party application limited access to their resources by redirecting through the resource owner\'s identity provider, which issues a scoped access token without ever exposing the user\'s password to the third party',
          'OAuth 2.0 is a cryptographic protocol that encrypts passwords during transmission so third-party applications can authenticate on the user\'s behalf without storing the password in plaintext',
          'OAuth 2.0 is a database access control framework that issues per-query tokens to third-party services, allowing them to read specific tables without receiving database credentials',
          'OAuth 2.0 is a federated authentication system that synchronises user accounts across multiple services, allowing a single password to be used for all integrated applications',
        ],
        correctIndex: 0,
        explanation: "The OAuth 2.0 dance: the user authenticates with their identity provider (Google, GitHub), approves specific scopes, and the provider issues a scoped access token to the third-party app. The app never sees the user's password — only the token, which can be revoked independently and expires automatically.",
      },
      {
        question: 'What is load balancing and why is it essential for a horizontally scaled backend?',
        options: [
          'Load balancing distributes incoming requests across multiple server instances so no single instance is overwhelmed; it is essential because horizontal scaling only works if traffic is spread evenly across all instances',
          'Load balancing compresses response payloads to reduce bandwidth consumption when multiple clients make identical requests simultaneously',
          'Load balancing ensures that database queries are executed on the replica closest to the requesting server, minimising round-trip latency',
          'Load balancing synchronises session state across all server instances so users are not logged out when the load balancer routes them to a different server',
        ],
        correctIndex: 0,
        explanation: 'Adding more server instances only helps if requests are distributed across them. A load balancer sits in front of the fleet and routes each incoming request to an instance — typically via round-robin, least-connections, or IP hashing strategies. Without it, all traffic hits one instance regardless of how many are running.',
      },
      {
        question: 'What is caching and what problem does it solve for backend performance?',
        options: [
          'Caching stores the result of an expensive computation or database query in fast storage (memory) so subsequent requests can be served from the cache without repeating the work — it dramatically reduces latency and database load for frequently accessed data',
          'Caching pre-computes all possible database query results at startup and stores them in a lookup table, eliminating the need for a database at runtime',
          'Caching buffers write operations and batches them to the database, reducing the number of individual write transactions and improving write throughput',
          'Caching compresses static assets (images, scripts) at the edge so browsers receive smaller payloads, reducing page load time for end users',
        ],
        correctIndex: 0,
        explanation: "A cache hit serves from memory in microseconds instead of querying the database in milliseconds — orders of magnitude faster. But caches introduce stale data risk: the cached value may not reflect recent writes. Cache invalidation (deciding when to evict or update cached entries) is the hard part, summarised in the industry maxim \"there are only two hard things in computer science.\"",
      },
      {
        question: 'What makes GraphQL a better fit than REST for a frontend that must fetch data from multiple related resources in a single request?',
        options: [
          'GraphQL lets the client specify exactly which fields it needs from multiple related types in a single query, eliminating over-fetching and the multiple round-trips required to assemble the same data from separate REST endpoints',
          'GraphQL uses binary serialisation, making it faster than REST for large payloads; REST is only efficient for small JSON responses under 10KB',
          'GraphQL automatically caches all query results at the server, while REST requires explicit cache-control headers on every endpoint',
          'GraphQL eliminates the need for a backend server by executing queries directly against the database from the client using the GraphQL schema as an access control layer',
        ],
        correctIndex: 0,
        explanation: 'Fetching a social media profile with REST might require GET /users/42, GET /users/42/posts, GET /users/42/followers — three round-trips. GraphQL expresses this as a single query that returns exactly the user, their last 5 posts, and follower count in one response. This solves both under-fetching (needing multiple trips) and over-fetching (receiving unused fields).',
      },
      {
        question: 'What is database normalisation and when might you deliberately denormalise?',
        options: [
          'Normalisation organises data to eliminate redundancy by splitting it into related tables; denormalisation intentionally duplicates data to reduce expensive JOIN queries in read-heavy systems where query performance is the priority',
          'Normalisation converts string columns to numeric IDs to save storage; denormalisation reverses this by storing human-readable strings instead of IDs for simpler queries',
          'Normalisation is the process of indexing all columns in a table; denormalisation removes indexes from infrequently queried columns to reduce write overhead',
          'Normalisation enforces uniqueness constraints across tables; denormalisation relaxes these constraints to allow duplicate rows in high-write scenarios',
        ],
        correctIndex: 0,
        explanation: 'Normalisation reduces redundancy: a customer name is stored once in the customers table, not repeated in every order row. This prevents update anomalies. But JOINs have a cost. In analytics systems or read-heavy APIs where JOIN performance is critical, deliberate denormalisation — storing computed or duplicated data — trades storage for query speed.',
      },
      {
        question: 'What is Docker Compose and what problem does it solve in local development?',
        options: [
          'Docker Compose defines and runs multi-container applications using a YAML file — it solves the problem of manually starting and networking multiple services (app, database, cache, queue) that together form the local development environment',
          'Docker Compose is a CI/CD tool that automates the build, test, and deployment of Docker images to a container registry and production cluster',
          'Docker Compose is a service mesh that provides encrypted communication between containers in a Kubernetes cluster using mutual TLS',
          'Docker Compose is a container image optimisation tool that merges multiple Docker layers into a single compressed layer to reduce image size and pull times',
        ],
        correctIndex: 0,
        explanation: 'A typical backend application requires the app server, a Postgres database, a Redis cache, and maybe a message broker — all running and networked together. Without Compose, each service must be started manually with long docker run commands. Compose defines the entire stack in one YAML file: `docker-compose up` starts everything.',
      },
      {
        question: 'Why does the OWASP Top 10 place broken access control at the top of the vulnerability list?',
        options: [
          'Broken access control means users can access data or perform actions they should not be permitted to — it is the most common vulnerability found in real applications and directly leads to data breaches and unauthorised operations',
          'Broken access control refers to weaknesses in the OAuth 2.0 implementation that allow attackers to forge access tokens and impersonate other users across multiple services',
          'Broken access control occurs when the load balancer routes requests to the wrong server, causing one user\'s session to be served to a different user',
          'Broken access control is a database misconfiguration that grants all application users read/write access to all tables, bypassing row-level security policies',
        ],
        correctIndex: 0,
        explanation: 'Access control bugs are pervasive: an endpoint that should require admin privileges does not check, or an object ID in the URL can be swapped to access another user\'s data (IDOR — Insecure Direct Object Reference). These flaws are common, easy to exploit, and cause direct data breaches — hence the top ranking.',
      },
      {
        question: 'What is the difference between symmetric and asymmetric encryption, and when is each appropriate?',
        options: [
          'Symmetric encryption uses the same key to encrypt and decrypt (fast, suitable for bulk data); asymmetric encryption uses a public key to encrypt and a private key to decrypt (slower, suitable for key exchange and digital signatures)',
          'Symmetric encryption requires the sender and receiver to be on the same network; asymmetric encryption works across the public internet because it does not require a shared network segment',
          'Symmetric encryption is reversible; asymmetric encryption is a one-way hash function that cannot be decrypted, only compared against a stored hash',
          'Symmetric encryption is used for database storage; asymmetric encryption is used for password hashing — they serve the same purpose but in different storage contexts',
        ],
        correctIndex: 0,
        explanation: 'Symmetric encryption is fast — AES can encrypt gigabytes per second on modern hardware — making it suitable for bulk data. Asymmetric encryption (RSA, ECDSA) is much slower but solves the key distribution problem: you can share your public key openly. TLS uses asymmetric cryptography to establish a symmetric session key, then switches to symmetric for the actual data transfer.',
      },
    ],
    senior: [
      {
        question: 'What does the CAP theorem state, and what is the practical design choice it forces?',
        options: [
          'A distributed data store can guarantee at most two of Consistency, Availability, and Partition Tolerance; because network partitions always happen, the practical choice is between CP (consistency over availability) and AP (availability over consistency)',
          'A distributed data store must choose two of Consistency, Availability, and Performance; partition tolerance is not a real constraint because modern networks rarely partition',
          'A distributed data store can achieve all three properties simultaneously if the replication factor is set high enough to ensure a quorum is always available',
          'A distributed data store guarantees Consistency and Partition Tolerance by default; Availability is an optional property enabled by configuring read-repair',
        ],
        correctIndex: 0,
        explanation: 'Network partitions are a reality, not an edge case. The CAP theorem\'s practical implication is that when a partition occurs, you must choose: refuse to serve stale data (CP) or serve potentially stale data rather than return an error (AP). The right choice depends on the business domain — financial ledgers need CP; social feeds can tolerate AP.',
      },
      {
        question: 'What is a distributed monolith and why is it worse than either a true monolith or well-designed microservices?',
        options: [
          'A distributed monolith is a system split into multiple services that are so tightly coupled they cannot be deployed independently — it has all the operational complexity of microservices with none of the autonomy, making it harder to operate than a monolith and less flexible than microservices',
          'A distributed monolith is a monolith deployed across multiple geographic regions for disaster recovery — it is worse than microservices because region failover takes minutes rather than seconds',
          'A distributed monolith is a microservices architecture where all services are written in the same programming language, creating fragility when that language ecosystem changes',
          'A distributed monolith is a system where the database is the only distributed component — the application tier is a monolith, creating a bottleneck at the persistence layer',
        ],
        correctIndex: 0,
        explanation: 'Splitting into services without aligning boundaries with team autonomy and domain ownership produces a distributed monolith: services that share databases, require coordinated deployments, and cascade failures to each other. You incur all the complexity of distributed systems — network calls, distributed tracing, service discovery — without gaining independent deployability.',
      },
      {
        question: 'What is the Ubiquitous Language in Domain-Driven Design and what problem does it solve?',
        options: [
          'A shared vocabulary developed with domain experts and used consistently in code, conversations, and documentation — it prevents translation errors where a developer\'s term for a concept differs from the business\'s term, and those differences become bugs',
          'A standard API naming convention enforced across all microservices in an organisation to ensure that service endpoints follow the same URL pattern and field naming style',
          'A code generation tool that produces domain model classes from a formal business glossary document, keeping the codebase automatically aligned with the business vocabulary',
          'A natural language processing layer that translates business requirements written in plain English into executable code specifications without developer intervention',
        ],
        correctIndex: 0,
        explanation: 'When developers say "shipment" and the business says "delivery", every handoff requires translation — and each translation is an opportunity for a misunderstanding to become a defect. The Ubiquitous Language eliminates this by making the business and the code use the same words for the same things, so mismatches surface in conversation before they become bugs.',
      },
      {
        question: 'An API endpoint scans a list to check authorisation on every request. At 1,000 users it is fast; at 100,000 users it causes timeouts. What is the complexity class and the correct fix?',
        options: [
          'O(n) linear — the fix is to store authorised user IDs in a hash set, reducing each membership check to O(1) average-case regardless of the number of users',
          'O(n²) quadratic — the fix is to sort the list once at startup and use binary search, reducing each check to O(log n)',
          'O(log n) logarithmic — this is already efficient; the timeouts are caused by network latency, not the algorithm, and the fix is to add caching at the load balancer',
          'O(n log n) linearithmic — the fix is to partition the user list across multiple nodes so each node only checks a fraction of the total list',
        ],
        correctIndex: 0,
        explanation: 'Scanning a list for membership is O(n) — time grows linearly with list size. At 100,000 users, 100,000 comparisons per request across thousands of requests per second becomes catastrophic. Replacing the list with a hash set reduces each lookup to O(1) — it takes the same time whether there are 100 or 10 million entries.',
      },
      {
        question: 'What are the three pillars of observability and what distinct question does each answer in a distributed system?',
        options: [
          'Logs answer "what happened?" (discrete events); metrics answer "how often and how fast?" (aggregated numbers over time); traces answer "where did time go and which service caused the failure?" (the end-to-end request journey across services)',
          'Logs answer "how fast is the system?" (latency records); metrics answer "who is using the system?" (user session counts); traces answer "what errors occurred?" (structured error event records)',
          'Logs, metrics, and traces all answer the same question from different angles — they are redundant, and most teams operate effectively with centralised logging alone',
          'Logs answer "is the system up?" (availability checks); metrics answer "what code ran?" (call graph records); traces answer "how much does it cost?" (infrastructure spend per request)',
        ],
        correctIndex: 0,
        explanation: 'The three pillars are complementary, not redundant. Metrics tell you something is wrong (error rate spike at 2am). Logs tell you what happened at specific points (payment declined, order 42). Traces show which service in the chain was slow or failing. Without all three, production debugging is guesswork.',
      },
      {
        question: 'What does eventual consistency guarantee, and when is it the wrong choice?',
        options: [
          'It guarantees all replicas will converge to the same state given enough time with no new writes; it is wrong when reading stale data causes incorrect business outcomes — overselling inventory, double-spending funds, or showing incorrect account balances',
          'It guarantees convergence within a configurable time window (typically under 100ms); it is wrong when that window exceeds the network round-trip time between data centres',
          'It guarantees that writes are acknowledged by a majority of replicas before returning; it is wrong when write throughput requirements exceed the synchronous replication capacity',
          'It guarantees that reads always reflect writes from the same client session; it is wrong when multiple clients must coordinate writes to the same record simultaneously',
        ],
        correctIndex: 0,
        explanation: 'Eventual consistency means "replicas will agree eventually, not necessarily now." For social feeds, search indexes, and recommendation data, a brief window of staleness is acceptable. For financial balances, inventory counts, and any case where a stale read directly causes a business error, you need strong consistency — CP is the right trade-off.',
      },
      {
        question: 'What is test-driven prompting for AI-assisted development and why does it improve code quality?',
        options: [
          'Ask the AI to write tests based on the specification first, then generate the implementation — this forces the spec to be made explicit and provides an executable mechanism for validating the generated code',
          'Run AI-generated code against the existing test suite and prompt the AI to fix failures, iterating until all tests pass without reviewing the fixes for correctness',
          'Write tests manually after receiving the AI-generated implementation, ensuring the tests cover the specific paths the AI chose rather than the intended specification',
          'Use AI to generate test data only, then implement both tests and production code manually to prevent AI-generated executable code from reaching the codebase',
        ],
        correctIndex: 0,
        explanation: 'AI produces code that matches what it infers you want, not necessarily what you need. Asking for tests first forces you to state expected inputs, outputs, and edge cases explicitly. The tests become an executable specification — if the generated implementation passes them all, you have much stronger confidence than if you reviewed the code alone.',
      },
      {
        question: 'Why is the Strangler Fig pattern the preferred migration path from monolith to microservices?',
        options: [
          'New functionality is built as services while the monolith handles legacy paths, allowing incremental migration without a risky big-bang rewrite that requires coordinating a complete system replacement in a single release',
          'The monolith is split into microservices all at once during a planned maintenance window, minimising the period of architectural inconsistency between the old and new systems',
          'A strangler fig proxy rewrites the monolith\'s internal function calls into network calls automatically, converting it to microservices without changing application code',
          'The monolith is containerised first, then its modules are progressively extracted into separate services based on CPU profiling data that identifies the heaviest-loaded components',
        ],
        correctIndex: 0,
        explanation: 'Big-bang rewrites fail at high rates — coordinating a complete system replacement in one release is technically and operationally risky. The Strangler Fig avoids this: build new features as services from the start; migrate existing monolith features incrementally over months or years. At any point, the system is working — the monolith shrinks gradually rather than being replaced overnight.',
      },
      {
        question: 'What are the two properties required for dynamic programming to apply, and how does memoisation exploit them?',
        options: [
          'Optimal substructure (the optimal solution is built from optimal solutions to subproblems) and overlapping subproblems (a naive recursive solution recomputes the same subproblems repeatedly); memoisation caches each subproblem\'s result the first time it is computed, converting exponential time to polynomial',
          'Polynomial input size (the problem input must be expressible as an integer) and monotonic state transitions (each subproblem must be strictly smaller than the parent); memoisation stores the transition decisions to enable solution reconstruction',
          'A greedy choice property (local optima compose into global optima) and a recursive structure (the problem decomposes into subproblems of the same type); memoisation eliminates redundant subproblem calls in the greedy search',
          'Deterministic outputs (the function returns the same result for the same input) and bounded state (the number of unique inputs is finite); memoisation is simply function-level caching that works for any pure function meeting these criteria',
        ],
        correctIndex: 0,
        explanation: 'DP works when (1) the best answer to the whole problem can be assembled from best answers to subproblems (optimal substructure), and (2) naive recursion solves the same subproblem many times (overlapping subproblems). Memoisation stores each answer when it is first computed — subsequent calls for the same subproblem return the cached result instantly instead of recomputing.',
      },
      {
        question: 'Why must trace context be explicitly propagated across asynchronous boundaries like message queue consumers?',
        options: [
          'Asynchronous processing breaks the implicit call chain — without propagating the trace ID in message headers, the trace terminates at the queue and downstream processing appears as an unrelated trace, making it impossible to follow a single user action end-to-end',
          'Message queue brokers generate new trace IDs for each message to prevent ID collisions between concurrent consumers, and this new ID must be propagated to downstream services',
          'Trace context propagation is only required for gRPC calls — HTTP and message queue communication uses separate correlation mechanisms built into the broker protocol',
          'Most message brokers strip custom headers for security reasons, so trace context must be embedded in the message body and extracted by the consumer before it can be propagated',
        ],
        correctIndex: 0,
        explanation: 'A distributed trace is a causal chain. Service A produces a message; service B consumes it later. If the trace ID is not included in the message headers, tracing tools see two unrelated spans — A\'s and B\'s — with no link between them. You cannot tell which consumer processed which message, or how long the end-to-end user action took.',
      },
      {
        question: 'What is the RED method and why are p95/p99 latency percentiles more useful than average latency for alerting?',
        options: [
          'RED defines Rate (requests/s), Errors (error rate), and Duration (latency distribution); percentiles like p99 reveal the worst experience for a fraction of users — averages hide tail latency because a few very slow requests are diluted by many fast ones',
          'RED defines Reliability, Efficiency, and Durability — the three SLO dimensions; p99 is preferred because it maps directly to SLA breach conditions and is easier to reason about than a mean',
          'RED defines Requests, Events, and Dependencies — the three distributed tracing signal types; p95 is used because it matches the 95th percentile confidence interval standard in statistical process control',
          'RED defines Resource usage, Error logs, and Deployment frequency — the DORA DevOps metrics; percentiles are used because average CPU utilisation masks burst behaviour that causes OOM errors',
        ],
        correctIndex: 0,
        explanation: 'Rate, Errors, Duration (RED) are the key service-level metrics. If 99% of requests return in 10ms but 1% take 10 seconds, the average might be 110ms — hiding a terrible user experience for 1 in 100 requests. p99 latency exposes that 10-second tail directly, letting you alert on what users actually experience rather than on a misleading average.',
      },
      {
        question: 'What makes an Aggregate Root the sole entry point into a DDD aggregate, and what invariant does this enforce?',
        options: [
          'The Aggregate Root is the only object external code can reference directly; all state changes must go through it so the root can validate business invariants — such as ensuring an order total never goes negative — before any change is persisted',
          'The Aggregate Root is the database primary key that all related tables reference via foreign key constraints, ensuring referential integrity at the schema level',
          'The Aggregate Root is a read-only projection of the aggregate\'s event history, produced by replaying domain events to reconstruct the current state on each read',
          'The Aggregate Root is the top-level service in a microservices cluster responsible for orchestrating writes to all child services that share the same bounded context',
        ],
        correctIndex: 0,
        explanation: 'An aggregate is a consistency boundary: all objects within it must satisfy certain invariants (business rules) at all times. By making the Aggregate Root the only entry point, you ensure every modification goes through code that can check these invariants before the change is committed. Bypassing the root — accessing inner objects directly — can leave the aggregate in an invalid state.',
      },
    ],
  },
}
